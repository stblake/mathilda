# Task: Document Reduce (docstring, reference page, tutorials)

User's 4-part spec:
1. Improve the `Reduce` docstring (currently a terse paragraph; `Solve`'s is far richer).
2. Build a reference page for `Reduce` on the site (documentation/solutions-of-equations/).
3. Update tutorial `07-solutions-of-equations` with `Reduce` examples.
4. Add a separate "Solutions of Inequalities" tutorial.

## Key facts discovered
- Reduce docstring: `src/solve/reduce.c:231`. Solve's model: `src/solve/solve.c:1197`.
- Site reference pages are AUTO-GENERATED by `site/generate.py` (`make docs`), from:
  docstring (Description) + `docs/spec/builtins/*.md` (`## Name` sections, mined + run)
  + optional `site/overlays/<Name>.md` + `site/impl/<Name>.md`. Also writes `builtins.json`.
- `docs/spec/builtins/solutions-of-equations.md` ALREADY has a rich `## Reduce` section
  (773-1013). Reduce is absent from `builtins.json` only because the site is STALE.
- Tutorials verified with `python3 site/verify_tutorial.py check <file>`. Nav via
  awesome-pages + `site/docs/tutorials/.pages`. New tutorial: unnumbered
  `solutions-of-inequalities.md` slotted after 07 (precedent: computing-limits-gruntz.md).
- Book `\B{Reduce}` link fails because builtins.json lacks Reduce; `make docs` fixes it,
  then rebuild book link table (gen_links.py via `make -C book pdf`).

## Steps
- [ ] 1. Rewrite Reduce docstring in reduce.c to Solve quality; rebuild binary.
- [ ] 2. Create site/overlays/Reduce.md (worked examples verified against binary + notes + refs).
- [ ] 3. `make docs`; confirm documentation/solutions-of-equations/Reduce.md generated and
        Reduce in site/docs/assets/builtins.json.
- [ ] 4. Add a Reduce section to tutorial 07 (contrast Solve vs Reduce; complete solution sets;
        forward-link the inequalities tutorial). Verify with verify_tutorial.py.
- [ ] 5. Create solutions-of-inequalities.md tutorial; add to .pages + index.md card. Verify.
- [ ] 6. Rebuild book (gen_links + pdf); confirm `make -C book check-links` passes for Reduce.
- [ ] 7. Update current week's docs/spec/changelog file with a short note.

## Review — DONE
1. ✅ Reduce docstring rewritten to Solve quality (src/solve/reduce.c); binary rebuilt;
   `Information["Reduce"]` renders the structured form.
2. ✅ site/overlays/Reduce.md created (verified worked examples + Solve-vs-Reduce notes +
   4 literature references; status: Partial).
3. ✅ `make docs` regenerated the site → documentation/solutions-of-equations/Reduce.md
   (18 verified examples) and Reduce added to builtins.json. Whole-site regen touched 153
   files (site catching up to the binary since 2026-08-22 — kept, since selective revert
   would leave builtins.json/index/cross-links inconsistent).
4. ✅ Tutorial 07 gained a "Complete solution sets with Reduce" section; 3 pre-existing
   stale Solve transcripts refreshed. verify_tutorial: 57 inputs, 0 mismatches.
5. ✅ New solutions-of-inequalities.md (31 inputs, 0 mismatches); added to .pages + index card.
6. ✅ Book link table regenerated; `make -C book check-links` OK; PDF builds (65 pp).
7. ✅ Changelog note added to docs/spec/changelog/2026-08-24.md.

Note: `mkdocs build --strict` not run locally (mkdocs not installed) — CI runs it. All new
internal links point to existing files; icon is a real MDI glyph. No commit made.
