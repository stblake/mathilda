# Benchmark gap report — 2026-08-21

Generated by `make bench-gap` (`benchmarks/run_all.py`). Do not edit by hand.

## Headline

| | |
|---|---|
| Experiments run | 2 |
| Cases measured | 27 |
| **Function coverage** | **100.0%** (2 of 2 declared heads present) |
| Slower than best baseline | 0 |
| Ahead or within 1.5× | 21 |
| Absent (feature work) | 0 |
| Incomplete / timed out | 0 |
| Check disagreements | 6 |
| Wall clock | 1.7 min |

`SLOWER` cases carry a ratio and are kernel work. `ABSENT` cases carry **no ratio** and are feature work — see `ABSENT.md`. The two are never pooled: averaging "we do not have `DSolve`" into a speed number would turn a missing algorithm into a fake 40×.

## Trend since 2026-08-06

| | then | now | Δ |
|---|---|---|---|
| coverage | 88.2% | 100.0% | +11.8 pp |
| SLOWER | 94 | 0 | -94 |
| AHEAD | 106 | 21 | -85 |
| ABSENT | 3 | 0 | -3 |
| INCOMPLETE | 1 | 0 | -1 |
| CHECK-FAIL | 7 | 6 | -1 |

### Regressed (0)

_None._

### Improved (0)

_None._

### Coverage of the suite itself

- 27 new case(s) since 2026-08-06.
- 211 case(s) present then and missing now — check whether an experiment stopped emitting rather than assuming it passed.

## Ranked gaps — where the week's work is

_No case is 1.5× or more behind its best baseline._

## Where Mathilda leads or ties

| case | Mathilda | Mathematica | Python | ratio |
|---|---|---|---|---|
| `90-nminimize-testbed` · T6 bukin n6 | 0.261 ms | — | 394.7 ms | 0.00× |
| `89-nminimize-nmaximize` · D4 rosenbrock 5d (DE) | 1.4 ms | 913.4 ms | 531.6 ms | 0.00× |
| `89-nminimize-nmaximize` · A3 beale 2d (auto) | 0.093 ms | — | 34.2 ms | 0.00× |
| `89-nminimize-nmaximize` · D6 sphere 10d (DE) | 2.2 ms | 1.08 s | 575.2 ms | 0.00× |
| `89-nminimize-nmaximize` · D1 rastrigin 2d (DE) | 0.110 ms | 199.7 ms | 25.2 ms | 0.00× |
| `89-nminimize-nmaximize` · D5 levy 5d (DE) | 1.0 ms | 720.3 ms | 220.1 ms | 0.00× |
| `89-nminimize-nmaximize` · M3 wrapper base nminimize | 0.111 ms | 201.5 ms | 24.2 ms | 0.00× |
| `89-nminimize-nmaximize` · A1 branin 2d (auto) | 0.080 ms | — | 14.9 ms | 0.01× |
| `89-nminimize-nmaximize` · M4 wrapper same via nmaximize | 0.141 ms | 202.5 ms | 24.3 ms | 0.01× |
| `90-nminimize-testbed` · T5 styblinski-tang 5d | 0.505 ms | — | 83.8 ms | 0.01× |
| `89-nminimize-nmaximize` · M1 nmaximize styblinski 5d | 0.523 ms | — | 82.6 ms | 0.01× |
| `89-nminimize-nmaximize` · A2 six-hump camel (auto) | 0.083 ms | — | 11.8 ms | 0.01× |
| `90-nminimize-testbed` · T7 eggholder 2d | 0.172 ms | — | 16.5 ms | 0.01× |
| `89-nminimize-nmaximize` · D3 ackley 10d (DE) | 9.1 ms | 1.11 s | 731.8 ms | 0.01× |
| `89-nminimize-nmaximize` · D2 rastrigin 5d (DE) | 2.8 ms | 704.7 ms | 160.0 ms | 0.02× |
| `89-nminimize-nmaximize` · I1 rastrigin 5d explicit vars | 2.8 ms | 721.7 ms | 153.3 ms | 0.02× |
| `89-nminimize-nmaximize` · C3 mixed integer | 0.019 ms | 0.601 ms | 5.8 ms | 0.03× |
| `89-nminimize-nmaximize` · A4 cross-in-tray (auto) | 1.6 ms | — | 12.1 ms | 0.13× |
| `89-nminimize-nmaximize` · I2 rastrigin 5d indexed vars | 111.5 ms | 732.9 ms | 156.8 ms | 0.71× |
| `89-nminimize-nmaximize` · C2 eq constrained | 0.200 ms | 0.387 ms | 0.199 ms | 1.01× |
| `89-nminimize-nmaximize` · C1 ineq constrained | 0.243 ms | 0.946 ms | 0.198 ms | 1.23× |

## Check disagreements — timings withheld

These cases are **not** timing comparisons: the systems did not agree on the answer, so any number here would be comparing different programs. Fix the disagreement, then re-run.

| case | detail |
|---|---|
| `90-nminimize-testbed` · T1 schwefel 5d | check disagreement: Mathilda=0; Python=1 |
| `90-nminimize-testbed` · T2 griewank 5d | check disagreement: Mathilda=0; Python=1 |
| `90-nminimize-testbed` · T3 drop-wave 2d | check disagreement: Mathilda=1; Python=0; Mathematica=1 |
| `90-nminimize-testbed` · T4 rastrigin 10d | check disagreement: Mathilda=0; Python=1; Mathematica=1 |
| `90-nminimize-testbed` · F1 ineq feasibility | check disagreement: Mathilda=0; Python=1; Mathematica=1 |
| `90-nminimize-testbed` · F2 eq feasibility | check disagreement: Mathilda=0; Python=1; Mathematica=1 |

## Where the slowness is, by area

Median ratios, not means: one 600000× case would drag a mean so far that the column would say nothing about the other sixty. A median of 1.0 means the typical case in that area is at parity.

| area | cases | slower | ahead | absent | incomplete | median vs Mathematica | median vs Python |
|---|---|---|---|---|---|---|---|
| D uncovered subsystems | 27 | 0 | 21 | 0 | 6 | 0.00× | 0.01× |

Read the two right-hand columns against each other. Behind Mathematica is a gap against a *competitor* — the same evaluation semantics, the same automatic compilation — so it is an engineering gap. Behind Python is a gap against *the machine*: on the dense linear-algebra areas all three call the same Accelerate BLAS, so any spread there is pure overhead. A case can read acceptably against one and badly against the other, and that difference is usually the finding.

### The worst area, by count

- **D uncovered subsystems** — 0 slower cases. Worst: 

## Method

- One untimed warm-up, then the **minimum** of 3 timed runs (`AbsoluteTiming` / `perf_counter`; never `Timing[]`, which sums CPU time over threads). Cases already expensive on their own use one timed run and no warm-up.
- **One process per experiment per system**, cwd set to the experiment folder.
- The same `.m` source runs in Mathilda and Mathematica. The `.py` is a separate implementation of the same algorithm in the same order.
- **Values are checked, not just timed.** A case whose systems disagree is reported `CHECK-FAIL` with its timing discarded.
- Exit codes are ignored: Mathilda exits 0 after errors, so classification comes from parsed cases plus stderr.

### Caveat that applies to group A only

Symbolic timings are frequently dominated by **algorithm choice, not execution speed**. A group-A case reading 40× behind sympy usually means *sympy has a method we do not*, which is different work from the numeric findings (nearly all of which were "the fast path existed and was not taken"). Read group A cases together with `ABSENT.md`.

## ⚠ Build warning — read before trusting any row

This build is missing optional dependencies. A missing optional dependency here does not disable a feature — it **silently substitutes a slower algorithm**, so the affected cases measure the fallback, not Mathilda's real implementation. Do not open kernel work on these cases until the dependency is installed and the tree rebuilt.

- GMP-ECM not detected; building with USE_ECM=0 (advanced factorisation disabled)

## Host

| | |
|---|---|
| platform | `macOS-26.6.1-arm64-arm-64bit-Mach-O` |
| machine | `arm64` |
| processor | `arm` |
| mathilda | `Mathilda 0.076 (GCC 16.2.0, GMP 6.3.0, MPFR 4.2.2, FLINT 3.6.0, ECM 7.0.7, Raylib 5.5, Accelerate, Readline)` |
| python_bin | `/opt/homebrew/opt/python@3.14/bin/python3.14` |
| python_libs | `3.14.3 2.4.1 1.17.1 1.14.0` |
| wolframscript | `/usr/local/bin/wolframscript` |
| runner_python | `3.14.3` |

