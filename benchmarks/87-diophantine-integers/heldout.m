(* generated by validate.py -- do not edit; edit heldout.py *)
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -25 <= pt[[1]] <= 25 && -25 <= pt[[2]] <= 25 && -25 <= pt[[3]] <= 25];
  sol = Solve[{x + 2 y + 3 z == 10, x - y + z == 2}, {x, y, z}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tlin-sys-2eq-3var\t", status];
  If[status === "finite",
    tuples = ({x, y, z} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tlin-sys-2eq-3var\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y, z} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y, z} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tlin-sys-2eq-3var\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -14 <= pt[[1]] <= 14 && -14 <= pt[[2]] <= 14 && -14 <= pt[[3]] <= 14 && -14 <= pt[[4]] <= 14];
  sol = Solve[{a + b + c + d == 10, a - b + c - d == 2, a + b - c - d == 0}, {a, b, c, d}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tlin-sys-3eq-4var\t", status];
  If[status === "finite",
    tuples = ({a, b, c, d} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tlin-sys-3eq-4var\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({a, b, c, d} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({a, b, c, d} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tlin-sys-3eq-4var\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -20 <= pt[[1]] <= 20 && -20 <= pt[[2]] <= 20];
  sol = Solve[{x + y == 5, x - y == 1}, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tlin-sys-determined\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tlin-sys-determined\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tlin-sys-determined\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -20 <= pt[[1]] <= 20 && -20 <= pt[[2]] <= 20];
  sol = Solve[{x + y == 2, x - y == 1}, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tlin-sys-noninteger\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tlin-sys-noninteger\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tlin-sys-noninteger\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -40 <= pt[[1]] <= 40 && -40 <= pt[[2]] <= 40];
  sol = Solve[{2 x + 2 y == 3, x - y == 0}, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tlin-sys-int-inconsistent\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tlin-sys-int-inconsistent\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tlin-sys-int-inconsistent\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -30 <= pt[[1]] <= 30 && -30 <= pt[[2]] <= 30];
  sol = Solve[x^2 + x y - 2 y^2 == 4, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tfactorable-cross-4\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tfactorable-cross-4\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tfactorable-cross-4\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -60 <= pt[[1]] <= 60 && -60 <= pt[[2]] <= 60];
  sol = Solve[(x - y) (x + 2 y) == 15, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tfactorable-prod-15-empty\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tfactorable-prod-15-empty\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tfactorable-prod-15-empty\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -40 <= pt[[1]] <= 40 && -40 <= pt[[2]] <= 40];
  sol = Solve[2 x^2 + 3 x y - 2 y^2 == 7, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tfactorable-nonunit-7\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tfactorable-nonunit-7\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tfactorable-nonunit-7\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -30 <= pt[[1]] <= 30 && -30 <= pt[[2]] <= 30];
  sol = Solve[x^2 - y^2 == 21, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tdiff-of-squares-21\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tdiff-of-squares-21\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tdiff-of-squares-21\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -6 <= pt[[1]] <= 6 && -6 <= pt[[2]] <= 6];
  sol = Solve[x^2 + x y + y^2 == 7, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tellipse-rotated-7\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tellipse-rotated-7\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tellipse-rotated-7\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -6 <= pt[[1]] <= 6 && -6 <= pt[[2]] <= 6];
  sol = Solve[3 x^2 + 2 x y + 3 y^2 == 24, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tellipse-nonunit-24\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tellipse-nonunit-24\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tellipse-nonunit-24\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -12 <= pt[[1]] <= 12 && -12 <= pt[[2]] <= 12];
  sol = Solve[x^2 + x y + y^2 - 3 x == 7, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tellipse-linear-empty\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tellipse-linear-empty\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tellipse-linear-empty\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -20 <= pt[[1]] <= 20 && -20 <= pt[[2]] <= 20];
  sol = Solve[5 x^2 + 6 x y + 5 y^2 == 8, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tellipse-norep-empty\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tellipse-norep-empty\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tellipse-norep-empty\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, 1 <= pt[[1]] <= 400 && 1 <= pt[[2]] <= 400];
  sol = Solve[x^2 - 2 y^2 == 7 && x > 0 && y > 0, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tgenpell-2-7\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tgenpell-2-7\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tgenpell-2-7\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, 1 <= pt[[1]] <= 400 && 1 <= pt[[2]] <= 400];
  sol = Solve[x^2 - 5 y^2 == 4 && x > 0 && y > 0, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tgenpell-5-4\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tgenpell-5-4\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tgenpell-5-4\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, 1 <= pt[[1]] <= 400 && 1 <= pt[[2]] <= 400];
  sol = Solve[x^2 - 2 y^2 == 5 && x > 0 && y > 0, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tgenpell-2-5-empty\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tgenpell-2-5-empty\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tgenpell-2-5-empty\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, 1 <= pt[[1]] <= 4000 && 1 <= pt[[2]] <= 4000];
  sol = Solve[x^2 - 13 y^2 == -1 && x > 0 && y > 0, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tnegpell-13-solvable\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tnegpell-13-solvable\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tnegpell-13-solvable\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, 1 <= pt[[1]] <= 4000 && 1 <= pt[[2]] <= 4000];
  sol = Solve[x^2 - 3 y^2 == -1 && x > 0 && y > 0, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tnegpell-3-empty\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tnegpell-3-empty\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tnegpell-3-empty\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, 1 <= pt[[1]] <= 999 && -4000 <= pt[[2]] <= 4000];
  sol = Solve[y^2 == x^3 - 2 && 0 < x < 1000, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tmordell-neg2-boxed\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tmordell-neg2-boxed\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tmordell-neg2-boxed\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -499 <= pt[[1]] <= 499 && -499 <= pt[[2]] <= 499];
  sol = Solve[x^3 - 2 y^3 == 1 && Abs[x] < 500 && Abs[y] < 500, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tthue-cubic-boxed\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tthue-cubic-boxed\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tthue-cubic-boxed\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -200 <= pt[[1]] <= 200 && -200 <= pt[[2]] <= 200];
  sol = Solve[x^3 - 2 y^3 == 3, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tthue-cubic-m3-unbounded\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tthue-cubic-m3-unbounded\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tthue-cubic-m3-unbounded\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -200 <= pt[[1]] <= 200 && -200 <= pt[[2]] <= 200];
  sol = Solve[x^3 - 2 y^3 == 5, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tthue-cubic-m5-empty\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tthue-cubic-m5-empty\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tthue-cubic-m5-empty\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -20 <= pt[[1]] <= 20 && -20 <= pt[[2]] <= 20];
  sol = Solve[x^4 + x^3 y + x^2 y^2 + x y^3 + y^4 == 1, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tthue-cyclo-phi5-unbounded\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tthue-cyclo-phi5-unbounded\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tthue-cyclo-phi5-unbounded\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -20 <= pt[[1]] <= 20 && -20 <= pt[[2]] <= 20];
  sol = Solve[x^4 + y^4 == 17, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tthue-totallycomplex-x4y4-17\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tthue-totallycomplex-x4y4-17\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tthue-totallycomplex-x4y4-17\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -20 <= pt[[1]] <= 20 && -20 <= pt[[2]] <= 20];
  sol = Solve[x^4 + y^4 == 3, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tthue-totallycomplex-x4y4-3-empty\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tthue-totallycomplex-x4y4-3-empty\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tthue-totallycomplex-x4y4-3-empty\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -50 <= pt[[1]] <= 50 && -50 <= pt[[2]] <= 50];
  sol = Solve[x^4 - 10 y^4 == 1, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tthue-rank2-quartic-d10\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tthue-rank2-quartic-d10\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tthue-rank2-quartic-d10\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -50 <= pt[[1]] <= 50 && -50 <= pt[[2]] <= 50];
  sol = Solve[x^4 - 10 y^4 == -1, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tthue-rank2-quartic-d10-neg-empty\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tthue-rank2-quartic-d10-neg-empty\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tthue-rank2-quartic-d10-neg-empty\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -6 <= pt[[1]] <= 6 && -6 <= pt[[2]] <= 6];
  sol = Solve[x^2 + y^2 == 25, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\ttwo-squares-25\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\ttwo-squares-25\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\ttwo-squares-25\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -39 <= pt[[1]] <= 39 && -39 <= pt[[2]] <= 39 && -39 <= pt[[3]] <= 39];
  sol = Solve[3 x^2 + 5 y^2 == 7 z^2 && Abs[x] + Abs[y] + Abs[z] > 0 && Abs[x] < 40 && Abs[y] < 40 && Abs[z] < 40, {x, y, z}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tternary-legendre-empty\t", status];
  If[status === "finite",
    tuples = ({x, y, z} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tternary-legendre-empty\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y, z} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y, z} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tternary-legendre-empty\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, 1 <= pt[[1]] <= 29 && 1 <= pt[[2]] <= 29 && 1 <= pt[[3]] <= 29 && 1 <= pt[[4]] <= 29];
  sol = Solve[x^3 + y^3 + z^3 == w^3 && 0 < x < y < z < w < 30, {x, y, z, w}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tthree-cubes-eq-cube-30\t", status];
  If[status === "finite",
    tuples = ({x, y, z, w} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tthree-cubes-eq-cube-30\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y, z, w} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y, z, w} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tthree-cubes-eq-cube-30\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -60 <= pt[[1]] <= 60 && -60 <= pt[[2]] <= 60];
  sol = Solve[x^2 + 3 x y + y^2 == 11, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tbqf-nonsquare-disc-unbounded\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tbqf-nonsquare-disc-unbounded\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tbqf-nonsquare-disc-unbounded\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -3 <= pt[[1]] <= 60 && -500 <= pt[[2]] <= 500];
  sol = Solve[y^2 == x^3 + 17, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tmordell-pos-unbounded\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tmordell-pos-unbounded\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tmordell-pos-unbounded\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -50 <= pt[[1]] <= 50 && -50 <= pt[[2]] <= 50];
  sol = Solve[x^4 - 3 y^4 == 13, {x, y}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tthue-quartic-generalm-unbounded\t", status];
  If[status === "finite",
    tuples = ({x, y} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tthue-quartic-generalm-unbounded\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tthue-quartic-generalm-unbounded\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -12 <= pt[[1]] <= 12 && -12 <= pt[[2]] <= 12 && -12 <= pt[[3]] <= 12];
  sol = Solve[x^2 + y^2 == z^2, {x, y, z}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tternary-pythagorean-unbounded\t", status];
  If[status === "finite",
    tuples = ({x, y, z} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tternary-pythagorean-unbounded\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y, z} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y, z} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tternary-pythagorean-unbounded\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -12 <= pt[[1]] <= 12 && -12 <= pt[[2]] <= 12 && -12 <= pt[[3]] <= 12];
  sol = Solve[x^2 + y^2 == 2 z^2, {x, y, z}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tternary-2z2-unbounded\t", status];
  If[status === "finite",
    tuples = ({x, y, z} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tternary-2z2-unbounded\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y, z} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y, z} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tternary-2z2-unbounded\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -15 <= pt[[1]] <= 15 && -15 <= pt[[2]] <= 15 && -15 <= pt[[3]] <= 15];
  sol = Solve[x^2 + y^2 == 3 z^2, {x, y, z}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tternary-3z2-trivial\t", status];
  If[status === "finite",
    tuples = ({x, y, z} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tternary-3z2-trivial\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y, z} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y, z} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tternary-3z2-trivial\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, -20 <= pt[[1]] <= 20 && -20 <= pt[[2]] <= 20 && -20 <= pt[[3]] <= 20];
  sol = Solve[x^2 + y^2 == 65 z^2, {x, y, z}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tternary-multirep-decline\t", status];
  If[status === "finite",
    tuples = ({x, y, z} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tternary-multirep-decline\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y, z} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y, z} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tternary-multirep-decline\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, 1 <= pt[[1]] <= 20 && 1 <= pt[[2]] <= 200];
  sol = Solve[2^n - 7 == x^2 && n > 0 && x > 0, {n, x}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tramanujan-nagell-7\t", status];
  If[status === "finite",
    tuples = ({n, x} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tramanujan-nagell-7\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({n, x} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({n, x} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tramanujan-nagell-7\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, 1 <= pt[[1]] <= 20 && 1 <= pt[[2]] <= 5000];
  sol = Solve[3^n - 7 == x^2 && n > 0 && x > 0, {n, x}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tramanujan-nagell-outofscope\t", status];
  If[status === "finite",
    tuples = ({n, x} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tramanujan-nagell-outofscope\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({n, x} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({n, x} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tramanujan-nagell-outofscope\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
Module[{sol, status, tuples, sols, inbox},
  inbox = Function[pt, 1 <= pt[[1]] <= 24 && 1 <= pt[[2]] <= 24 && 1 <= pt[[3]] <= 24];
  sol = Solve[4/5 == 1/x + 1/y + 1/z && x > 0 && y > 0 && z > 0, {x, y, z}, Integers];
  status = Which[Head[sol] === Solve, "uneval", sol === {}, "empty",
    FreeQ[sol, C] && FreeQ[sol, ConditionalExpression], "finite", True, "param"];
  Print["STATUS\tegyptian-4-5-unordered\t", status];
  If[status === "finite",
    tuples = ({x, y, z} /. sol);
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tegyptian-4-5-unordered\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
  If[status === "param",
    (* C[1] ranges over ALL integers for a linear-lattice family; a Pell family carries ConditionalExpression[_, C[1] >= 0] so its negative-kk substitutions become non-integer and drop out of the VectorQ filter. A multi-parameter family (e.g. the ternary-quadratic C[1],C[2],C[3]) is enumerated over a 3-D grid with a modest per-axis range. *)
    tuples = If[FreeQ[sol, C[2]] && FreeQ[sol, C[3]],
      Flatten[Table[Simplify[({x, y, z} /. sol) /. C[1] -> kk], {kk, -60, 60}], 1],
      Flatten[Table[({x, y, z} /. sol) /. {C[1] -> k1, C[2] -> k2, C[3] -> k3}, {k1, -16, 16}, {k2, -16, 16}, {k3, -16, 16}], 3]];
    sols = Select[tuples, VectorQ[#, IntegerQ] && inbox[#] &];
    Print["SOLS\tegyptian-4-5-unordered\t", InputForm[Sort[DeleteDuplicates[sols]]]]];
];
