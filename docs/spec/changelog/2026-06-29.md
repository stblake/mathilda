# Changelog: week of 2026-06-29 (Mon) – 2026-07-05 (Sun)

Feature additions and fixes recorded during this week.

## Limit: Method option (2026-07-04)

`Limit` now accepts a `Method` option that selects which internal strategy runs.
The evaluator has always been a cascade of strategy layers (`src/calculus/limit.c`,
`compute_limit`); those layers are now grouped under six user-facing names.

- **`Options[Limit]`** gains `Method -> Automatic` (alongside `Direction` and
  `Assumptions`).
- **`Method -> Automatic`** (default) runs the full cascade in order, exactly as
  before — behaviour is unchanged for existing inputs.
- **Named methods** — `"Substitution"`, `"RationalFunction"`, `"Series"`,
  `"LHospital"`, `"Asymptotic"` (the `ArcTan`/`Log`/dominant-term/`f^g` layers),
  and `"Bounded"` (envelope + oscillation) — restrict the **outermost** call to
  that one group. If the group resolves nothing, the whole `Limit` is left
  **unevaluated**; an unrecognised value emits `Limit::method` and is likewise
  left unevaluated. The restriction is confined to depth 1 (a new `method` field
  on `LimitCtx`, gated by `depth == 1`), so recursive sub-limits — one-sided
  probes, L'Hospital iterations, `Abs` splitting — keep the full cascade.
- Docstring, `docs/spec/builtins/calculus.md` (new `## Limit` section), the
  calculus tutorial, and the site overlay/impl pages updated. New tests in
  `tests/test_limit.c` (`test_method`) cover per-method success, the
  fail-⇒-unevaluated contract, unknown-method handling, and the depth-1
  recursion carve-out. Valgrind-clean (no leaks beyond the macOS init baseline).

## RootReduce: WL-faithful algebraic-number canonicalisation (G1–G5) (2026-07-04)

Brought `RootReduce` up to the core Wolfram-Language contract. Previously it only
rationalised radical denominators over a parametric function tower; it now also
canonicalises **constant algebraic numbers** and threads over relations. New
engine `src/poly/flint_qqbar.c` backed by FLINT `qqbar` (exact real/complex
algebraic numbers); `src/rootreduce.c` becomes a dispatcher.

- **G1 — canonical form for constant algebraic numbers.** An expression of
  integers, rationals, radicals, roots of unity, the imaginary unit and `Root[]`
  objects (no free symbol) folds bottom-up into one `qqbar`, then emits a
  **rational**, a **quadratic radical**, or `Root[Function[minpoly&], k]`
  (degree ≥ 3). `RootReduce[Sqrt[2]+Sqrt[3]]` → `Root[1 - 10 #1^2 + #1^4 &, 4]`;
  provably-equal nested radicals canonicalise identically. The `Root` index `k`
  uses Wolfram ordering (reals ascending, then non-reals) — indices match WL on
  the reference examples (including the degree-15 Root-product example).
- **G2 — nested radicals reduce.** `RootReduce[(Sqrt[18]+Sqrt[27])/Sqrt[5+2Sqrt[6]]]`
  → `3`; `RootReduce[(Sqrt[2]+Sqrt[3]+Sqrt[6]+3)/Sqrt[5+2Sqrt[6]]]` → `1+Sqrt[3]`.
- **G3 — diagnostics.** Wrong argument count emits `RootReduce::argx`; an unknown
  `Method` value emits `RootReduce::mtd`; the call is left unevaluated.
- **G4 — threads over relations/logic.** `Equal`, `Unequal`, `Less`,
  `LessEqual`, `Greater`, `GreaterEqual`, `And`, `Or`, `Not`, `Xor`, `Implies`.
  Binary (in)equalities of constant algebraic numbers are decided exactly via
  `qqbar` (`qqbar_equal` / `qqbar_cmp_re`): the Wolfram equality example
  `Sqrt[2]+Sqrt[3]+Sqrt[5] == Sqrt[10+2Sqrt[15]+4Sqrt[4+Sqrt[15]]]` → `True`.
- **G5 — `Method` option.** `Options[RootReduce] = {Method -> Automatic}`.
  `"Recursive"`/`"Automatic"` fold `qqbar` arithmetic directly; `"NumberField"`
  builds a primitive element of the common field and re-expresses the value via
  `qqbar_express_in_field`. All three agree (verified on the degree-21 example).
- **Bug fixed while wiring the `Root[]`-object input path:** the univariate
  poly-Expr → `fmpz_poly` builder reused a temp polynomial across `Plus`/`Times`
  terms without zeroing it in the variable case, so `t^3 + t + c` came out as
  `2 t^3 + …` (wrong minimal polynomial, and a resulting arithmetic blow-up).
- Dispatch is additive: parametric towers (free-variable radicands, e.g. the
  Goursat `k^(1/3)` families) still route to `flint_algebraic_field_canonical`;
  all prior `rootreduce_tests` remain green. `test_rootreduce.c` extended with
  G1–G5 cases. Degree cap (120) guards against pathological `qqbar` blow-up.

## RootReduce + WL-faithful Together/Cancel over cube-and-higher-root towers (2026-07-04)

Milestone B of the genuine-algebraic FLINT programme: full **field arithmetic**
(not just the zero test) over the tower `K = Q(params)(α_1..α_r)`, delivering two
user-visible capabilities.

- **New builtin `RootReduce`** (`src/rootreduce.c`, engine
  `flint_algebraic_field_canonical` in `src/poly/flint_bridge.c`). Rewrites an
  algebraic expression in canonical form with the **denominator rationalised** —
  cleared of every radical / root-of-unity generator. `K` is a finite-dimensional
  `Q(params)`-vector space with the monomial basis
  `{ gen_0^{e_0} … gen_{r-1}^{e_{r-1}} : 0 ≤ e_i < d_i }` (`d_i` = degree of
  `gen_i`'s minimal polynomial: `Q_i` for a radical, `deg Φ_{2Q_i}` for a root of
  unity). Multiplication-by-the-denominator is a `Q(params)`-linear map — each
  product reduced to normal form via `fmpz_mpoly_divrem_ideal` — and inverting it
  with a linear solve over the field `Q(params)` (`gr_mat_nonsingular_solve` over
  `fmpz_mpoly_q`) gives `N/D` with denominator `Norm_{K/Q(params)}(D)`, free of
  every generator, over which the coordinate solutions are recombined (lcm of
  their denominators) into a single fraction. Rigorous and complete when the
  minimal polynomials are irreducible; **no numeric zero oracle**.
  Examples:
  - `RootReduce[1/(1 + k^(1/3))]` → `(1 - k^(1/3) + k^(2/3))/(1 + k)`
  - `RootReduce[1/(a + b k^(1/3))]` → `(a² - a b k^(1/3) + b² k^(2/3))/(a³ + b³ k)`
  - `RootReduce[1/(1 + Sqrt[2])]` → `-1 + Sqrt[2]`
  - `RootReduce[1/(a + b Sqrt[2] + c Sqrt[3])]` →
    `(…)/(a⁴ - 4 a² b² + 4 b⁴ - 6 a² c² - 12 b² c² + 9 c⁴)`

  Listable, Protected. Leaves an expression with no algebraic generator unchanged
  (matching WL). Idempotent.

- **`Together`/`Cancel` now combine a sum of cube-and-higher-root fractions**
  (`flint_algebraic_field_together`, wired into `flint_cancel_fraction`). The
  radicals are treated as **free kernels** — `Power[base, p/q] → gen^p`, combined
  over a common denominator in lowest terms via `fmpz_mpoly_q` — so this keeps
  radicals in the denominator and does **not** rationalise (WL-faithful,
  generalising the sqrt-only `flint_parametric_field_normalize` to any index).
  It fires only on `Plus` inputs with genuine (symbol/polynomial-radicand)
  generators, so it never pre-empts the relation-aware single-fraction GCD path.
  - `Together[1/(x - k^(1/3)) + 1/(x + k^(1/3))]` → `2 x/(x² - k^(2/3))`
    (previously left uncombined)
  - `Cancel[(x^3 - k)/(x - k^(1/3))]` → `k^(2/3) + k^(1/3) x + x²` (unchanged —
    single-fraction path still handles relation-dependent cancellation)
  - constant-radical (`Sqrt[2]`) sums still combine via the number-field path

Tests: `tests/test_rootreduce.c` (`rootreduce_tests`) — exact canonical forms,
value preservation cross-checked by the **independent** ideal-reduction zero test
(`flint_algebraic_field_normalize` agrees with the matrix inversion), rationalised
denominators, idempotence, WL-faithful Together/Cancel, and a stress batch of
towers. No regressions across the FLINT / rat / simp / Goursat / extension /
radical / trigrat / intrat suites.

## Rigorous FLINT reduction over genuine-algebraic parametric towers (2026-07-04)

`Simplify`/`Together`/`Cancel` could not verify the cube-root Goursat identity
`D[Integrate[f,x],x] - f == 0` (with `f = (2-(k+1)x)/((1-(k+1)x)(x(1-x)(1-kx))^(1/3))`):
the derivative-minus-integrand is a rational function over the tower
`Q(x,k)(α,β,ω)` with `α = (x(1-x)(1-kx))^(1/3)` (a **genuine** degree-3 algebraic
generator — polynomial radicand), `β = k^(1/3)`, and `ω = (-1)^(1/3)`. This is the
parametric-algebraic regime the sqrt-only path (`flint_parametric_*`, which only
accepts `Sqrt[symbol]`) and the constant-radicand number-field path both reject,
so it fell to the classical paths, which under-reduce or exhaust memory.

- **New engine** `flint_algebraic_field_normalize` (`src/poly/flint_bridge.c`).
  Models `K = Q(params)(α_1..α_r)` as the quotient ring `Q[params, gens]/I` with
  `I = (gen^q − radicand, Φ_{2Q}(ω), …)`. Detects genuine-algebraic generators
  (radicals `Power[base, p/q]` of **any** index `q ≥ 2` whose radicand carries a
  free symbol — cube roots and polynomial/symbol radicands — plus roots of unity
  `(-1)^(p/q)`), canonicalising radicands by `Expand` so two printed forms of one
  radicand (`x(1-x)(1-k x)` vs `x - x² - k x² + k x³`) collapse to a single
  generator. With the generators ordered as the leading LEX variables the monic
  minimal polynomials form a Gröbner basis (pairwise-coprime leading monomials),
  so `fmpz_mpoly_divrem_ideal` yields the canonical normal form. An expression
  written `N/D` over `Q[params,gens]` is zero in `K` iff `N` reduces to 0 modulo
  `I` — a **rigorous** zero test (sound always; complete for irreducible minimal
  polynomials), with **no numeric zero oracle** (`Simplify` never calls
  `PossibleZeroQ`) and no primes/CRT.

- **Wired into `Cancel`/`Together`** (`flint_cancel_fraction`, `src/rat.c`) as the
  first whole-expression try. It returns the integer `0` exactly when the input is
  identically zero in the field, else `NULL` — so it can only *add* the ability to
  reach `0` rigorously and never worsens an existing reduction. `Simplify` picks
  this up through its `Together`/`Cancel` transforms. `Simplify[D[F,x]-f]`,
  `Together[…]`, `Cancel[…]` now all give `0`, while perturbed / genuinely-nonzero
  variants (e.g. `D[F,x]-2f`, `(x(1-x)(1-kx))^(1/3)+k^(1/3)`) are left untouched.

- Tests: `test_algebraic_field_normalize` + `test_goursat_identity_reduces` in
  `tests/test_flint_bridge.c` (direct zero/NULL contract and the end-to-end
  Goursat identity, including a non-zero guard). Regression suites green.

## Cube-root Goursat: third-kind logarithmic part + Cancel/Together fix (2026-07-03)

Two connected changes let `Integrate[]` close the pseudo-elliptic cube-root
family `(2 - (k+1) x)/((1 - (k+1) x) (x (1-x)(1-k x))^(1/3))` — including with a
**symbolic parameter `k`** — both through the default cascade and through
`Method -> "GoursatAlgebraic"`.

- **General `Cancel`/`Together` fix over algebraic number fields** (`src/rat.c`).
  `Cancel[Together[e, Extension -> Automatic], Extension -> Automatic]` failed to
  reduce a rational function whose coefficients carry algebraic denominators
  (e.g. `1/Sqrt[3]`) over a multi-generator field such as `Q(I, Sqrt[3]) =
  Q(zeta_12)`: FLINT's number-field GCD (`flint_cancel_fraction`) and the QA
  tower path both returned the fraction un-cancelled. Fixes: (1)
  `cancel_with_extension` now `Expand`s numerator/denominator before the
  `PolynomialGCD`, so an unexpanded product of algebraic-coefficient factors
  reduces; (2) a new `cancel_auto_gcd_quotient` reduces a single fraction
  directly through `PolynomialGCD` + `PolynomialQuotient` with
  `Extension -> Automatic` (each re-detects the whole splitting field), run as a
  strict-shrink first pass ahead of the FLINT/tower routing for algebraic-atom
  inputs. Plain-rational cancellation is untouched. The concrete failure — the
  cube-root descent's `R(t(z))(1-z)^3` coming back as degree-6-over-`(z-1)^3`
  with a zero `z^3` coefficient — now reduces to the correct cubic.

- **Third-kind cube-root logarithmic-part reduction** (`goursat_cubic_thirdkind`
  in `src/calculus/integrate_goursat.c`). When the order-3 eigendescent
  obstruction `H1 != 0` but the cofactor `F` has a pole at a NON-branch point,
  `F/R^(1/3)` is still elementary and its antiderivative is the logarithmic part
  `C * Sum_{j=0..2} omega^(2 j) Log[R^(1/3) - omega^j kappa t]`
  (`kappa = (lead R)^(1/3)`, `omega = e^(2 pi i/3)`). The coefficient direction
  `(1, omega^2, omega)` is forced by killing the trace and `omega^1` components;
  the reduction differentiates the ansatz modulo `Y^3 = R`, checks the `Y^0`/`Y^1`
  slots vanish, and reads the scale `C = F N / (R p2)` (`N = R - (lead R) t^3`) by
  sampling. Tried before the eigendescent (cheaper, and avoids its budget-
  exhausting parametric Möbius canonicalisation); every result is verified by the
  differentiate-back guard, so the polynomial-cofactor / genuinely-elliptic cases
  (`t/(t^3-1)^(1/3)`) still decline. Tests: `test_integrate_goursat.c::test_thirdkind`.

  Known limitation: over the two-generator field `Q(kappa, omega)` the descent's
  canonicalisation exercises a pre-existing, bounded (size-scaled, per-call) leak
  in the extension tower LCM/`Together` path; the new integrator code itself is
  valgrind-clean. Only the `m = 0` linear form (F's pole a root of
  `N = R - (lead R) t^3`) is handled; a general affine form `kappa t + m` is
  deferred.

## New infinite Sum / Product closed-form families (2026-07-03)

Six new algorithmic families close a batch of previously-unevaluated infinite
`Sum[]`/`Product[]` inputs, plus a warning fix. Each is a self-contained stage
in the existing cascades (`src/sum/sum.c`, `src/product/product.c`).

- **`Sum`Dirichlet`** (`src/sum/sum_dirichlet.c`) — Dirichlet series of recognised
  multiplicative arithmetic functions map to ratios of Riemann zeta values:
  `Sum[MoebiusMu[k]/k^s] = 1/Zeta[s]`, `Sum[LiouvilleLambda[k]/k^s] = Zeta[2s]/Zeta[s]`,
  `Sum[EulerPhi[k]/k^s] = Zeta[s-1]/Zeta[s]`, `Sum[DivisorSigma[0,k]/k^s] = Zeta[s]^2`,
  `Sum[DivisorSigma[0,k]^2/k^s] = Zeta[s]^4/Zeta[2s]`, `Sum[DivisorSigma[a,k]/k^s] =
  Zeta[s] Zeta[s-a]`, `Sum[MoebiusMu[k]^2/k^s] = Zeta[s]/Zeta[2s]`. Table-driven;
  even zetas fold (e.g. `Sum[LiouvilleLambda[k]/k^2] = Pi^2/15`). Runs before
  `Sum`Rational`.
- **`Sum`ZetaSeries`** (`src/sum/sum_zetaseries.c`) — sums of functions of
  `Zeta[a k + b]` by interchanging summation order: `Sum[Zeta[k]-1, {k,2,Inf}] = 1`,
  `Sum[(-1)^k (Zeta[k]-1), {k,2,Inf}] = 1/2`, `Sum[Zeta[2k]-1, {k,1,Inf}] = 3/4`;
  unblocks `Product[Exp[Zeta[k]-1], {k,2,Inf}] = E` via `Product`LogSum`.
- **`Sum`LogZeta` generalised** (`src/sum/sum_logzeta.c`) — now emits the inert
  `-c Derivative[1][Zeta][s]` for any integer `s >= 2` (previously only the `s==2`
  Glaisher form), so `Sum[Log[k]/k^3] = -Zeta'[3]` and, via `Product`LogSum`,
  `Product[k^(1/k^3)] = Exp[-Zeta'[3]]`.
- **`Sum`EulerNonlinear`** (`src/sum/sum_euler_nonlinear.c`, `sum_euler_internal.h`)
  — nonlinear Euler sums (products of `HarmonicNumber[k,p]`, and `PolyGamma[m,k]`
  factors) reduced to polynomials in Riemann zeta values via multiple zeta value
  reduction, gated to total weight `<= 7` (first irreducible MZV is `zeta(6,2)` at
  weight 8; heavier sums fall through, never a wrong value). Pipeline: quasi-shuffle
  (stuffle) expansion → `T`/`That` lift → depth-1/2 reduction (Euler, reflection,
  odd-weight double-zeta — shared with `Sum`Euler`) + a curated weight-<=7 depth->=3
  table. Examples: `Sum[HarmonicNumber[k]^3/k^4]`, `Sum[HarmonicNumber[k]
  HarmonicNumber[k,2]/k^3]`, `Sum[PolyGamma[1,k]^2] = 3 Zeta[3]`. Runs after the
  narrow `Sum`Euler` (existing fast paths untouched).
- **`Product`QProduct` infinite branch** (`src/product/product_qproduct.c`) —
  `Product[1 - a q^(b k + c), {k,imin,Inf}] = QPochhammer[a q^(b imin+c), q^b]`, e.g.
  `Product[1 - q^k] = QPochhammer[q,q]`, `Product[1/(1-x^k)] = 1/QPochhammer[x,x]`,
  `Product[1 - q^(2k)] = QPochhammer[q^2,q^2]`.
- **`Product`BesselZero`** (`src/product/product_besselzero.c`) + new symbol
  **`BesselJZero`** (`src/special_functions/besseljzero.{c,h}`) — the Bessel
  Hadamard product `Product[1 - x^2/BesselJZero[n,k]^2, {k,1,Inf}] =
  Gamma[n+1] (2/x)^n BesselJ[n,x]`, so `Product[1 - x^2/BesselJZero[0,k]^2] =
  BesselJ[0,x]`. `BesselJZero` stays symbolic for now.
- **`Product`Infinite` warning fix** (`src/product/product_infinite.c`) — the
  speculative Weierstrass-sinh recogniser (`try_sinh_family`) now skips
  symbolic-power bodies and mutes its speculative `Simplify`, eliminating the ~24
  spurious `Power::infy: 1/0` warnings on
  `Product[(1+x/k)Exp[-x/k], {k,1,Inf}]` (answer unchanged).

Tests in `tests/test_sum_product_families.c` (29 checks); `tests/test_sum_log.c`
updated for the new `Sum`LogZeta` output. Not addressed (deferred): the Chudnovsky
1/π series, Rogers–Ramanujan q-series, sums over `ZetaZero`, and the Glaisher-type
log product `Product[(1+1/k)^k Exp[-1+1/(2k)]]` (needs LogGamma/Barnes-G asymptotic
expansion in the Series/Limit engine).

## PrimeNu: number of distinct prime factors (2026-07-03)

New number-theory builtin `PrimeNu[n]` (`src/numbertheory/primenu.c`) giving
`nu(n)`, the number of **distinct** prime factors of `n`. It is the companion to
`PrimeOmega[n]` (`Omega(n)`, prime factors with multiplicity) and shares the
same factoring machinery — the implementation is `primeomega.c` with the final
reducer returning the *count* of factors rather than the exponent sum. `nu` and
`Omega` coincide exactly when `n` is square-free.

- Computed from the prime factorisation via the shared `df_factor_mpz` helper;
  machine integers and GMP bigints (e.g. `PrimeNu[50!] -> 15`) are handled
  uniformly. `PrimeNu[1]` and `PrimeNu[-1]` are `0`; the sign of `n` is ignored
  (`nu(-n) = nu(n)`). A prime power gives `1` (`PrimeNu[32] -> 1`).
- Gaussian integers: `PrimeNu[n, GaussianIntegers -> True]`, or a non-real
  `Complex[a, b]` argument, factors over `Z[i]` via `df_gaussian_prime_factor`
  and counts distinct Gaussian prime factors (a rational prime `p ≡ 1 (mod 4)`
  splits into two conjugate primes, so `PrimeNu[105, GaussianIntegers -> True]`
  is `4` versus `PrimeNu[105] -> 3`; `PrimeNu[3 + I] -> 2`).
- Additive on coprime arguments (`nu(m n) = nu(m) + nu(n)` when `GCD[m,n] == 1`);
  for a square-free `n`, `MoebiusMu[n] == (-1)^PrimeNu[n]` and
  `LiouvilleLambda[n] == (-1)^PrimeNu[n]`.
- Attributes `Listable`, `Protected`. Non-integer or zero `n` is left
  unevaluated; a wrong argument count emits `PrimeNu::argt`.
- Docstring in `info.c`; symbol `SYM_PrimeNu` registered in `sym_names.{c,h}`;
  unit tests in `tests/test_primenu.c` (valgrind-clean — no leak frames in the
  new code, macOS dyld/libobjc baseline noise and vendored-ECM `possibly lost`
  blocks only).

## Fix: FactorInteger mislabelled prime powers of a large prime as prime (2026-07-03)

`FactorInteger[(2^61-1)^2]` returned `{{5316911983139663487003542222693990401,
1}}` — declaring the composite square of a Mersenne prime to be prime. The root
cause was in `factorize_mpz` (`src/facint.c`): a prime power `p^k` of a large
prime defeats the general search methods (Pollard rho collides on the repeated
factor, and ECM has no small cofactor to grab and cannot crack the large `p`
directly), so the "gave up" fallback added `n` with exponent `1`, silently
lying about primality. `PrimeQ` was unaffected (it uses `mpz_probab_prime_p`
directly); the same latent bug also corrupted `EulerPhi`, `DivisorSigma`, and
`Divisors` on such inputs, since they share `factorize_mpz`.

- Added a **perfect-power reduction** at the top of `factorize_mpz` (after the
  primality test, before method dispatch): if `mpz_perfect_power_p(n)`, extract
  exact integer roots to a fixpoint to reduce `n = base^mult` to a primitive
  base, factor the base once into a private list, then merge the base's factors
  with each exponent scaled by `mult`. This sidesteps the search entirely for
  the whole prime-power / perfect-power class.
- Handles multi-prime bases and stacked exponents: `(2^61-1)^3`, `(2^61-1)^6`,
  `6^10 -> {{2,10},{3,10}}`, `(2^31-1)^2 (2^61-1)^3`, and negatives all factor
  correctly. Genuine Mersenne primes (`2^127-1`) still return `{{p, 1}}`.
- The `FactorInteger[n, k]` factor-count cap and explicit `Method ->` selections
  are respected inside the new branch (the merge honours `k_limit`).
- The residual limitation (a hard composite the bounded search can't split —
  e.g. a semiprime of two large distinct primes) no longer fails silently: the
  `Automatic` give-up path now emits a `FactorInteger::nofac` warning to stderr
  before returning the composite with exponent 1. The warning honours the shared
  `arith_warnings_muted()` gate, so it stays quiet inside numeric integration,
  `Limit`, `Series`, and other contexts that mute arithmetic messages.

## PrimeOmega: number of prime factors with multiplicity (2026-07-03)

New number-theory builtin `PrimeOmega[n]` (`src/numbertheory/primeomega.c`)
giving `Omega(n)`, the number of prime factors of `n` counted with multiplicity
(the exponent sum in the factorisation). It is the additive companion to the
newly added `LiouvilleLambda[n] = (-1)^Omega(n)`, and reuses the same factoring
machinery — the implementation is `liouvillelambda.c` with the final reducer
returning `Omega` itself instead of `(-1)^Omega`.

- Computed from the prime factorisation via the shared `df_factor_mpz` helper;
  machine integers and GMP bigints (e.g. `PrimeOmega[30!] -> 59`) are handled
  uniformly. `PrimeOmega[1]` and `PrimeOmega[-1]` are `0`; the sign of `n` is
  ignored (`Omega(-n) = Omega(n)`).
- Gaussian integers: `PrimeOmega[n, GaussianIntegers -> True]`, or a non-real
  `Complex[a, b]` argument, factors over `Z[i]` via `df_gaussian_prime_factor`
  and counts Gaussian prime factors with multiplicity (so
  `PrimeOmega[12, GaussianIntegers -> True]` is `5` from `(1+i)^4 3`, and
  `PrimeOmega[5 + 9 I]` is `2`).
- Attributes `Listable`, `Protected`. Non-integer or zero `n` is left
  unevaluated; a wrong argument count emits `PrimeOmega::argt`.
- Docstring in `info.c`; symbol `SYM_PrimeOmega` registered in
  `sym_names.{c,h}`; unit tests in `tests/test_primeomega.c` (valgrind-clean —
  no leak frames in the new code, macOS dyld/libobjc baseline noise only).

## LiouvilleLambda: Liouville function (2026-07-03)

New number-theory builtin `LiouvilleLambda[n]` (`src/numbertheory/liouvillelambda.c`)
giving the Liouville function `lambda(n) = (-1)^Omega(n)`, where `Omega(n)` counts
the prime factors of `n` with multiplicity. It is completely multiplicative and
returns `1` (even number of prime factors) or `-1` (odd).

- Computed from the prime factorisation via the shared `df_factor_mpz` helper;
  machine integers and GMP bigints (e.g. `LiouvilleLambda[10^30 + 1]`) are
  handled uniformly. The sign of `n` is ignored.
- Gaussian integers: `LiouvilleLambda[n, GaussianIntegers -> True]`, or a
  non-real `Complex[a, b]` argument, factors over `Z[i]` via
  `df_gaussian_prime_factor` and counts Gaussian prime factors with
  multiplicity (so `LiouvilleLambda[2, GaussianIntegers -> True]` is `1`).
- Attributes `Listable`, `Protected`. Non-integer/zero `n` is left unevaluated;
  a wrong argument count emits `LiouvilleLambda::argt`.
- Docstring in `info.c`; symbol `SYM_LiouvilleLambda` registered in
  `sym_names.{c,h}`; unit tests in `tests/test_liouvillelambda.c` (valgrind-clean).

## TimeConstrained: async-signal-safe abort (fixes intermittent SIGILL crash) (2026-07-03)

`TimeConstrained` aborts an over-budget computation with a `siglongjmp` taken
from its asynchronous `SIGPROF` handler. `SIGPROF` can land on any instruction —
including deep inside `malloc`/`realloc`/`free` while libmalloc holds an internal
zone lock. Unwinding past that point never releases the lock, so the *next*
allocation deadlocks; on macOS libmalloc detects the still-owned lock and traps
with `_os_unfair_lock_recursive_abort`, i.e. `Illegal instruction: 4`.

This was reproducible (~1 in 8 runs) on integrands whose algebraic-extension
`Together`/`Cancel` churns giant GMP integers, e.g.

```
Integrate[(2 - (k + 1) x)/((1 - (k + 1) x) (x (1 - x) (1 - k x))^(1/3)), x,
          Method -> "GoursatAlgebraic"]
```

The 6 s Goursat budget expires while a multi-megabyte `__gmpz_mul` is inside
`large_malloc`, leaking the large-zone lock.

**Fix** (`src/core.c`, `src/core.h`): the large allocations that leak the lock
originate exclusively from GMP (and MPFR, which shares GMP's allocators), so GMP
is routed through guarded memory functions (`mp_set_memory_functions`, installed
first thing in `core_init`). While a real `malloc`/`realloc`/`free` is in flight
a `tc_gmp_alloc_busy` flag is set; the `SIGPROF` handler then *defers* the jump
(records the timeout and returns) instead of unwinding mid-lock, and the
allocator takes the deferred jump from `tc_alloc_safepoint()` the instant the
real call returns and releases libmalloc's lock. Code outside a GMP allocation
keeps the immediate async jump, so `TimeConstrained` still interrupts long pure-C
GMP loops (e.g. `FactorInteger` on a large composite) — now at the next
allocation boundary rather than at an arbitrary lock-holding instruction.

Verified: 60/60 crash-free on the reproducer (was ~12.5 %); `TimeConstrained`
still returns `$Aborted` on a genuine CPU-time overrun; `core_tests` and
`integrate_goursat_tests` pass; heavy bignum/MPFR paths (`FactorInteger`,
`Factorial[2000]`, `N[Sqrt[2], 1000]`, `Together`, `GCD`) unaffected. The
underlying algebraic-extension `Together` slowness on this integrand is a
separate, tracked performance limitation.

## Docs: two advanced tutorials — symbolic summation & infinite products (2026-07-03)

Added `site/docs/tutorials/11-symbolic-summation.md` and
`12-infinite-products.md`, capstone tutorials that showcase the infinite `Sum`
and `Product` cascades shipped this week. Each is worked end to end with
historical context and the underlying algorithm named for every example
(Faulhaber, Gosper telescoping, the rational–`Zeta`/`Coth` master identity,
Dirichlet/`LerchPhi`, Euler-sum double-zeta reduction, Fourier, hypergeometric
`π`-machines; Weierstrass sine/sinh factorization, Viète, Euler prime products,
Gamma reflection, Glaisher/`ζ'`). All 67 `In[]`/`Out[]` transcripts are verified
against the current binary. Two "frontier" cases that `Product` leaves held
(the quartic `(k⁴-1)/(k⁴+1)` and the Stirling product `(1+1/k)^(k+1/2)/e`) are
resolved numerically with `NProduct`, and the tutorial corrects two commonly
mis-quoted closed forms in the process (`π sinh π/(cosh √2π − cos √2π)`, and
`e/√(2π)` / `e^(γ−1)` — not their reciprocals). Wired into the tutorials
`.pages` nav and `index.md`; the stale "finite sums only" note in
`08-calculus.md` was updated to cross-link the new pages.

## Product: seven infinite-product families + Gamma reflection (2026-07-02)

Infinite `Product[...]` now closes a broad set of classes, each a general
algorithm for its type, mirroring the `Sum[]` cascade. New stages (all under
`src/product/`, wired in `src/product/product.c`), plus two supporting `Sum`
stages and reusable Gamma identities:

- **`Product`RationalInfinite`** — convergent infinite rational products with
  complex-conjugate roots, via the Gamma canonical form. Factors numerator /
  denominator over `Q` (irreducible quadratics resolved by the quadratic
  formula → exact complex roots), checks the convergence conditions (unit
  leading constant, balanced degrees, balanced root sums), assembles
  `∏ Γ(a−β)^n / ∏ Γ(a−α)^m`, and reduces it in C (integer shift, cancellation,
  conjugate→hyperbolic, real reflection) with a numeric guard against the raw
  Gamma product. `Product[(k^2-1)/(k^2+1), {k,2,∞}] → Pi/Sinh[Pi]`,
  `Product[(k^3-1)/(k^3+1), {k,2,∞}] → 2/3`,
  `Product[1+1/(2k-1)^2, {k,1,∞}] → Cosh[Pi/2]`. Irreducible quartics (roots with
  irrational real part) stay held.
- **`Product`Viete`** — cosine double-angle telescoping `∏ Cos[a(i)]` with the
  angle halving each step → `Sin[2 a(imin)]/(2 a(imin))`.
  `Product[Cos[Pi/2^(k+1)], {k,1,∞}] → 2/Pi`, `Product[Cos[x/2^k]] → Sin[x]/x`.
- **`Product`Cantor`** — double-exponential telescoping
  `∏(1 + x^(2^i)) = 1/(1 - x^(2^imin))` for `|x|<1`.
  `Product[1+(1/3)^(2^k), {k,0,∞}] → 3/2`.
- **`Product`EulerPrime`** — Euler products over the primes:
  `∏ 1/(1 - Prime[i]^-s) → Zeta[s]` (`Pi^2/6`, `Pi^4/90`, …), and the `chi_4`
  product `→ Pi/4` (Dirichlet `L(1, chi_4)`). Divergent (`s ≤ 1`) stays held.
- **`Product`LogSum`** — general `∏ f = Exp[Sum[PowerExpand[Log[f]]]]` bridge
  (runs last). `Product[Exp[(-1)^k/k]] → 1/2`, `Product[k^(1/k^2)] → Exp[-Zeta'[2]]`
  (Glaisher form), `Product[E^(1/k)(1-1/k), {k,2,∞}] → E^(EulerGamma-1)`.
- **`Product`Geometric` (extended)** — infinite bound now allowed for a pure
  `base^(summable exponent)` product: `Product[2^(k/2^k), {k,1,∞}] → 4`.
- **`Sum`LogZeta`** — `Sum[c Log[k]/k^s] = -c Zeta'[s]`; elementary (Glaisher)
  closed form for `s = 2`, unblocking `Product[k^(1/k^2)]`.
- **`Sum`LogRational`** — convergent `Sum[rational + Log[rational]]` via matched
  digamma / `LogGamma` asymptotics. `Sum[1/k + Log[(k-1)/k], {k,2,∞}] = EulerGamma-1`.
- **Gamma transforms (`src/internal/simp/transforms/gamma.m`)** — reusable,
  guarded `FullSimplify` pair-rules: reflection `Γ[z]Γ[1-z] = Pi/Sin[Pi z]`
  (non-integer `z`), and conjugate pairs `Γ[1±ib] = Pi b/Sinh[Pi b]`,
  `Γ[1/2±ib] = Pi/Cosh[Pi b]`. `FullSimplify[Gamma[1/4]Gamma[3/4]] → Pi Sqrt[2]`,
  `FullSimplify[Gamma[1+I]Gamma[1-I]] → Pi Csch[Pi]`.

New helper `prod_rational_roots` (`src/product/product_util.c`) resolves
quadratic factors to exact complex roots. New tests:
`tests/test_product_infinite_families.c` (51), `tests/test_sum_log.c` (16),
`tests/test_gamma_transforms.c` (19); all existing product/sum/simp/gamma suites
still pass. New Product/Sum stages are valgrind-clean; a pre-existing leak in the
older `Sum` stages is documented in `SUM_LEAKS.md`.

## Sum: trigonometric, non-diagonal/quadratic Euler, Ramanujan 1/Pi (2026-07-02)

Four more infinite-`Sum` families now close, each a general algorithm for its
type (with honest bounds where full generality is impossible).

- **Trigonometric sums (new `Sum`Trigonometric` stage).** Fourier-type
  `Sum[T(k)/k^s, {k, imin, Infinity}]` with `T` a trigonometric polynomial in
  `k`. `TrigReduce` linearises `T`, then each single-angle term maps via
  `Sum Sin[a k]/k^s = Im PolyLog[s, e^{i a}]` / `Sum Cos[a k]/k^s = Re PolyLog[…]`;
  at `s = 1` it collapses to elementary form. `Sum[Sin[k]/k] -> (Pi-1)/2`,
  `Sum[Cos[k]/k] -> -Log[2 Sin[1/2]]`, `Sum[Sin[k]/k^2] -> Im PolyLog[2, e^i]`
  (Wolfram parity). Divergent (`s=1` DC term) and symbolic-coefficient inputs
  stay held. `src/sum/sum_trigonometric.c`, wired in `src/sum/sum.c`.
- **Non-diagonal linear Euler sums, odd weight.** `Sum[HarmonicNumber[k,p]/k^q]`
  with `p != q`, `p+q` odd, via the Borwein–Borwein–Girgensohn double-zeta
  reduction (`sigma_h(p,q) = Z(q,p) + Zeta[p+q]`, with the reflection for even
  outer `q`). `Sum[HarmonicNumber[k,2]/k^3] -> 3 Zeta[2] Zeta[3] - 9/2 Zeta[5]`.
  Even weight has no zeta reduction and stays held. `src/sum/sum_euler.c`.
- **Quadratic Euler sums.** `Sum[HarmonicNumber[k]^2/k^q]` for `q = 2..5`
  (weight `<= 7`) from a rigorous per-weight table:
  `q=2 -> 17/4 Zeta[4]` (= `17 Pi^4/360`), etc. Weight 8 (`q >= 6`) has an
  irreducible MZV and stays held. `src/sum/sum_euler.c`.
- **Ramanujan 1/Pi series (table-backed).** `Sum[(4k+1) Binomial[2k,k]^3/(-64)^k,
  {k,0,Infinity}] -> 2/Pi`. The `Sum`Hypergeometric` `|z|=1` convergence gate
  now passes a boundary case through **only when the pFq genuinely reduces**
  (never fabricating the divergent continuation); `HypergeometricPFQ` gained the
  very-well-poised recognizer `4F3({1/2,1/2,1/2,5/4},{1/4,1,1},-1) -> 2/Pi`.
  General `1/Pi` summation is an open problem, so this is a recognizer for the
  known central-binomial-cubed class, not universal.
  `src/sum/sum_hypergeometric.c`, `src/special_functions/hypergeopfq.c`.
- **`ipow` overflow fix (`src/power.c`).** Integer `Power` returned `64^22 = 0`
  (and similar) — a silent `__int128` wrap for perfect-power bases past `2^127`.
  `ipow` now keeps `|b| <= INT64_MAX` across iterations and routes overflow to
  the bigint path. Also unblocks numeric verification of `(-64)^k` series.
- **Tests.** `tests/test_sum_trigonometric.c` (new, 14), extended
  `tests/test_sum_euler.c`, `tests/test_sum.c`, `tests/test_hypergeopfq.c`; no
  regression. Memory-clean.

## Sum: binomial / hypergeometric-term sums (2026-07-02)

`Sum[…/Binomial[2k, k], …]` now reaches a closed form.
`Sum[2^k/Binomial[2k,k], {k,1,∞}]` → `1 + Pi/2`, `{k,0,∞}` → `2 + Pi/2`,
`Sum[1/Binomial[2k,k], {k,0,∞}]` → `4/3 + 2 Pi/(9 Sqrt[3])`.

- **Term-ratio through binomials (C1).** `Sum`Hypergeometric` now expands
  `Binomial[a, b] -> a!/(b!(a-b)!)` in the summand before forming the term ratio
  (Mathilda's simplifier reduces `Factorial` ratios but not `Binomial`/`Gamma`
  ones), so a binomial hypergeometric term is recognised and mapped to
  `HypergeometricPFQ`. `src/sum/sum_hypergeometric.c`.
- **2F1 arcsin family (C2).** `HypergeometricPFQ` gained the central-binomial
  reductions `2F1(1,1;1/2;z)`, `2F1(1,1;3/2;z)`, `2F1(2,1;3/2;z)` (arcsin closed
  forms, valid as analytic continuations for all `z`), so the resulting `pFq`
  collapses to an elementary constant. `src/special_functions/hypergeopfq.c`.
- **Tests.** `tests/test_sum.c` (63 pass) + `tests/test_hypergeopfq.c`; no
  regression. Memory-clean.

## Sum: alternating rational sums (new `Sum`Alternating` stage) (2026-07-02)

New closed-form stage for infinite **alternating rational sums**
`Sum[sigma (-1)^k R(k), {k, imin, Infinity}]`, reduced to elementary constants:
`Sum[(-1)^(k+1)/k, …]` → `Log[2]`; `Sum[(-1)^k/(2k+1), …]` → `1/4 (Pi - 4)`;
`Sum[(-1)^k/(2k+1)^2, {k,0,∞}]` → `Catalan`; also `Sum[(-1)^k/k^2, …]` →
`-Pi^2/12`, `Sum[(-1)^k/(2k+1)^3, …]` → `Pi^3/32`.

- **Algorithm.** Detect `(-1)^(a k + b)` (`a` odd, `sigma = (-1)^b`); partial-
  fraction `R` over Q; each linear pole `c0/(b1 k + b0)^m` →
  `c0 b1^{-m} (-1)^imin LerchPhi[-1, m, imin - rho]` (`rho = -b0/b1`). Convergence
  gate `deg q >= deg p + 1`. Complex/radical poles and divergent inputs stay
  unevaluated. `src/sum/sum_alternating.c`, dispatched before `Sum`Rational`;
  `Method -> "Alternating"`.
- **Supporting LerchPhi / PolyGamma reductions** (make the results elementary):
  - `LerchPhi[-1, 1, a]` now uses the finite digamma form
    `(1/2)(PolyGamma[0,(a+1)/2] - PolyGamma[0,a/2])` instead of the divergent
    two-`Zeta` `0/0` (`src/special_functions/lerchphi.c`).
  - **Gauss digamma theorem**: `PolyGamma[0, p/q]` at positive rational `p/q` now
    closes into an elementary `EulerGamma/Log/Cot/Cos·Log·Sin` form
    (`src/special_functions/polygamma.c`), e.g. `PolyGamma[0,3/4]-PolyGamma[0,1/4]
    = Pi`.
  - **Dirichlet beta**: `LerchPhi[-1, s, 1/2] = 2^s beta(s)` for integer `s ≥ 2`
    (Catalan at `s = 2`, rational·`Pi^s` at odd `s`), with the half-integer
    recurrence for higher `a`; even `s ≥ 4` falls back to the `Zeta` form.
- **Tests:** `tests/test_sum_alternating.c` (12 pass); no regression in
  `sum_tests`/`sum_rational_tests`/`sum_euler_tests`. Memory-clean (valgrind: no
  leak frames in the new code).

## Sum: linear Euler sums (new `Sum`Euler` stage) (2026-07-02)

New closed-form stage for infinite **linear Euler sums**
`Sum[c HarmonicNumber[k, p]/k^q, {k, 1, Infinity}]` in Riemann zeta values.
`Sum[HarmonicNumber[k]/k^2, {k, 1, Infinity}]` → `2 Zeta[3]`;
`…/k^3` → `Pi^4/72`; `Sum[HarmonicNumber[k, 2]/k^2, …]` → `7 Pi^4/360`.

- **Closed forms.** Order 1 (`H_k`) via Euler's formula
  `Sum H_k/k^q = (1 + q/2) Zeta[q+1] - (1/2) Sum_{j=1}^{q-2} Zeta[j+1] Zeta[q-j]`
  for all `q >= 2`; the diagonal `Sum H_k^{(p)}/k^p = (Zeta[p]^2 + Zeta[2p])/2`
  from the reflection identity. Even zeta values reduce to powers of `Pi` via the
  `Zeta` builtin. General non-diagonal `sigma_h(p, q)` (odd weight), nonlinear
  sums (`H_k^2`), and divergent `q < 2` are left unevaluated (no fabricated
  values).
- **Wiring.** `src/sum/sum_euler.c` (builtin `Sum`Euler`, `HOLDALL`-free stage,
  `PROTECTED | READPROTECTED`), dispatched in `Automatic` after `Sum`Rational`
  and before `Sum`Hypergeometric`; `Method -> "Euler"`. Registered in
  `src/sum/sum.c`. Tests: `tests/test_sum_euler.c` (9 pass).

## Sum: infinite polynomial × geometric sums (2026-07-02)

`Sum[p(k) r^k, {k, k0, Infinity}]` with `|r| < 1` now evaluates to its exact
rational closed form instead of falling through to a `HypergeometricPFQ`. For
example `Sum[k^2/2^k, {k, 0, Infinity}]` → `6`, `Sum[k^3/2^k, {k, 0, Infinity}]`
→ `26`, `Sum[1/2^k, {k, 0, Infinity}]` → `2`.

- **Mechanism.** `Sum`Geometric` already builds the antidifference `F(k) = q(k) r^k`
  for finite ranges. For an infinite range with `|r| < 1` the boundary term
  `q(n+1) r^(n+1) -> 0`, so the sum is exactly `-F(imin)`. The stage now takes
  this branch after a convergence gate (`|r| < 1` decided via `Abs[r] < 1`);
  symbolic `r` or `|r| >= 1` (including the divergent `r = -1`/`r = 1` edges) is
  left unevaluated rather than given a false analytic continuation.
- **Geometric-factor detection generalised.** `find_geometric` now recognises a
  geometric factor by the representation-independent ratio test `g(i+1)/g(i)`
  free of `i`, so `2^(-k)` (parsed as `(2^k)^(-1)`), `(1/2)^k`, `r^(a i + b)`,
  etc. are all handled. The polynomial part is accumulated by multiplying in
  each geometric factor's var-free constant `C = g|_{i->0}` (rather than
  dividing the summand by a reconstructed `r^i`, which failed to cancel when the
  ratio's base differed structurally from the factor's, e.g. `1/2` vs `2`).
- **Files.** `src/sum/sum_geometric.c`; regression tests in `tests/test_sum.c`
  (60 pass). No change to the finite/indefinite results (`Sum[a^i, i]`,
  `Sum[q1^i q2^i, i]`, …).

## Simplify: numeric contagion must not cross List boundaries (2026-07-02)

`{1., 1} // Simplify` returned `{1.0, 1.0}` — the exact `1` was numericalised
merely because it shared a list with the inexact `1.`. More visibly,
`Simplify[{0.982981, r}]` (e.g. the first element being a `Timing[]` result)
numericalised the *whole* symbolic `r`, turning an exact closed form into
`2.0 k - 4.0 k x - …`.

- **Cause.** `Simplify` is not `Listable`, so a list argument was handed whole to
  the `internal_rationalize_then_numericalize` round-trip (rationalise every
  inexact leaf → run the exact pipeline → **numericalise the entire result
  tree**). The final blanket `numericalize` swept exact numbers that never
  touched an inexact input. (`Together`/`Cancel` escape this because they *are*
  `Listable` and thread before the round-trip.)
- **Fix.** When the argument is a `List` and contains an inexact leaf, thread
  `Simplify` over the elements (`simplify_thread_list` in `simp_builtins.c`),
  deciding inexactness independently per element and preserving trailing
  options/assumptions. Nested lists (matrices) are handled by recursive
  re-entry. Genuine within-expression contagion is unchanged
  (`Simplify[(x-1.5)/(x^2-2.25)] == 1/(1.5+x)`).
- **Result.** `{1., 1} // Simplify == {1., 1}`; `{1.5, x, {2, 3.}} // Simplify ==
  {1.5, x, {2, 3.}}`; `Simplify[{0.982981, r}]` keeps `r` exact.

## Cancel / Together / Simplify: FLINT fast path for the internal multivariate GCD (2026-07-02)

`Simplify` of the (exact) Goursat antiderivative
`Log[…Sqrt[R]…]/(k Sqrt[…])` took **~16 s**. Profiling showed the cost was not
FLINT-able number-field work but the generic `Expr` subresultant Euclid:
`simp_algebraic` substitutes `Sqrt[u_i] -> g_i` and then leans on
`Together`/`Cancel`, whose `cancel_recursive` issues a `PolynomialGCD` over
`Q[k, x, g_1, g_2]`. The extension engine (`flint_extension_gcd`) correctly
declines — there is no algebraic *coefficient ring* here, the radicals are
already plain polynomial generators — so the GCD fell back to the interpreted
subresultant PRS with `Expand`-based multiplication (dominant `expr_expand_patt`
churn).

- **Fix.** New `flint_multivariate_gcd_normalized` (a thin normalising wrapper
  over the existing `fmpq_mpoly_gcd` core in `flint_bridge.c`): it rescales
  FLINT's monic GCD to the same **primitive-integer, positive-leading**
  associate the classical path returns (Gauss's lemma —
  `content(gcd) = gcd(content a, content b)`). `cancel_recursive` (`rat.c`)
  now tries it first, falling back to the `PolynomialGCD` builtin when the
  input is not a plain polynomial over `Q` (numeric, non-polynomial). Scoped to
  the *internal* cancellation GCD, so user-facing `PolynomialGCD` output form is
  byte-for-byte unchanged.
- **Result.** `Simplify` of the Goursat result: **~16 s → ~3.7 s** (≈4.3×). The
  antiderivative is unchanged up to the usual additive constant of integration
  (`Log[-1] = iπ`). No new test failures across the poly / rat / simp / apart /
  together / integrate / goursat / zero-test / solve / series / sum / extension
  / cyclotomic / tower suites.

## Integrate`GoursatAlgebraic: Theorem-1 fast path — no needless character split (2026-07-02)

`Integrate[(1 + k x)/((-1 + k x) Sqrt[(1-x) x (1-k² x)]), x, Method -> "GoursatAlgebraic"]`
produced a *correct* but enormous antiderivative (9495 leaves, two `ArcTanh` + two
`Log` with 40 nested radicals). Root cause: `goursat_v4` always ran the **Theorem-2**
Klein-four character decomposition `F = F₁ + F₂ + F₃`, even when the cofactor `F` is
*already* anti-invariant under one of the three root-pairing involutions
(`F + F(S_k) == 0`, Goursat's **Theorem 1**). Splitting an already-anti-invariant `F`
turns a *constant* genus-0 integrand into two *non-constant* ones, so the two piece
reductions bloat and — lacking a full `Simplify` — never recombine.

- **Fix.** Before the character split, test each involution for `F + F(S_k) == 0` (a
  pure rational zero test, so `PossibleZeroQ` is reliable) and, on a hit, run a single
  direct `reduce_v4_piece(F, S_k)`. Falls back to the Theorem-2 split when no single
  involution works or the direct reduction does not close. Verified by the existing
  differentiate-back guard.
- **Result.** The named parametric integral: **9495 → 444 leaves**, a single correct
  term, `PossibleZeroQ[D[result,x] - integrand] == True`. No regressions across the
  goursat / quadrad / jeffrey / chebychev / derivdivides / linrad / linratiorad /
  dispatch integrate suites.
- **Former remaining gap — now closed** (see the next section). The 444-leaf term still
  carried the single radical `Sqrt[R]` *split* as `Sqrt[x] Sqrt[(1-x)(1-k² x)]` across a
  numerator/denominator, which is a spurious branch point and several independent radical
  generators that made a downstream `Simplify` fail to converge. That split is now
  recombined inside the integrator, so `Simplify` completes.

## Integrate`GoursatAlgebraic: recombine split radicals into the single generator Sqrt[R] (2026-07-02)

Follow-up to the Theorem-1 fast path above. The 444-leaf antiderivative of
`Integrate[(1 + k x)/((-1 + k x) Sqrt[(1-x) x (1-k² x)]), x, Method -> "GoursatAlgebraic"]`
was correct but presented the one radical `Sqrt[R]` (R = x(1-x)(1-k² x)) *split* as a
product of factor roots — e.g. a term `(1/k+x)² Sqrt[R] / (Sqrt[x] (-1/k+x)² Sqrt[(1-x)(1-k² x)])`
that is, on the integrand's domain, just the rational `(1/k+x)²/(-1/k+x)²`. Two symptoms,
one cause:

- an apparent `Sqrt[x]` branch point the true antiderivative does not have, and
- three independent radical generators, so a following `Simplify` blew up on the
  multivariate GCD/`Together` cost and never converged (effectively hung).

**Where the split comes from.** The descent's own output carries the radical as a *single*
`Sqrt[c·poly]`. `reexpress_over_radical` re-expresses each nested radical as
`rational·Sqrt[R]` one `Sqrt` node at a time; once the evaluator has distributed a factored
radicand across a term, that per-node rewrite leaves `Sqrt[R]` split across the term's
numerator and denominator. Individually neither `x/R` nor `(1-x)(1-k² x)/R` is a perfect
square, so the per-node pass cannot re-merge them.

**Fix (general, correct-by-construction).** After `reexpress_over_radical`, evaluate to the
flat form where the split is visible and run a new `combine_radicals` pass: at every `Times`
node it merges the *x-dependent* half-power factors `Power[base, m/2]` into one rational
radicand ρ (bases of `+m/2` into the numerator, `−m/2` into the denominator), reduces
`Sqrt[ρ]` over R (→ a rational when ρ is a perfect square, → `rational·Sqrt[R]` when ρ/R is),
reattaches the integer-power leftovers and all non-radical (and *constant*-radical) factors,
and fixes the one remaining global sign numerically. Constant radicals such as
`Sqrt[-1-1/k²+2/k]` are left untouched (they keep the `Log` real). The rewrite is adopted
only if the differentiate-back guard still passes, so it can never introduce a wrong answer.
New helpers: `is_half_power`, `numeric_sign_ratio`, `reduce_sqrt_over_R`,
`combine_times_radicals`, `combine_radicals` in `src/calculus/integrate_goursat.c`.

- **Result.** The named integral now returns with a single x-radical `Sqrt[R]` and no
  spurious `Sqrt[x]` (`FreeQ[result, Power[x, -1/2]] == True`); the differentiate-back
  residual is `< 1e-25`. `Simplify[result]` completes (≈ 16 s, previously non-terminating)
  to the compact single-`Log` closed form
  `Log[(2k - 4kx + 4k²x - 4k³x + 2k³x² - 4k² Sqrt[-(-1+k)²/k²] Sqrt[x(-1+x)(-1+k²x)])/(1-2kx+k²x²)] / (k Sqrt[-(-1+k)²/k²])`.
  Integrate time unchanged (≈ 1 s). No regressions across the goursat and zero-test suites.

## NIntegrate: fast, correct high-precision semi-infinite oscillatory integrals (2026-07-02)

Fixed a wrong-answer bug and a severe performance gap in the arbitrary-precision
(`WorkingPrecision`) branch of `NIntegrate` for semi-infinite oscillatory
(Fourier-type) integrals such as `∫_0^∞ x Sin[x]/(x²+4) dx = π/(2 E²)`.

- **The bug.** At machine precision the semi-infinite path already fell back from
  exp-sinh to an integrate-between-the-zeros + Wynn-epsilon engine when the
  oscillatory tail defeated exp-sinh. The MPFR path had **no such fallback**, so
  `WorkingPrecision -> 200` ran exp-sinh to non-convergence and returned a wrong,
  un-converged value (`0.434…` for the case above) with an `ncvb` warning.
- **Correctness fix.** Added `osc_integrate_mpfr` (`src/numerical_calculus/oscint.c`):
  the MPFR counterpart of the machine oscillatory integrator — cheap machine-precision
  zero-finding for the lobe geometry, each half-period lobe integrated to full precision
  with tanh-sinh, partial sums accelerated with the MPFR Wynn epsilon (degree scaled to
  the requested digits, not the machine-path cap of 6). Wired in as the MPFR AUTO/Levin
  fallback. This alone makes the answer correct, but per-lobe high-precision quadrature is
  slow (hundreds of thousands of samples for 200 digits).
- **Speed fix (the elegant path).** New `src/numerical_calculus/oscde.{c,h}` implements
  the **Ooura–Mori double-exponential rule for Fourier integrals** (Ooura & Mori, *JCAM*
  112, 1999): `x = (M/ω) φ(t)`, `φ(t) = t/(1 − exp(−2t − α(1−e^{−t}) − β(e^t−1)))`,
  `M = π/h`, whose abscissae land on the oscillation's zeros `kπ/ω` (sine) or `(k+½)π/ω`
  (cosine) double exponentially. `NIntegrate`'s Automatic/Levin dispatch now recognises an
  aligned Fourier integrand (`amp·{Sin|Cos}(ω x)` from lower limit 0, via a flattened
  reuse of the Levin kernel detector) and routes to it. **~200 digits in ~0.6 s** vs the
  minutes-and-wrong prior behaviour; agrees with `N[π/(2 E²), 200]` to ~194 digits.
  Gotcha fixed during development: the abscissa scale `M = π/h` must be formed in MPFR —
  a double-rounded `M` makes `x_k = kπ(1+ε)` and floors `sin(x_k)` at ~1e-16 in the tail,
  capping accuracy at 15 digits *and* preventing the node march from terminating.
- Verified: `x Sin[x]/(x²+4)` (→`π/2E²`), `Cos[x]/(x²+1)` (→`π/2E`), `Sin[x]/x` (→`π/2`),
  `x Sin[2x]/(x²+1)` (→`π/2 e^{-2}`) at 30/50/80/100/200 digits; `BesselJ[0,x] Exp[-x/10]`
  (→`1/√1.01`) still correct via the general fallback. New assertions in
  `tests/test_nint.c::test_high_precision`; all `nint_tests` pass; valgrind-clean
  (macOS dyld/libobjc baseline noise only).

## PossibleZeroQ: rigorous rational `False`, ladder early-exit, and a resolved "failure" (2026-07-02)

Another hardening round on `src/zero_test.c` (`PossibleZeroQ`), driven by the two
`Integrate\`GoursatAlgebraic`-verification cases in `POSSIBLE_ZEROQ_FAILURES.md`.

- **Case B1 was not a bug.** `PossibleZeroQ[D[Integrate[f, t, "GoursatAlgebraic"], t] - f]`
  returning `False` on the period-3 cyclotomic case is *correct*: the antiderivative is
  only valid on the positive real branch, and the residual is a genuine non-zero off it
  (`t = -3.4 → 0.20`). B1 shares the exact numeric signature of `Sqrt[x^2] - x` and
  `Log[x^2] - 2 Log[x]` — zero on the positive axis, non-zero on the negative — both of
  which must return `False`. No real/complex/exact-algebraic sampling policy can return
  `True` for B1 without breaking those; only genuine algebraic-function-field zero testing
  could, which is out of scope. The Goursat descent's positive-real-only numeric gate
  (`diff_back_ok`) is the correct domain-aware verifier and is retained. Reclassified in
  `POSSIBLE_ZEROQ_FAILURES.md`.

- **Rigorous `False` for pure rational functions (Zippel).** New
  `is_pure_rational_function` gate: when the whole input is a rational function of its free
  symbols over `Q` (exact rational coefficients, free symbols, `Plus`/`Times`/integer
  `Power` only), Stage-1 normalisation is complete, so a non-zero normalised numerator is a
  **rigorous** `False` — exact DeMillo–Lipton–Schwartz–Zippel, no sampling. `x + y`,
  `(x + 1)/(x - 1)`, `(x + y)^2 - x^2 - y^2` now decide `False` deterministically and
  instantly. Named algebraic constants (`GoldenRatio`) and any transcendental/radical atom
  are deliberately excluded, so identities like `Sin[2 x] - 2 Sin[x] Cos[x]` and
  `GoldenRatio^2 - GoldenRatio - 1` still route to the paths that return `True`.

- **Precision-ladder early-exit (case B2 performance).** Once a numeric residual has *both*
  shrunk geometrically and dropped far below any plausible cancellation floor
  (`ZT_DEEP_ZERO_BITS`), the identity is settled `True` without climbing the costly
  500/1000-bit rungs. Combined with reusing the (precision-independent) operand scale
  across rungs instead of recomputing it, `PossibleZeroQ[D[r, x] - f]` on the 9495-leaf
  parametric Goursat output drops from **4.8 s → 1.9 s** (well inside the descent's 6 s
  budget) with every verdict unchanged.

- **Tests.** `tests/test_zero_test.c` gains groups 12–14: Zippel-rigor cases (with
  determinism-across-hostile-seeds and not-misapplied-to-transcendentals guards),
  precision-ladder early-exit cases (deep radical denestings stay `True`, resolvable tiny
  non-zeros stay `False`), and a large-tree performance guard. Suite valgrind-clean
  (identical to the `Sin[1.0]` baseline).

## Integrate: closed-form quadratic-radical integrator + parametric Goursat fixes (2026-07-01)

**`Integrate\`QuadraticRadicals` now emits ArcTan / ArcTanh / ArcSinh / ArcSin /
Log directly** instead of the Log-of-radical forms the Euler substitution
produced. `src/calculus/integrate_quadrad.c` gained a closed-form path (`qr_direct`,
tried before Euler, falling back to it on anything it cannot fully decompose): it
splits the integrand as `E(x) + H(x)/Sqrt[rad]`, integrates the rational part `E`,
partial-fractions `H`, and assembles the standard building blocks
`I_n = ∫x^n/Sqrt[rad]` (order reduction to `I0`) and `J(p) = ∫1/((x−p)Sqrt[rad])`
(→ `ArcTanh` if `rad(p)>0`, `ArcTan` if `rad(p)<0`). Each block is an exact
identity, so the assembled answer needs no verification. `I0` picks `ArcSinh`/`ArcSin`
only when `Sqrt[|disc|]` is an exact rational (so the derivative stays
Simplify-reducible), else a clean-cancelling `Log` form; a symbolic-leading-coefficient
pure-polynomial integrand is declined to the Euler path. E.g.
`∫1/Sqrt[x^2+1] = ArcSinh[x]`, `∫1/(x Sqrt[x^2+x+1]) = -ArcTanh[(2+x)/(2 Sqrt[1+x+x^2])]`.

**Parametric square-root Goursat (`R = F(x) (a x^2+b x+c param)^(...)`) no longer
hangs.** In `src/calculus/integrate_goursat.c`: (1) `monomialize_root_of_unity`
now requires `NumberQ[N[base]]` before denesting, so a symbolic parameter (the `k`
in `R = x(1-x)(1-k^2 x)`) no longer triggers the numeric root-of-unity heuristic
and injects spurious `Arg[k^2]`/`Abs[k^2]` terms; (2) the V4 fixed points are
`PowerExpand`-ed so `Sqrt[k^2]/k^2 → 1/k`, keeping the descent over `Q(k)` with a
single radical instead of carrying `Sqrt[k^2]` as a phantom second generator that
caused the genus-0 integrand to recurse. With these and the cleaner quadratic-radical
output, `Integrate[(1+k x)/((-1+k x) Sqrt[(1-x) x (1-k^2 x)]), x, Method ->
"GoursatAlgebraic"]` returns a verified antiderivative (was: non-terminating).

Verification note: the descent's differentiate-back gate should be
`PossibleZeroQ[D[result,x]-f]` (never `Simplify`), but `PossibleZeroQ` currently
misfires on the period-3 cases' cyclotomic-tower nested radicals and is too slow on
the large parametric outputs; the gate therefore uses a numeric differentiate-back
as an interim measure. Both `PossibleZeroQ` failure modes are written up in
`POSSIBLE_ZEROQ_FAILURES.md` (cases B1/B2). Tests: `integrate_quadrad_tests`,
`integrate_goursat_tests` (all green); no regressions across the integrate/rat/simp/
trigrat suites.

## Perf: FLINT-backed squarefree test in algebraic Factor (Trager) (2026-07-01)

`Factor[p, Extension -> α]` (Trager's algorithm, `src/poly/qafactor.c`) must find
the smallest linear shift `s` for which the norm `Norm(f(x − sα)) ∈ Q[x]` is
squarefree, testing squarefreeness by computing `gcd(R, R′)`. That GCD ran
through the classical content/pseudo-remainder `poly_gcd_internal`, which
sampling showed to be **~88 %** of total runtime on higher norm degrees (norm
degree = `deg f × deg minpoly`). `expr_qx_is_squarefree` now computes that GCD
via FLINT (`flint_multivariate_gcd` → `fmpq_mpoly_gcd`; the univariate rational
norm is squarely in FLINT's wheelhouse), falling back to the classical path
unchanged when FLINT returns NULL (numeric/non-polynomial input, or `USE_FLINT`
off). Only the GCD degree is consulted, so FLINT's monic normalisation is
irrelevant and factor outputs are byte-identical. Measured speedups:
`Factor[x^16 - 2, Extension -> 2^(1/4)]` 1.31 s → 0.076 s (**17×**),
`x^12 - 15 x^8 + 15 x^4 - 1` over Q(√2) 0.32 s → 0.011 s (**30×**),
`(x^2-2)(x^2-3)(x^2-5)(x^2-7)` over Q(√2) 0.20 s → 0.006 s (**32×**). After the
fix the profile is dominated by the genuine algebraic work — norm factorisation
(already FLINT `fmpz_poly_factor`) and the Trager Q(α) GCD lifts. The norm
*factorisation* was never the bottleneck; the squarefree-shift search was.

## Feature: transparent multivariate Factor via FLINT (2026-07-01)

`Factor[p]` on a genuine multivariate polynomial over Q now routes through
FLINT's `fmpq_mpoly_factor` (public `flint_polynomial_factor`, guarded fast path
at the top of `builtin_factor`, right after the univariate path and gated to the
plain single-argument form). The classical heuristic multivariate factoriser is
exponential in the variable count and effectively hangs on inputs such as
`Factor[x^99 - y^99]` (> 20 s); the FLINT path returns it in a few
milliseconds. Inputs that are *not* a multivariate polynomial over Q — a
denominator (rational function), a fractional or symbolic exponent, or a
non-polynomial head (`Sqrt`, `Sin`, `Complex`, …) — make the bridge return NULL
and fall through to the classical path unchanged, so rational-function `Factor`,
algebraic-field factoring (`Extension`/`GaussianIntegers`, which travel a
separate options branch), and generator-style factoring (`Factor[Sin[x]^2-y^2]`)
are untouched.

Output convention: each irreducible factor is normalised to a **positive leading
coefficient** (highest total degree, deglex tie-break — Mathematica's
convention), with the discarded sign folded into a separated rational content
(the factoring context now uses `ORD_DEGLEX` so FLINT's term 0 is that leading
term). This makes the result *more* canonical than the classical output, which
spread factor signs inconsistently: `Factor[y^2 - x^2]` → `-(x - y)(x + y)`
(matching Mathematica) instead of `(x + y)(-x + y)`, and `Factor[u^2 - k]` stays
`u^2 - k` rather than the ugly `-(k - u^2)` a naive lex normalisation gives. The
factored product is identical; the evaluator re-sorts the `Orderless` `Times`, so
factor ordering is independent of FLINT. Two pinned classical-convention tests
(`test_facpoly.c`) were rebaselined to the verified-equivalent normalised forms.
Tests: `test_flint_bridge.c::test_flint_poly_ops` (multivariate cases +
rational-function / non-polynomial fall-through). `USE_FLINT=0` builds and the
classical path handles multivariate factoring as before.

## Feature: FLINT` exact linear algebra — Inverse / LinearSolve / RowReduce / MatrixRank (2026-07-01)

Extended the FLINT exact-matrix bridge (`src/linalg/flint_mat_bridge.{c,h}`)
beyond `Det` with four more `fmpq_mat` kernels, each wired both transparently
into the `System`` builtin and exposed directly in the `FLINT`` context
(`Protected`, docstringed, `SYM_FLINT_*` in `sym_names.c`):

- `` FLINT`Inverse[m] `` — exact inverse via `fmpq_mat_inv`.
- `` FLINT`RowReduce[m] `` — reduced row echelon form via `fmpq_mat_rref`.
- `` FLINT`LinearSolve[m, b] `` — square nonsingular solve via `fmpq_mat_solve`
  (vector or matrix RHS).
- `` FLINT`MatrixRank[m] `` — rank via `fmpq_mat_rref`.

Transparent wiring: an all-integer/rational matrix now takes the FLINT path in
`Inverse` (division-free/Automatic method), `RowReduce`, `LinearSolve`
(square nonsingular unique-solution case), and `MatrixRank` (exact path).
Because the inverse, RREF, unique solution, and rank are each *unique* /
basis-independent, the FLINT result is identical to the classical
division-free output — but computed in polynomial time. A singular, non-square,
or symbolic input returns NULL from the bridge and falls through to the
classical path unchanged (which handles rectangular / underdetermined /
inconsistent systems and emits the usual messages). `NullSpace` inherits the
acceleration through its internal `RowReduce`. No separate `` FLINT`NullSpace ``
(a null-space basis is not canonical, so it would diverge from the classical
convention). Tests: `test_flint_bridge.c::test_flint_mat_ops`; valgrind
baseline-identical; `USE_FLINT=0` builds and the `FLINT`` calls stay symbolic.

## Feature: transparent univariate Factor via FLINT + bignum-coefficient fix (2026-07-01)

`Factor[p]` on a univariate polynomial over Z now routes through FLINT's
`fmpz_poly_factor` (new internal `flint_univariate_factor_auto`, guarded fast
path at the top of `builtin_factor`). It self-detects the single variable with
FLINT's bignum-safe variable scan and emits Mathematica's convention —
primitive factors with positive leading coefficient plus a signed integer
content (so `Factor[4 x^2 - 9]` → `(2x-3)(2x+3)`, *not* the monic
`4(x-3/2)(x+3/2)` an `fmpq` factorisation would give). Multivariate,
rational/symbolic-coefficient, and rational-function inputs return NULL and take
the classical path unchanged.

This also **fixes a correctness bug**: a univariate polynomial with a bignum
(> 64-bit) coefficient previously came back *unfactored*. Root cause was not the
classical factoriser but `collect_variables` — via `is_number`, which omits
`EXPR_BIGINT` — misclassifying a bignum coefficient as a *variable*, routing the
polynomial into the multivariate path which dropped its constant term. Running
the FLINT fast path before that machinery sidesteps it. (Transparent
multivariate FLINT factoring landed separately — see the entry below.) Tests:
`test_flint_bridge.c::test_flint_poly_ops`.

## Feature: FLINT` context — direct REPL access to FLINT kernels (2026-07-01)

New `FLINT`` context exposing the FLINT-backed polynomial kernels directly at
the REPL, each with a docstring and an `ATTR_PROTECTED` symbol interned in
`sym_names.c` (`SYM_FLINT_*`). Registered in `flint_bridge_init`; all return
their argument unevaluated when the input is out of FLINT's scope or the build
is `USE_FLINT=0`, so they degrade cleanly.

- `` FLINT`PolynomialGCD[a, b] `` — monic GCD over Q via `fmpq_mpoly_gcd`
  (multivariate). Replaces the old M1 scaffold `` Flint`GCD `` (renamed).
- `` FLINT`Resultant[a, b, x] `` — resultant eliminating `x` via
  `fmpq_mpoly_resultant`; other symbols are coefficients.
- `` FLINT`Factor[p] `` — irreducible factorisation over Q via
  `fmpq_mpoly_factor`, as `Times[const, factor^exp, …]` (multivariate).
- `` FLINT`FactorSquareFree[p] `` — squarefree factorisation via
  `fmpq_mpoly_factor_squarefree`.

New public bridge entry points back these (`src/poly/flint_bridge.{c,h}`):
`flint_polynomial_resultant`, `flint_polynomial_factor`,
`flint_polynomial_factor_squarefree` (plain `Q[x_1..x_n]`, the rational
analogues of the existing extension helpers).

**Transparent acceleration:** `Resultant[a, b, x]` now routes plain rational
inputs through `flint_polynomial_resultant` (falling back to the classical
subresultant PRS when a coefficient is non-rational). FLINT's convention was
verified to match the classical output *exactly* across asymmetric-degree,
non-monic and sign-sensitive cases, so this is a zero-regression speedup on
multivariate / high-degree resultants. (Transparent `PolynomialGCD` and
`Factor` rewires are deferred: their FLINT output uses monic / sign
conventions that differ from Mathematica's primitive-over-Z form and would
change system-wide output without a normalisation pass — the `FLINT`` context
gives direct access in the meantime. See `FLINT_ACCELERATION_SWEEP.md`.)

Tests: `tests/test_flint_bridge.c::test_flint_poly_ops`. Valgrind-clean
(baseline-identical); `USE_FLINT=0` fallback builds and the `FLINT`` calls stay
symbolic.

## Feature: FLINT` exact determinant — Det acceleration + FLINT`Det (2026-07-01)

New `src/linalg/flint_mat_bridge.{c,h}`: an `Expr` matrix ↔ `fmpq_mat` boundary
(mirroring `flint_bridge.c`'s scalar conversion). `Det[m]` now computes an
all-integer / all-rational matrix exactly via `fmpq_mat_det` in polynomial
time — replacing the `O(n!)` Laplace expansion (`laplace_det`) on the common
numeric case (a 12×12 Hilbert determinant: instant, exact). Symbolic matrices
fall through to Laplace unchanged; the FLINT value matches it exactly.

Exposed directly as `` FLINT`Det[m] `` (`Protected`, docstringed, `SYM_FLINT_Det`
in `sym_names.c`), returning unevaluated for a non-square or non-rational
matrix. Registered via `flint_mat_bridge_init` in `core_init`; no-op stub
without FLINT. Tests: `test_flint_bridge.c::test_flint_mat_ops`;
`linalg`/`hilbertmatrix`/`eigen` suites green; valgrind baseline-identical.

## Feature: FLINT` numeric special functions (arb/acb) (2026-07-01)

New `src/flint_num_bridge.{c,h}`: an `Expr` scalar ↔ FLINT `acb` (arbitrary-
precision complex ball) boundary. Rendering an `acb` back to an `Expr` reuses
`numeric_mpfr_make_complex` (real leaf when the imaginary part rounds to zero,
else `Complex[…]`); working precision is taken from the arguments
(`numeric_min_inexact_bits`, 53-bit floor) plus guard bits. Gated on
`USE_FLINT && USE_MPFR` with stubs otherwise.

Four `FLINT`` context builtins (all `Protected`, docstringed, `SYM_FLINT_*` in
`sym_names.c`), each rigorous over the whole complex plane and returning
unevaluated for symbolic arguments / poles:

- `` FLINT`Zeta[s] `` — Riemann ζ (`acb_dirichlet_zeta`).
- `` FLINT`HurwitzZeta[s, a] `` — Hurwitz ζ (`acb_dirichlet_hurwitz`).
- `` FLINT`PolyGamma[n, z] `` — ψ⁽ⁿ⁾ (`acb_polygamma`; `n = 0` is digamma).
- `` FLINT`StieltjesGamma[n] `` / `` [n, a] `` — Stieltjes constants
  (`acb_dirichlet_stieltjes`), a **new numeric capability** (the existing
  `StieltjesGamma` was symbolic-only apart from `γ₀ = EulerGamma`).

Validated to closed forms (`ζ(2)=π²/6`, `ψ(1)=−γ`, `γ₀=EulerGamma`, ≈0 at the
first Riemann zero `0.5 + 14.1347i`) and precision propagation
(`` FLINT`Zeta[2.0`40] `` → 40 digits). Registered via `flint_num_bridge_init`
in `core_init`. The existing hand-rolled MPFR kernels are unchanged (transparent
migration deferred — those carry hand-tuned branch-cut/precision conventions).
Tests: `test_flint_bridge.c::test_flint_num_ops`; valgrind baseline-identical
(FLINT arb constant-cache noise is pre-existing, present on any FLINT path).

## Feature: Integrate`GoursatDebug trace switch (2026-06-30)

Added the user-settable global `Integrate`GoursatDebug` (default `False`,
registered in `integrate_goursat_init`).  When set `True`, the
`Integrate`GoursatAlgebraic` descent narrates its execution to stderr:

- **Form recognition** — whether the integrand matches `F(x) R(x)^(-p)` with
  `p in {1/2, 1/3, 2/3, 1/4, 3/4}`, and the recognised cofactor `F`, radicand
  `R`, and exponent `p` (or a clear "no match" decline).
- **Branch chosen** — square-root `V4`, cube-root order-3, or fourth-root
  order-4 eigendescent (and the `V4 → period-3` fallback).
- **Criterion tests** — which involution / eigenspace equation must hold and
  whether it does: the `V4` trivial-character projection, the order-3/order-4
  ω-eigencomponents (numeric gate then exact), the harmonic-roots test for
  quartics, and the period-3 trivial projection per candidate fixed point.
- **Verification** — the differentiate-back guard verdict and the final
  antiderivative (or decline).

The flag is read once at the outermost entry (`gs_set_debug`, called from
`gs_guarded` at `gs_depth == 0`) into a module static, so the recursive genus-0
reductions — which run inside the `TimeConstrained` sub-evaluation — share the
setting without re-reading the symbol per log site, and trace lines indent by
recursion depth.  Off by default every log site is a single `int` test, so there
is no cost when tracing is disabled.  Mirrors the existing `Integrate`$Verbose`
switch in `intrat.c`.

## Docs: clarified that GoursatAlgebraic matches numerator radicals (2026-06-30)

No behaviour change — `recognise` already accepts a radical `R(x)^(n/d)` with
*any* rational exponent of reduced denominator `d in {2,3,4}` (added 2026-06-29),
folding `R^(n/d) = R^k R^(-p)` so the radical may sit in the **numerator** as
readily as the denominator: e.g. `x^2 (1-x^3)^(1/3)` integrates to
`-(1/4)(1-x^3)^(4/3)`, `(1-x^4)^(3/4)/x` and the cube-root family all match.  The
`Integrate`GoursatAlgebraic` docstring and the `GoursatDebug` `FORM:` trace line
previously described only the denominator form `F(x)/R(x)^p`; both now state that
the radical may start in either position and is reduced to the canonical
`F(x) R(x)^(-p)`.  (Cases like `Sqrt[1-x^3]/x` still *decline* under the explicit
method — `F=(1-x^3)/x` has a pole at the non-branch point `x=0`, so the V4
criterion genuinely fails; the default cascade integrates them via Chebyshev.)

## Fix: PossibleZeroQ Schwartz–Zippel false-zeros on algebraic-coefficient rationals (2026-06-30)

`PossibleZeroQ` (`src/zero_test.c`) wrongly returned `True` for several genuine
non-zeros that mix a free variable with an algebraic constant, e.g.
`-Sqrt[2] + (1 + u^3) Sqrt[2]` (= `Sqrt[2] u^3`), `-(-1)^(1/3) + (1 + u^3) (-1)^(1/3)`,
and `(u (-1)^(1/3))/(-1 + u^3)`.  The latter's verdict was even *flaky* — it
depended on the variable name (hence on the structural-hash sample seed).

Two independent root causes in the Stage-3 sampler, both fixed:

- **Sample points clustered near 0.** `sample_random_value` drew `n/d` with
  `|n| ≤ 64` but `d` up to `2^16`, so most samples had `|value| ≪ 1`.  A
  high-degree monomial (`u^3`) then collapses far below the operand scale and is
  misread as an identity.  Samples now have magnitude bounded **below by 1** (an
  integer part in `[1, 64]` plus a full-granularity fractional part, random
  sign); by the identity theorem an analytic identity true on `[1, 64]` is true
  everywhere, so no decision power is lost.
- **`magnitude_scale` mis-scored reciprocals.** The cancellation-threshold
  scale used `pow(base, |exponent|)`, so `Power[denom, -1]` was scored as
  `denom` instead of `1/denom`, hugely inflating the operand scale of any
  expression with a denominator and pushing genuine non-zeros below the noise
  floor.  Now uses the **signed** exponent value (new `expr_signed_double`).

A third, related false-zero was found and fixed in the same pass: `decide_numeric`'s
"residual never shrank across the precision ladder → assume zero (lenient)"
fallback misfired on **algebraic** expressions whose operand scale is inflated by
internal cancellation, e.g. `Sqrt[10^12+1] - 10^6` (≈ `5e-7`, scored as noise).
For a purely-algebraic expression MPFR honours precision exactly, so a residual
that stays above the machine-noise floor (`scale·2^-48`) while *not* shrinking is
a genuine non-zero, not a cancellation-hidden zero. New `is_algebraic_expr`
predicate gates this: non-shrinking algebraic residuals above the floor settle
`False`; transcendental heads (whose MPFR path may stay pinned to machine
precision) keep the lenient zero verdict. A true algebraic zero shrinks under MPFR
and never reaches this branch.

No regressions: `zero_test_tests` and the PossibleZeroQ-heavy suites
(simplify/solve/series/limit/intrat/rat/radical/trigrat/sum_rational/trig) show
identical pass/fail to baseline (the pre-existing `solve_tests` hidden-zero
fail is unchanged).

**Knock-on effect on Integrate`GoursatAlgebraic`:** the cube-root descent gates
each eigenpiece with `PossibleZeroQ` (`integ_backsub`, criterion checks), so the
false-zeros silently collapsed correct antiderivatives to `0` (then rejected by
the differentiate-back guard → decline).  With the fix the method now solves its
elementary cube-/fourth-root cases across **all four exponents** directly, e.g.
`Integrate[(1-x^3)^(1/3)/x, x, Method -> "GoursatAlgebraic"]`,
`x/(1-x^3)^(2/3)`, `t^2/(t^3-1)^(1/3)`, `1/(t^4-1)^(1/4)`, `t^2/(t^4-1)^(3/4)`.
(Previously these were only reachable via Chebyshev in the default cascade.)

## Progress: V4 cyclotomic-tower descent — denest fixed points + select clean involutions (2026-06-30)

Groundwork for the Legendre–Clausen integral `t/((t³+8) Sqrt[t³-1])` (and the
broader cyclotomic-root V4 class). `Solve[S == t]` returns each V4 involution's
fixed points via the quadratic formula, so they carry an opaque nested radical
`Sqrt[disc]` whose discriminant is a cyclotomic constant (e.g. `Sqrt[12 (-1)^(2/3)]`).
Left opaque, that radical reads as an independent variable: `R(t(u))·(1-u)⁴`
never reduces to a polynomial, the descended `lc`/`Q`/`gx` keep spurious
`(1-u)` denominators and `Sqrt[cyclotomic]` coefficients, and `integrate_in`
re-recognises a cubic-radical Goursat problem and recurses until the time guard
fires (the old ~12 s hang).

Two changes in `src/calculus/integrate_goursat.c`:

- **Denest cyclotomic fixed points** (`denest_const_radicals`): the discriminant
  is, by construction, a rational multiple of a root of unity `c (-1)^(p/q)`,
  whose square root IS a tower element (`Sqrt[12 (-1)^(2/3)] = 2 Sqrt[3] (-1)^(1/3)`).
  Detect the monomial numerically (`c = Rationalize[Abs[N[disc]]]`,
  `q = Rationalize[Arg[N[disc]]/Pi]`), verify it equals the discriminant exactly,
  then `PowerExpand` the monomial. The fixed points become explicit
  cyclotomic-tower elements and the descent stays rational over that field.
- **Select the cleanest involution** (`involution_score`): each projection is
  anti-invariant under two involutions; pick the one whose denested fixed points
  are real (smallest tower) rather than the first with a single finite fixed
  point. For `t³-1` this picks the real `1 ± Sqrt[3]` pairing, and that piece's
  genus-0 reduction now closes.

Net: the descent no longer grows an unbounded radical tower, and the lead
projection closes. The named integral still declines (~8 s, time-budget), now
for a *different* and newly-isolated reason: its remaining genus-0 pieces are
`∫ (rational over Q(ζ₁₂)) / Sqrt[quadratic over Q(√3)]`, which Mathilda's
elementary integrator does not yet close over a number field — the next target,
separate from Goursat. All existing `integrate_goursat` / `integrate_jeffrey`
tests pass; behaviour on rational-coefficient V4 integrands is unchanged.

## Fix: `Integrate` `GoursatAlgebraic` returns the antiderivative over the integrand's own radical (2026-06-30)

`Integrate[(x-1)/((x+2) Sqrt[x^3-1]), x]` (and other square-root pseudo-elliptic
integrands routed through the V4 / period-3 descent) produced a **correct but
unverifiable** antiderivative: the closed form carried a *nested* radical
`Sqrt[g]` in the Möbius-substituted coordinate (e.g. `Sqrt[w² − w]`,
`w = ((x-1)/(x+2))³`) instead of the integrand's own radical `Sqrt[x³−1]`. Since
the answer and the integrand then contained *different* radicals,
`D[result, x] // Simplify` could not reduce to the integrand (it would have to
relate two distinct nested roots), so the result failed a routine
differentiate-back verification even though it was correct.

By the Goursat construction `g` is always `R` times a perfect rational square,
so `Sqrt[g] = (rational)·Sqrt[R]`. A new output-normalisation pass in
`src/calculus/integrate_goursat.c` re-expresses every such nested square root
over the integrand's own `Sqrt[R]`:

- `reexpress_over_radical` collects the radicands `g` of `Sqrt[g]` in the result,
  and for each computes `h = Factor[Cancel[Together[g/R]]]`. Factoring exposes the
  square part; `PowerExpand[Sqrt[h]]` yields a *radical-free* rational `s` exactly
  when `h` is a perfect rational square (the gate: `s` contains no `Power[_,1/2]`
  and `s² = h`). The sole branch freedom — a global `±` sign — is fixed
  numerically by sampling `s·Sqrt[R] / Sqrt[g]`.
- The rewrite is **correct-by-construction** (the integrator knows the
  perfect-square relationship) and is adopted only if the existing
  differentiate-back guard still passes on the rewritten form. It is confined to
  the integrator — `Simplify` is unchanged (pulling a polynomial square factor out
  of a radical is branch-cut-sensitive in general, so `Simplify` rightly refuses
  it without assumptions).

Result: `Integrate[(x-1)/((x+2) Sqrt[x^3-1]), x]` now returns a `Log` in the
single radical `Sqrt[x³−1]` and `D[%, x] // Simplify` reduces to the integrand
(`= 0`). The V4 sqrt cases whose radical was already `Sqrt[R]` are unchanged
(the pass skips `g == R`). Regression: `test_period3` in
`tests/test_integrate_goursat.c` now also asserts the symbolic `Simplify`
verification, not only a numeric differentiate-back.

## Fix: cyclotomic `Together`/`Cancel` lift into the `Q(α)` substrate (2026-06-30)

SIMPLIFY_IMPROVEMENT_PLAN.md Phase 3 (reduce-into-substrate). `Together`/`Cancel`
with `Extension -> Automatic` over a cyclotomic field collapsed to the slow
**multivariate** `Q[ζ, ζ², t]` subresultant path whenever a single summand mixed
two surface forms of the same root of unity — e.g. `(-1)^(1/3)` (= ζ₆) and
`(-1)^(2/3)` (= ζ₆²). The 4-term V4 projection
`F + F(S₁) − F(S₂) − F(S₃)` of `F = t/(t³+8)` over the involutions of `t³−1`
(the Legendre–Clausen integrand, `SIMPLIFY_ALGEBRA_SLOW.md` §6) took **>25 s**;
it now finishes in **~0.06 s**, verified equal to the input by `PossibleZeroQ`
and a sampled diff. Two root causes in the substrate, both fixed:

- **`qa_expr_to_qaupoly_with_alpha` (`src/poly/qafactor.c`) refused to lift any
  expression whose coefficient carried `α^k` with `k ≥ deg(P_α)`.** For radical
  generators the evaluator pre-reduces (`Sqrt[2]^3 → 2 Sqrt[2]`), but the opaque
  internal α-symbol carries no such auto-rule, and cyclotomic inputs surface high
  powers after Stage-2 `Expand` of products/powers of the rewritten root-of-unity
  atoms (e.g. `((-1)^(2/3)+…)^3` expands to `α², α³, …`). The refusal made the
  lift return `NULL`, dropping `PolynomialGCD`/`PolynomialQuotient` to the
  multivariate path that treats each conjugate as an independent variable. Fix:
  fold every `α^k` mod `P_α` via the existing `qa_alpha_power` (field
  multiplication already reduces), since the internal symbol *always* denotes the
  generator. A genuinely out-of-field atom is not an α-power — it stays a
  `Power[base, p/q]` node and is still rejected downstream by `expr_coef_to_mpq`.

- **`qaupoly_gcd` (`src/poly/qaupoly.c`) used a bare Euclidean PRS.** `qaupoly_divrem`
  multiplies the dividend through by `1/lc(divisor)`, so a non-monic divisor
  inflates every remainder coefficient by the rational height of that leading
  field element, compounding step over step (degree-12 cyclotomic inputs produced
  50+ digit numerators). Now each remainder is normalised to monic before it
  becomes the next divisor — GCD-preserving (the final `make_monic` already
  canonicalises), keeping intermediate heights bounded.

All substrate (`qa`, `qaupoly`, `qafactor`, `poly`, `extension_*`, `facpoly`,
`radical_polyops`) and consumer (`rat`, `simp`, `simplify`, `zero_test`,
`minimalpolynomial`, `factor_baseline`, `radical_simplify`, `integrate_goursat`,
`intrat`) tests pass; a new regression in `test_extension_auto_builtins.c`
(`test_cyclotomic_mixed_generator_lift`) pins the mixed-generator case.

## Fix: `Integrate`GoursatAlgebraic` period-3 reductions with cyclotomic fixed points (2026-06-30)

SIMPLIFY_IMPROVEMENT_PLAN.md Phase 5 (partial). The Section-4 period-3 reducer
(`period3_reduce` in `src/calculus/integrate_goursat.c`) previously hung on any
single character whose order-3 Möbius fixes a **cyclotomic** root of `R = t^3-1`
(e.g. `Integrate[(t-(-1)^(2/3))/((t+2(-1)^(2/3)) Sqrt[t^3-1]), t]`). Two root
causes, both fixed:

- **Fixed points came from `Solve[S==t]` + `Simplify`**, which for the cyclotomic
  roots returns *nested* radicals (`Sqrt[-9(-1)^(1/3)]`) that live in
  `Q(ζ₃, √ζ₃)`, not the pure cyclotomic field. `canonic` then leaves
  roots-of-unity identities uncollapsed, so `to_function_of_power`'s
  multiple-of-3 denominator vanishes → `ComplexInfinity`. Fix: for the centered
  pure cubic `R = lc((t-s)^3 + c)` the map fixing a finite root has the **exact**
  fixed points `{r, 3s-2r}` (Goursat's `((t-r)/(t+2r))^3` substitutions); supply
  them directly so the whole reduction stays in `Q(ζ₃)`, where `canonic` is fast
  and complete.

- **The descended genus-0 integrand `(1/3)φ(x)/Sqrt[x(Ax+B)]` has an algebraic
  leading coefficient `A`**, which Mathilda's `Sqrt`-of-quadratic integrator
  cannot handle (it manages an algebraic constant on the linear/constant term but
  not on the leading term). Fix: rescale `x → w/lc` so the radicand becomes the
  monic `w^2 + Bw` (`B` kept on the linear term — *not* divided by `lc`, which
  would need the cyclotomic constant-ratio `B/lc` that `Cancel`/`Simplify` cannot
  reduce, e.g. `(-1+ζ)/(1-ζ) → -1`), with the constant `1/(3 Sqrt[lc])` pulled
  *out* of the integral (a nested-radical constant left inside is mistaken for a
  second variable radical and hangs the integrator).

Single cyclotomic characters now integrate correctly (real-axis
differentiate-back ~1e-16) and fast (~0.2s). The full Legendre–Clausen integral
`t/((t^3+8) Sqrt[t^3-1])` — a *sum* of three such characters — is still declined
(it reaches `goursat_v4`, whose cyclotomic V4-projection `canonic` is a separate
blow-up); the clean V4 partition is the remaining Phase-5 work.

## Perf: `PossibleZeroQ` + `Simplify` fast over algebraic-number constants (2026-06-30)

Follow-on to the cyclotomic `Together`/`Cancel` work below (SIMPLIFY_IMPROVEMENT_PLAN.md Phase 2).

- **`PossibleZeroQ`** (`src/zero_test.c`): when the expression mixes free
  symbols with a radical / root of unity (`Sqrt[...]` or `Power[base, p/q]`,
  `q != 1`), `zero_test_decide` now skips the Stage-1 `Together ∘ Cancel`
  (which runs *without* an Extension option and blows up super-polynomially over
  Q(α)) and goes straight to numeric Schwartz–Zippel sampling. The 4-term
  cyclotomic V4 projection of `t/(t^3+8)` — previously ~20s+ — is now decided in
  **~40ms**. Correctness is unchanged: Stage 1's only trustworthy verdict is
  `True`, which the sampler reproduces for genuine identities (and `False` for
  genuine non-zeros). New predicate `expr_has_algebraic_constant`; purely
  rational `PossibleZeroQ` is unaffected (guard does not fire).

- **`Simplify`** (`src/simp/simp_builtins.c`): the algebraic fast-path now uses
  `Together[Extension -> Automatic]` whenever `extension_autodetect` returns
  *any* tower, including a single generator (`n == 1`). Previously single-gen was
  routed to plain `Together`, which is fine for a `Sqrt` (auto-reduced) but blew
  up for a single cyclotomic generator. `Simplify[1/(x-(-1)^(1/3)) +
  1/(x+(-1)^(1/3))]` now returns `(2x)/(1-(-1)^(1/3)+x^2)` (reduced mod Φ_6).
  The leaf-count gate is unchanged, so radical cases do not regress. (Large
  cyclotomic *sums* whose combined numerator is not smaller by leaf-count still
  fall to the general pipeline and remain slow — a separate Simplify-pipeline
  item.)

## Perf: fast `Together` / `Cancel` over cyclotomic extensions Q(ζ_n) (2026-06-30)

`Together` / `Cancel` with `Extension -> Automatic` (and explicit
`Extension -> Exp[2πI/n]` / `(-1)^(p/q)` / `I`) over a field generated by a root
of unity were previously **non-terminating in practice** — the constants were
never recognised as extension generators, so they fell through to the
rational-over-Q path which treats each distinct algebraic atom as an independent
transcendental and forms a giant common denominator. The 4-term cyclotomic V4
projection of `t/(t^3+8)` did not finish in minutes; it now combines in
**~0.27s**, and `Together[1/(x-a)+1/(x+a), Extension -> a]` for
`a = (-1)^(1/3)` is **~3ms** (and reduces `a^2 = (-1)^(2/3)` to `(-1)^(1/3)-1`
modulo Φ_6).

**Cause / fix.** The Q(α)[x] arithmetic substrate (`qa.c`/`qaupoly.c`/
`qafactor.c`) was already fast; cyclotomic constants simply never reached it.
New `src/poly/rootofunity.{c,h}`:
- `qaext_cyclotomic(n)` — builds Q(ζ_n) with minimal polynomial Φ_n(y) (the
  cyclotomic polynomial, degree φ(n)) via the exact divisor recursion
  `Φ_n = (y^n − 1)/∏_{d|n,d<n} Φ_d` over Z.
- `expr_is_root_of_unity_pow(e)` — recognises Mathilda's canonical
  root-of-unity surface forms `Power[-1, Rational[p,q]]` and `Complex[0, ±1]`.

Wired into `qafactor.c`: `qa_resolve_extension` resolves any `(-1)^(p/q)` to the
cyclotomic field of the natural generator `ζ_{2q} = (-1)^(1/q)`; a cyclotomic
analogue of `expand_radicals_to_atomic_poly` rewrites each root-of-unity atom in
the input to a reduced power of ζ modulo Φ (so identities like `ζ^{2q}=1` and
`1+ζ+ζ^2=0` are structural, not rediscovered); and `extension_autodetect` now
collects base `-1` generators, merging by the LCM of the exponent denominators.
Because elements live in a fixed Q-basis bounded by φ(n), intermediate size can
no longer explode. (SIMPLIFY_IMPROVEMENT_PLAN.md Phase 1.) `PossibleZeroQ` over
the same fields is **not** yet fast — its Stage-1 `decide_rational` does more
than `Together` and remains a separate work item (Phase 2).

## Feature: `Integrate`GoursatAlgebraic` period-3 reduction for √(cubic) (Goursat 1887 §4) (2026-06-29)

`Integrate[(x-1)/((x+2) Sqrt[x^3-1]), x]` (and other `F(t)/Sqrt[t^3-1]` with the
`t^3-1` higher symmetry) now integrates — to
`-(2/3) ArcTan[Sqrt[((x-1)^3(1-((x-1)/(x+2))^3))/(x+2)^3]/(((x-1)/(x+2))^3-1)]`,
equal to the closed form `-(2/3) ArcTan[3 Sqrt[x^3-1]/(x-1)^2]` up to a constant
— in ~0.1s, under both `Method -> "GoursatAlgebraic"` and the default cascade.

This is the case Goursat's **Section 4** covers and the V4 Theorems 1–2
(`goursat_v4`) do **not**: there is an order-3 Möbius `S` fixing one of the four
ramification points `{roots(R), Infinity}` and cycling the other three, and `F`
is a non-trivial period-3 character `F(S) = α F` (`α = Exp[2πI/3]`). The V4
trivial projection is non-zero here, but the period-3 trivial projection
`F + F(S) + F(S^2)` vanishes.

**Reduction** (`goursat_period3` / `period3_reduce` in
`src/calculus/integrate_goursat.c`): with `z = (t-a)/(t-a1)` for the two fixed
points `a, a1` of `S`, the map becomes `z -> α z`, the differential
`dt/Sqrt[R]` becomes `dz/Sqrt[z(A z^3 + B)]`, and the α-character `F(t(z))`
becomes `z·φ(z^3)`; setting `x = z^3` yields the elementary
`(1/3) φ(x)/Sqrt[x(A x + B)]`, which the cascade integrates and the result is
back-substituted `x -> ((t-a)/(t-a1))^3`. Reuses the existing `cyclic_mobius`,
`fixed_points`, and `to_function_of_power`; the two fixed-point orientations are
tried to cover the α vs α² character. Avoids the cyclotomic `Together` blowup by
substituting the *original* (simple) `F` into `t(z)` before any projection, and
gates each stage with the numeric `is_cube_function` / `sample_clearly_nonzero`
checks; `Solve`'s fixed points are `Simplify`-ed (they come back as
un-simplified nested cube-roots-of-unity, e.g. collapsing to `1` and `-2`).
Correctness is enforced by `gs_core`'s `diff_back_ok` guard.

Scope: this implements the single-fixed-point period-3 reduction. The
cross-character A4 cases (Goursat **Section 5**, e.g. the Legendre–Clausen
`Integrate[t/((t^3+8) Sqrt[t^3-1]), t]`, which is a combination of period-3
characters under *different* fixed points) are not yet handled and still
decline. Tests in `tests/test_integrate_goursat.c::test_period3`.

## Perf: `Integrate`GoursatAlgebraic` fast numeric elementarity gate (2026-06-29)

Integrands whose radicand `R` has cyclotomic roots but that **fail the V4 /
period-3 eigenspace criterion** took ~12s to decline — they exhausted the 6
CPU-second `TimeConstrained` budget inside the criterion's `canonic`/`Simplify`
(Together/Cancel over the splitting field of `R`, which blows up
super-polynomially). The criterion only needs a *zero test* of a projection, so
it is now decided numerically on the **uncombined** projection before any
symbolic combination — turning a 12s timeout-decline into a fast decline:

- **Square-root (V4) case.** The trivial-character projection
  `P0 = (f0+f1+f2+f3)/4` is sampled at a spread of fixed rationals via
  `N[Abs[…], 30]` (a `sample_clearly_nonzero` helper). A decisively non-zero,
  finite sample declines instantly; only a vanishing sample authorises building
  the three non-trivial projections and descending.
- **Cube-root / fourth-root cases.** The discarded criterion eigenprojection
  (`H1` for `p=1/3`; `H0` for `p=2/3`; `H1,H2` for `p=1/4`; `H0,H1` for `p=3/4`)
  is built in a *raw* `evaluate`-only form (`eigenproj3_raw`/`eigenproj4_raw`,
  no `canonic`/`Simplify`) from the pre-`canonic` integrand source and sampled
  the same way — sidestepping both the `H` canonicalisation and the eigenproj
  `Simplify`.

The gate is a fast-reject only: a passing (zero-ish) sample falls through to the
identical symbolic path, and every emitted antiderivative is still independently
verified by `gs_core`'s `diff_back_ok` differentiate-back check, so a numeric
false-positive-for-zero cannot yield a wrong answer. A sample is counted as
evidence of non-vanishing only when `|·|` is both above the `1e-6` noise floor
**and** below `1e9` (a point landing on a pole of some `f_j` numericalises to
`Infinity` though the symbolic projection is identically zero, and must be
skipped, not mistaken for a genuine non-zero).

Measured: `Integrate[(x-1)/((x+2) Sqrt[x^3-1]), x, Method -> "GoursatAlgebraic"]`
12.1s → 0.04s; `Integrate[(1-x^3)^(1/3)/(1+x), …]` 12.0s → 0.16s; the
`integrate_goursat` test suite 16.8s → 4.7s. All elementary cases still compute
their full antiderivatives and all tests pass. Implementation in
`src/calculus/integrate_goursat.c` (`sample_clearly_nonzero`, `eigenproj3_raw`,
`eigenproj4_raw`, and the three gate sites in `goursat_v4`/`goursat_cubic`/
`goursat_quartic`).

## Fix: `Integrate`GoursatAlgebraic` positive-power radicals + a hang guard (2026-06-29)

`Integrate[(1 - x^3)^(1/3)/x, x, Method -> "GoursatAlgebraic"]` returned
unevaluated even though it is a textbook cube-root Goursat integral. It now
closes (to `(1-x^3)^(1/3) - ArcTan[…]/Sqrt[3] - …`), both under
`Method -> "GoursatAlgebraic"` and in the default `Integrate` cascade.

**Recognition: any rational exponent, not just radicals in the denominator.**
The recogniser previously matched only the exact exponents
`-1/2, -1/3, -2/3, -1/4, -3/4` — i.e. a radical already in the denominator. A
*positive*-power radical such as `(1-x^3)^(1/3)` was rejected outright. The
recogniser now accepts any rational exponent `n/d` whose reduced denominator is
`2`, `3`, or `4`, splitting `R^(n/d) = R^k · R^(-p)` where the radical order
`p = pnum/d ∈ {1/2,1/3,2/3,1/4,3/4}` (with `pnum = (-n) mod d`) and the integer
remainder `k = (n+pnum)/d` folds into the rational cofactor `F` as `R^k`. So
`(1-x^3)^(1/3)/x = (1-x^3)/x · (1-x^3)^(-2/3)` is recognised as the `p = 2/3`
case with `F = (1-x^3)/x`. The old exact-negative exponents are the `k = 0`
special case, so previously-working inputs are unchanged.

**Termination guard.** The cube-/fourth-root descents canonicalise rational
functions over the splitting field of `R`. When `R` has cyclotomic roots
(e.g. `1 - x^3`, whose roots are the cube roots of unity), the core
`Together`/`Cancel`/`Simplify` over that algebraic-number field can blow up
super-polynomially for unlucky cofactors — `Sqrt[1-x^3]/x` and
`(1-x^3)^(1/3)/(1+x)` spin for minutes inside `Cancel`/`Simplify`, even though
the same machinery dispatches `(1-x^3)^(1/3)/x` in a tenth of a second. Because
`Integrate`GoursatAlgebraic` sits in the *default* `Integrate` cascade, an
unbounded attempt would hang every `Integrate[]` of such an integrand. The
outermost attempt is now wrapped in a CPU-time budget (`TimeConstrained`, whose
SIGPROF abort can interrupt the in-flight C canonicaliser); on exhaustion the
integrator declines and the integral is left for the rest of the cascade.
Recursive genus-0 descents run inside that single timer. (Genuinely solving the
cyclotomic-cofactor cases such as `(1-x^3)^(1/3)/(1+x)` awaits a faster
algebraic-number `Together`/`Cancel`; today they decline rather than hang.)

## Fix: `Integrate`GoursatAlgebraic` branch-cut error in the square-root reduction (2026-06-29)

`Integrate[(x^4 + 2 x^3 - 4)/(x^2 Sqrt[(x^2-1)(x^2-4)]), x, Method -> "GoursatAlgebraic"]`
— a textbook pseudo-elliptic integral — returned unevaluated with a
`Power::infy: 1/0` message instead of its (elementary) antiderivative.

**Root cause.** In Goursat's V4 (square-root) reduction, each anti-invariant
eigenpiece descends to `Integrate[gx / Sqrt[Q], x]` with `Q` the *monic*
quadratic obtained by dividing the descended radicand `H` by its leading
coefficient `lc`, with `Sqrt[lc]` pulled out into the prefactor `(α-β)/(2
Sqrt[lc])`. That factoring is invalid across the branch cut: `Sqrt[lc] Sqrt[Q]
!= Sqrt[lc Q]` whenever the monic `Q` evaluates negative (here `lc = -2` and the
relevant `Q` is negative on the whole integration interval), so the assembled
antiderivative was wrong and the differentiate-back guard correctly rejected it
— but only after a stray `1/0`.

**Fix** (`src/calculus/integrate_goursat.c`):
- New `rebase_radical`: after the (fast, exact) monic integration, rewrite every
  `Q^p` in the result as `(lc Q)^p · lc^(-p)` — an identity that re-expresses the
  radical as the *true* `H = lc Q` (which, after back-substitution, collapses to
  the clean real `Sqrt[R]·rational`) while `lc` survives only as a constant that
  cancels the prefactor's `1/Sqrt[lc]`. No branch cut is straddled on a variable
  radicand, and the recursive reduction stays monic (Euler-I, integer
  coefficients) so it remains fast.
- The differentiate-back guard now verifies numerically (`diff_back_ok`):
  `D[result,x] - f` is sampled at several fixed rational points with `Abs`
  (robust to the `(x-β)` poles and the complex `R<0` stretch that defeat
  `PossibleZeroQ`'s real sampling on the un-simplified nested radical). It still
  rejects the Ferrari-quartic misfire (a wrong piece differs from `f` by O(1)).

The integral now closes in ~0.9s — both under `Method -> "GoursatAlgebraic"` and
via the default `Integrate` cascade — and differentiates back to the integrand.
The cube-/fourth-root reductions are unaffected (their `cval^(1/3)`, `cval^(1/4)`
roots are odd and single-valued on the reals). Regression test in
`tests/test_integrate_goursat.c`.

## Feature: user-pinned substitution for Integrate's DerivativeDivides (2026-06-29)

`Integrate`'s `Method` option now accepts a list form for `"DerivativeDivides"`
that pins the substitution kernel:

```mathematica
Integrate[Sqrt[x]/(1 + Sqrt[x]), x,
          Method -> {"DerivativeDivides", "Substitution" -> Sqrt[x]}]
(* -2 Sqrt[x] + x + 2 Log[1 + Sqrt[x]] *)
```

Without the sub-option, `"DerivativeDivides"` collects **every** `x`-dependent
subexpression as a candidate kernel and trials each. The
`"Substitution" -> u` sub-option restricts the search to the single kernel
`u(x)`, running both the direct-quotient and the Eliminate/Solve strategies on
it. Behaviour is strict, as for all named methods: if `u` does not close the
integral the call returns unevaluated, with no fallback to the automatic kernel
search (e.g. `Substitution -> x^2` on the non-elementary `Sin[x^2]`).

Implementation: a new `forced_kernel` parameter on `dd_core`
(`src/calculus/integrate_derivdivides.c`) plus the public entry
`integrate_derivdivides_with_sub`; `parse_method_option`
(`src/calculus/integrate.c`) gained a `{"<method>", subopt -> val, ...}` list
branch mirroring the existing `FactorInteger` list-form precedent
(`src/facint.c`), with the `method_from_string` mapping factored out for reuse.
Only `"Substitution"`, and only for `"DerivativeDivides"`, is recognised; any
other sub-option key is `Integrate::method`-invalid. Covered by
`test_substitution_option` in `tests/test_integrate_derivdivides.c`.

## Feature: Chebychev binomial integrator `Integrate`ChebychevAlgebraic` (2026-06-29)

A new, fast integration method for Chebychev binomial differentials
`x^p (a x^r + b)^q` (`p, q, r` rational, `a, b` free of `x`).  By Chebychev's
theorem (1853) such an integral is elementary iff one of `q`, `(p+1)/r`,
`q+(p+1)/r` is an integer, each giving a substitution that turns the integrand
into a *rational function*:

- **Type I** (`q ∈ ℤ`): `x = u^N`, `N = LCM(den p, den r)`.
- **Type II** (`(p+1)/r ∈ ℤ`): `u^s = a x^r + b`, `s = den q`.
- **Type III** (`q+(p+1)/r ∈ ℤ`): `u = x^r`, then `t^s = (a u + b)/u`.

```mathematica
Integrate[x^(1/3) (a Sqrt[x] + b)^3, x]                     (* Type I  *)
Integrate[x^(3/2) (a x^(5/6) + b)^(1/3), x]                 (* Type II *)
Integrate[x^(-5/6) Sqrt[x^(1/3) - 1], x]
(* 3 x^(1/6) Sqrt[-1 + x^(1/3)] - 3 ArcTanh[Sqrt[-1 + x^(1/3)]/x^(1/6)]  Type III *)
```

Recognition is a single structural scan, so the method is cheap enough to run
ahead of `DerivativeDivides`'s Eliminate/Solve search (it sits between
`LinearRatioRadicals` and `Weierstrass` in the cascade — stage 6 of 10).  The
reduced rational integral is handled by the `BronsteinRational` stage and the
result is back-substituted.  Types I and II introduce a single principal radical
and are correct by construction (no differentiate-back verification, which is
branch-fragile with symbolic `a, b`); Type III is branch-sensitive and uses a
**split** back-substitution `t -> (a x^r + b)^(1/s) / x^(r/s)` so every radical
in the antiderivative is the principal root of a sub-expression of the original
integrand.

Non-elementary binomials (none of the three quantities integral, e.g.
`(a x^2 + b)^(1/3)`, or `1/Sqrt[x^4 + 1]`) return `NULL`, so under the default
`Method -> Automatic` the cascade falls through to the remaining methods; only
the forced `Method -> "ChebychevAlgebraic"` form surfaces as unevaluated.

Also available as `Integrate[f, x, Method -> "ChebychevAlgebraic"]` and the
package head `Integrate`ChebychevAlgebraic[f, x]`.

Implementation: `src/calculus/integrate_chebychev.{c,h}` (modelled on
`integrate_linrad.c` / `integrate_linratiorad.c`), wired into the cascade,
`method_from_string`, the `Method` switch and warning, and `integrate_init`
(`src/calculus/integrate.c`).  Covered by `tests/test_integrate_chebychev.c`
(all of `chebychev_problems.tex` problems 1 and 3, the worked Type III example,
problem-4 non-elementary cases, and method plumbing).

## Feature: Goursat pseudo-elliptic integrator `Integrate`GoursatAlgebraic` (2026-06-29)

A new integration method for **pseudo-elliptic integrals** `F(t)/R(t)^p`
(`F` rational, `R` a polynomial, `p ∈ {1/2, 1/3, 2/3, 1/4, 3/4}`) — integrands
that live on a positive-genus algebraic curve `y^n = R(t)` (`n = den p`) yet
integrate elementarily.  Goursat's 1887 algorithm and its cube-/fourth-root
generalisations (Blake 2026) detect this via a Möbius automorphism cycling the
roots of `R`, which splits the integrand into eigencomponents; when the
obstructive components vanish the rest descend to genus-0 curves with rational
antiderivatives.

```mathematica
Integrate[t/Sqrt[(t^2-1)(t^2-4)], t]      (* p=1/2, V4 — elementary *)
Integrate[t^2/(t^3-1)^(1/3), t]           (* (t^3-1)^(2/3)/2 *)
Integrate[t^2/(t^3-1)^(2/3), t]           (* (t^3-1)^(1/3)   *)
Integrate[t^3/(t^4-1)^(1/4), t]           (* (t^4-1)^(3/4)/3 *)
Integrate[t^3/(t^4-1)^(3/4), t]           (* (t^4-1)^(1/4)   *)
```

Criteria by exponent (else obstructed → `NULL`): `p=1/2` the Klein four-group
`V4` of root-pairing involutions, elementary iff the trivial-character
projection `F^(0)` vanishes (`R` cubic or quartic); `p=1/3, 2/3` an order-3
cyclic Möbius, elementary iff the `ω`-eigencomponent (`H1` at 1/3, `H0` at 2/3)
vanishes (`R` cubic, or quadratic); `p=1/4, 3/4` an order-4 cyclic Möbius on
roots in **harmonic position** (cross-ratio `-1`), elementary iff two
eigencomponents vanish (`V1,V2` at 1/4; `V0,V1` at 3/4).

The method orchestrates existing builtins (`Solve` with `Cubics/Quartics ->
True`, `Together`/`Cancel` over algebraic extensions, `PossibleZeroQ`,
`Coefficient`, `Numerator`/`Denominator`), recurses `Integrate` on the rational
reductions, and back-substitutes.  `Simplify` collapses roots-of-unity
identities (e.g. `1 + ω + ω^2 → 0`) that `Cancel`/`Together` leave intact.  It
runs ahead of `DerivativeDivides`/Risch in the cascade (stage 7 of 11), filling
a gap no other radical method covers (cubic/quartic radicands).

A differentiate-back guard `PossibleZeroQ[D[result, t] - f]` runs before
returning: the eigenspace zero-test can misfire on deeply nested radical roots
(an irreducible quartic solved via Ferrari) where catastrophic cancellation
numericalises a genuinely nonzero projection to ~0, so the guard rejects such
cases (declining rather than emitting a wrong answer).  Every canonical
factorable/biquadratic example passes it trivially.

Also available as `Integrate[f, x, Method -> "GoursatAlgebraic"]` and the
package head `Integrate`GoursatAlgebraic[f, x]`.

Implementation: `src/calculus/integrate_goursat.{c,h}`, wired into the cascade,
`method_from_string`, the `Method` switch and warning, and `integrate_init`
(`src/calculus/integrate.c`).  Reference: `goursat/GoursatAppendix.wl` and the
two preprints.  Covered by `tests/test_integrate_goursat.c` (square-, cube-, and
fourth-root worked examples, obstructed/non-harmonic declines, method plumbing).

## Feature: Ferrari radical quartic solver for `Solve` (2026-06-29)

`Solve[quartic == 0, x, Quartics -> True]` now returns the four roots in
radicals (Ferrari's resolvent-cubic method), where previously the
`Quartics -> True` option was parsed but ignored (degree-4 factors always became
held `Root[]` objects).  Mirrors the existing Cardano `Cubics -> True` path.

```mathematica
Solve[x^4 + x + 1 == 0, x, Quartics -> True]   (* four radical roots *)
```

The radical path is taken only for the Complexes domain; a `Reals` request
still falls through to `Root[]` (a faithful real/non-real split of quartic
radicals is not attempted).  Biquadratic and binomial quartics keep their
existing dedicated fast paths.  Implementation: `solve_quartic_radical` in
`src/poly/solvepoly.c`, wired into the per-factor dispatcher.

## Fix: Plot adaptive sampler density was inverted (2026-06-29)

`Plot[Sin[x], {x, 0, 2 Pi}, Mesh -> All]` placed **more** sample points on the
straight zero-crossings than on the curvy peaks — the opposite of curvature-
proportional sampling. Root cause: the absolute screen-space chord-**length**
cap (`MAX_CHORD_FRAC`), which keys on *slope* not curvature, was tight enough
(≈1/44 of the frame) to out-compete the curvature (vertical-gap) test on
ordinary moderately-sloped curves, pouring samples into the steep-but-straight
stretches while the loose curvature tolerance (`FLAT_TOL`) let the actually-
curvy peaks pass as "flat" at the 50-point grid scale.

Fix (`src/graphics/sampling.c`): tightened `FLAT_TOL` `0.0025 → 0.0006` so the
curvature test is the primary driver (density now tracks `|f''|`), and relaxed
`MAX_CHORD_FRAC` `0.0225 → 0.08` to a loose backstop that only binds on genuine
near-vertical gaps (asymptotes). Verified across `Sin` (peaks now ~2× denser
than zero-crossings), `x^3` (densest at the curved ends), `x^2` (uniform —
constant curvature), a straight line and `Exp` (no wasted refinement), the
`Runge` spike (peaked at centre), and `Sin[22 x]` (oscillation still fully
resolved). Total point counts dropped (less wasteful) while the shape is now
correct. Clean build; the chord cap's asymptote/`Log` protection is preserved.

## Real Hershey vector font for graphics text (2026-06-29)

Graphics text (`Text[]`, axis tick labels, `AxesLabel`/`PlotLabel`) now uses a
faithful transcription of the classic Hershey **Roman Simplex** single-stroke
font, replacing the previous small hand-authored stick alphabet.

- Glyph data is generated from the historical Hershey dataset
  (`tools/hershey.dat`) by `tools/gen_hershey.py` into
  `src/graphics/hershey_glyphs.inc`. The build and runtime read only the
  generated `.inc`; re-run the generator (and rebuild) to change the font set.
- Full printable ASCII (32–126): digits, **real upper- and lowercase** letters
  (no more folding lowercase to uppercase), punctuation and symbols, with
  **proportional advance widths** (`hershey_text_width` sums per-glyph bearings).
- Raw Hershey coordinates (design cap height 21, baseline-down) are normalised
  at draw time into the renderer's existing 7-unit cap-height frame
  (`HERSHEY_CAP_HEIGHT`), so all `scale`/positioning math and every caller in
  `render.c` are unchanged and text sizes match the prior build. Glyph shapes,
  lowercase, and spacing are substantially improved.
- Strokes are drawn at 2× weight via a single `HERSHEY_STROKE_SCALE` knob in
  `hershey_font.c`, with rounded joins, so labels read more boldly.
- The makefile now lists `hershey_glyphs.inc` as a prerequisite of
  `hershey_font.o`, so regenerating the font forces a recompile.

## Arithmetic of `SeriesData` (2026-06-29)

`Plus`, `Times`, and `Power` now combine `SeriesData[x, x0, {coefs}, nmin,
nmax, den]` objects, matching Mathematica. `Divide` and `Subtract` are covered
automatically because they reduce to `Times[a, Power[b,-1]]` and
`Plus[a, Times[-1, b]]`. Previously a `SeriesData` was opaque to the arithmetic
builtins, so `series + 1`, `x series`, `series^3`, `s1 s2`, `s1/s2` all stayed
unevaluated.

- **Plus / Times** fold every operand into a single `SeriesData` about the
  common `(x, x0)`, truncated to the **minimum** O-term order of the operands.
  A non-series operand free of `x` folds into the `a0` coefficient (fast path
  for `series + scalar`); the bare variable, polynomials, and transcendental
  functions are series-expanded with the full engine and combined. Operands
  about a **different** variable or expansion point are left as a symbolic
  `Plus`/`Times` (unevaluated), as in Mathematica.
- **Power** handles integer exponents (negative via series inversion) and
  scalar exponents free of the series variable directly. An exponent that
  depends on the series variable — or an ordinary base raised to a series
  exponent, e.g. `2^Series[x,{x,0,3}]` — is evaluated as `Exp[exp*Log[base]]`
  and re-expanded.
- **Mixed exact/approximate precision** works per-coefficient: coefficient
  arithmetic flows through the evaluator (`Plus`/`Times`), so integer, bigint,
  rational, machine-real, and MPFR coefficients mix with the usual contagion.
  Adding a real to a series folds it into `a0` as a real while the remaining
  coefficients stay exact.

Verified end-to-end: `Series[Exp[x],{x,0,2}]+1` → `2+x+x^2/2+O[x]^3`;
`x Series[Sin[x],{x,0,5}]` → `x^2-x^4/6+x^6/120+O[x]^7`;
`Series[Exp[x],{x,0,2}] Series[Exp[x],{x,0,3}]` → `1+2x+2x^2+O[x]^3` (order is
the min); `Series[Exp[x],{x,0,2}]^3` → `1+3x+9x^2/2+O[x]^3`;
`2^Series[x,{x,0,3}]` → `1+Log[2] x+Log[2]^2/2 x^2+Log[2]^3/6 x^3+O[x]^4`;
`Series[Exp[x],{x,0,3}]^Series[x,{x,0,3}]` → `1+x^2+O[x]^4` (= `Exp[x^2]`).

**Implementation.** Public entry points `series_combine_plus`,
`series_combine_times`, `series_power`, and the `is_series_data` predicate in
`src/calculus/series.c` (declared in `series.h`), driving the existing
`SeriesObj` algebra (`so_add`, `so_mul`, `so_pow_int`, `so_pow_expr`, `so_inv`,
`so_align_den`). A new `SeriesData` leaf case in `series_expand` lets a series
embedded in an expression (the `Exp[exp*Log[base]]` rewrite) flow through the
expander. Cheap `is_series_data` dispatch added before the grouping loops in
`builtin_plus` / `builtin_times` (ahead of inexact contagion so it cannot
re-`N` the structural integers) and after the identity reductions in
`builtin_power`. Tests in `tests/test_series.c`; valgrind shows no new leaks vs.
the macOS dyld baseline. Documented in
`docs/spec/builtins/power-series.md`.

Two guards keep folding faithful and terminating. A non-series operand whose
dependence on the series variable is *non-analytic* (a piecewise-constant head
such as `Floor`, `Arg`, `Sign`, ...) cannot be power-series expanded, so it
makes the combine bail to a symbolic `Plus`/`Times` instead of looping in
`series_expand` — this preserves the branch-point wrapper
`Times[(-1)^Floor[...], SeriesData[...]]` produced by `Series[ArcCosh[x+1], ...]`
and friends. And for a series at `Infinity` (where the formal variable is
`1/x`), a factor that is structurally free of `1/x` but still depends on the
underlying `x` (e.g. `E^x` in `Times[E^x, SeriesData[1/x, ...]]` from
`Series[ExpIntegralEi[x], {x, Infinity, n}]`) is *not* treated as a constant and
stays outside the series, matching Mathematica.

## Differentiation and integration of `SeriesData` (2026-06-29)

`D` and `Integrate` now act on `SeriesData[x, x0, {coefs}, nmin, nmax, den]`
objects term-by-term, returning a new `SeriesData`, matching Mathematica.
Previously `D[SeriesData[...], x]` fell through to the generic chain-rule
fallback (producing a meaningless `Derivative[1,0,...][SeriesData][...]`) and
`Integrate[SeriesData[...], x]` matched no method and stayed unevaluated.

- **Differentiation w.r.t. the series variable** applies the power rule: the
  coefficient of `(x - x0)^((nmin+i)/den)` is scaled by `(nmin+i)/den` and the
  exponent drops by one. `nmin` and `nmax` each decrease by `den`; the
  differentiated constant becomes a leading zero and is trimmed.
- **Integration w.r.t. the series variable** scales each coefficient by
  `den/(nmin+i+den)` and raises the exponent by one; `nmin` and `nmax` each
  increase by `den`. The integration constant is `0` (as in Mathematica). A
  nonzero `(x - x0)^-1` residue term would integrate to a `Log` that
  `SeriesData` cannot represent, so `Integrate` is left unevaluated in that
  case.
- **W.r.t. a different variable** (when `x0` is free of it) both operations
  thread into the coefficients, leaving the powers of `(x - x0)` unchanged.

Verified end-to-end: `Integrate[Series[Exp[x],{x,0,8}], x]` →
`x + x^2/2 + ... + x^9/362880 + O[x]^10`; `D[Series[Exp[x],{x,0,8}], x]` →
`1 + x + ... + x^7/5040 + O[x]^8`; the two round-trip; `1/x^2 + 1` integrates to
`-1/x + x + O[x]^5` while a true `1/x` residue stays unevaluated.

**Implementation.** Two helpers `series_differentiate` / `series_integrate`
(plus a private `so_from_seriesdata` reader) in `src/calculus/series.c`, reusing
the existing `SeriesObj` machinery (`so_alloc`, `so_set_coef`,
`so_trim_leading`, `so_to_expr`); one-line dispatch on `SYM_SeriesData` added to
`compute_deriv` in `src/calculus/deriv.c` and to `builtin_integrate` in
`src/calculus/integrate.c`. Tests in `tests/test_series.c`; valgrind-clean
(no new leaks vs. the macOS dyld baseline). Documented in
`docs/spec/builtins/power-series.md`.

## Tests/Docs: Goursat graded exercise ladder (2026-06-30)

Added a graded battery of Goursat pseudo-elliptic exercises, easy → hard, each
**forced through `Method -> "GoursatAlgebraic"`** so it is genuinely solved by
that integrator. The set spans every radical exponent `p ∈ {1/2,1/3,2/3,1/4,3/4}`,
**both signs of the exponent** (radical in the numerator `R^(+p)` and the
denominator `R^(-p)`), and **every involution equation**: square-root `V4`
(`P0==0`), period-3 (`F+F(S)+F(S²)==0`), cube-root (`H1==0` / `H0==0`),
fourth-root harmonic (`H1==H2==0` / `H0==H1==0`), plus negative controls that the
gate must decline (obstructed criteria, Chebyshev-non-elementary, genuinely
elliptic). Captured as `test_graded` in `tests/test_integrate_goursat.c` (closed
forms verified by symbolic differentiate-back, with a numeric check for the
complex-Log period-3 and the large positive-`1/2` numerator case) and documented
as `GOURSAT_EXERCISES.md` at the repository root, linked from
`docs/spec/builtins/calculus.md`. No engine changes.

## Infra: FLINT-backed algebraic-extension engine — M1 foundation (2026-06-30)

First increment of the FLINT-backed polynomial-arithmetic engine specified in
`ALGEBRAIC_EXTENSION_ARITHMETIC_PLAN.md` (the rigorous, performant replacement
for the pseudo-variable `Together`/`Cancel`/`Factor` paths that hang over
algebraic-extension coefficient rings).

**Build integration.** New `USE_FLINT` switch in `makefile`, autodetected via
`pkg-config 'flint >= 3.0'` (the version floor at which ANTIC — number-field
arithmetic — was merged into FLINT) with the same graceful-degrade policy as
`USE_MPFR`/`USE_LAPACK`/`USE_GRAPHICS`: when FLINT is absent the build still
succeeds and the algebraic-extension paths fall back to the classical (slower
but rigorous) code. FLINT's hard deps (GMP, MPFR) were already linked. System
library only (LGPL-3, GPLv3-compatible); no `src/external/` vendoring.

**Bridge.** New `src/poly/flint_bridge.{c,h}` — the `Expr ⇄ FLINT` boundary.
This increment implements the rational multivariate case `R = Q[x_1..x_n]`:
recognise an `Expr` as a member of `Q[x]` (integer/`Rational` coefficients;
`Plus`/`Times`/`Power`-with-nonneg-integer-exponent), convert to `fmpq_mpoly`,
and render results back to `Expr`. Exercised end-to-end through a scaffolding
builtin `` Flint`GCD[a, b] `` (multivariate GCD over Q via `fmpq_mpoly_gcd`), which
returns unevaluated — deferring to the classical path — on any out-of-scope
input (radicals, symbolic/negative powers, inexact reals, pure numerics). To be
removed when real consumers are re-pointed (plan M5). The whole module compiles
to stubs under `USE_FLINT=0`. `flint_cleanup_master` is registered via `atexit`
so FLINT's thread-local pools are released at teardown; valgrind shows no
`flint_bridge`/`libflint` leak (`definitely lost` stays at the macOS dyld/
Accelerate baseline; FLINT-pool `possibly lost` dropped 136 KB → 1.6 KB).

The M1 API question is resolved: number-field univariate GCD/factor will use
FLINT's generic-ring (`gr`) layer — `gr_ctx_init_nf` + `gr_poly_gcd` /
`gr_poly_factor_squarefree` (probe-verified that `gcd(x²−2, x−α) = x−α` over
`Q(√2)`, the rigorous split the free-variable view misses) — landing next in M2.

**Tests.** `tests/test_flint_bridge.c` (wired into `tests/CMakeLists.txt` with a
`USE_FLINT` pkg-config probe mirroring the makefile, and `flint_bridge.c` added
to `COMMON_SRC`): 10 groups — univariate, multivariate, monic normalisation
(sign + content stripping), rational coefficients, big-integer coefficients
(beyond `int64`), zero/identity edge cases, determinism across variable order,
nine bail-out cases (radicals, inexact reals, Laurent/symbolic exponents, pure
numerics, transcendental heads → the bridge declines with NULL), the builtin
path through the evaluator, and the `Protected` attribute. The binary skips
itself cleanly when built `USE_FLINT=0`. valgrind shows no leak record citing
the bridge; existing suites (e.g. `expr_tests`) still pass against the modified
common build.

## Infra: FLINT engine — M2, GCD over a quadratic number field Q(√d) (2026-06-30)

Second increment of the FLINT-backed engine: rigorous univariate GCD over a real
quadratic number field `Q(√d)` (single radical generator, `d` a positive
non-square integer) — the `p = 0, r = 1` case of
`ALGEBRAIC_EXTENSION_ARITHMETIC_PLAN.md`.

**Capability.** `flint_numberfield_gcd(a, b)` in `src/poly/flint_bridge.c`:
detects a single `Sqrt[d] = Power[d, Rational[1,2]]` generator and one
polynomial variable across both operands; builds the field via FLINT's
generic-ring number-field layer (`gr_ctx_init_nf` on the minimal polynomial
`y² − d`); converts each input to a `gr_poly` in x over `Q(√d)` (going through a
two-generator `fmpq_mpoly` in `{x, s}` and reducing `s² = d`); computes the
FLINT-monic GCD with `gr_poly_gcd`; and renders it back to an `Expr` with
coefficients `c0 + c1 √d` (read out of each `nf_elem` via a parallel `nf_t` and
`nf_elem_get_fmpq_poly`). This makes the **rigorous split** the rational/
free-variable view cannot: `gcd(x² − 2, x − √2) = x − √2` (not 1), and likewise
mixed coefficients, e.g. `gcd((x−1−√2)(x−1), (x−1−√2)(x−2)) = x − 1 − √2`.

The scaffolding builtin `` Flint`GCD `` now dispatches rational-multivariate
first, then this number-field path; it still declines (stays unevaluated) for
out-of-scope inputs. Towers / two distinct radicals, multivariate-over-a-field,
non-integer radicands, and number-field **factoring** are later M2/M3 work; the
function returns NULL on those so nothing is silently mishandled.

**Tests.** New `test_numberfield` group in `tests/test_flint_bridge.c` — 12
positive cases (the canonical split, both conjugate signs, several radicands,
coprime-over-the-field → 1, monic normalisation of a `√d` leading coefficient,
mixed `c0 + c1√d` reconstruction) and 4 bail-outs (no radical, two radicals,
two variables, non-integer radicand). Full bridge suite green; valgrind shows no
leak record citing the bridge or the `nf`/`gr` objects it builds.

## Infra: FLINT engine — M2, GCD over a cyclotomic field Q(ζ_n) + generalised single-generator core (2026-06-30)

Generalised the M2 number-field GCD from "Q(√d) only" to **any simple algebraic
extension Q(s)** given a minimal polynomial, and added the **cyclotomic** case
Q(ζ_n) on top of it.

**Generalised core** (`absfield_gcd_core` in `src/poly/flint_bridge.c`). A
`GenSpec` maps an input atom to a power `s^k` of the generator; the core converts
each operand to an `fmpq_mpoly` in `{x, s}`, reduces each x-coefficient modulo
the minimal polynomial with `fmpq_poly_rem` (replacing the bespoke `s²=d` fold),
runs `gr_poly_gcd` over `Q(s)`, and reads the monic result back in the power
basis `{1, s, …, s^{m-1}}` (`nf_elem_get_fmpq_poly`). The Q(√d) path is now a
thin wrapper (`sqrt_atom_power`, minpoly `y²−d`); its existing tests are
unchanged and still pass, confirming the refactor.

**Cyclotomic** (`flint_cyclotomic_gcd`). Recognises roots of unity
`(-1)^(p/q) = Power[-1, Rational[p,q]]`, takes the field `Q(ζ_N)` with
`N = 2·lcm(q)` and minimal polynomial `Φ_N` (`fmpz_poly_cyclotomic`), maps each
`(-1)^(p/q)` to `ζ_N^{p·N/(2q)}`, and renders the GCD in the canonical generator
`(-1)^(1/Q)`. Example: `gcd(x²+x+1, x−(-1)^(2/3)) = x − ζ₃`, returned as
`1 − (-1)^(1/3) + x` (equal to `x − (-1)^(2/3)` in the `ζ₆` basis, confirmed by
`PossibleZeroQ`); `gcd(x⁴+x³+x²+x+1, x−(-1)^(2/5)) = x − (-1)^(2/5)`. The
scaffolding builtin `` Flint`GCD `` now dispatches rational → Q(√d) → Q(ζ_n).

Bails (NULL) on a mixed radical+root-of-unity input (a genuine **tower**, the
next milestone), the imaginary unit `Complex[0,1]` (deferred), two variables, or
no algebraic generator.

**Tests.** New `test_cyclotomic` group in `tests/test_flint_bridge.c` (Q(ζ₃),
Q(ζ₅), Q(ζ₆); exact canonical forms plus two basis-independent `PossibleZeroQ`
cross-checks; four bail-outs). Full bridge suite green; valgrind shows no leak
record citing the bridge or the `nf`/`gr`/`fmpz_poly` objects it builds.

## Infra: FLINT engine — M2, radical-tower collapse Q(√d₁,…,√d_r) (2026-06-30)

Completed the algebraic-extension **tower** case (plan §3.2.1): GCD over
`Q(√d₁,…,√d_r)`, r ≥ 2, by collapsing the tower to a single primitive element θ.

**Method** (`tower_build` in `src/poly/flint_bridge.c`) — fully-rational linear
algebra, no resultants. Each `√d_j` multiplies by a known matrix in the product
(bitmask) basis `{∏ √d_i^{e_i}}`, so `T = Σ c_j·(mult-by-√d_j)` is multiplication
by `θ = Σ c_j √d_j`. The columns `{1, θ, …, θ^{D−1}}` (D = 2^r) form `P`; when `P`
is invertible θ is primitive, the absolute minimal polynomial is read from
`θ^D = P·(P⁻¹θ^D)`, and each `√d_i`'s image in `Q(θ)` is `P⁻¹` applied to its
product-basis unit vector. Independent retries over small coefficient vectors
`c`; bails if the radicals are dependent (no full-degree θ).

The GCD runs through the shared `absfield_gcd_core` (now generalised to an
atom→image-polynomial `GenSpec` and a `CoefBuilder` readback). The tower readback
maps the power-basis result back through `P` into the **product basis**, so
coefficients print as clean single radicals — e.g.
`gcd((x−√2)(x−√3)(x−1), (x−√2)(x−√3)(x−2)) = x² − (√2+√3)x + √6` (the `√2·√3`
cross term reconstructed as `√6`), and `gcd((x−√2−√3)(x−1), (x−√2−√3)(x−2)) =
x − √2 − √3`. `` Flint`GCD `` dispatches rational → Q(√d) → Q(ζ_n) → tower.

The same generalisation refactored the Q(√d) and Q(ζ_n) paths onto the shared
core (their tests unchanged and green). **Tests:** new `test_tower` group
(Q(√2,√3), the √6 cross term, a degree-8 Q(√2,√3,√5) case, coprime → 1, three
bail-outs) plus a `PossibleZeroQ` semantic check. Full bridge suite green;
valgrind shows no leak record citing the bridge or the `fmpq_mat`/`nf`/`gr`
objects it builds.

## Infra: FLINT engine wired into PolynomialGCD / Cancel / Together (2026-06-30)

The FLINT algebraic-extension GCD/division now reaches the real consumers — the
improvements are no longer reachable only through the `` Flint`GCD `` scaffold.

**PolynomialGCD** (`builtin_polynomialgcd`, `src/poly/poly.c`): a `USE_FLINT`-
guarded hook tries `flint_extension_gcd` for the two-argument case. The field is
detected from the operands, so it fires for the bare `PolynomialGCD[a, b]` that
`Cancel`/`Together` issue internally as well as with a trailing `Extension`
option. Plain Q[x] and parametric inputs return NULL and fall through to the
classical path unchanged.

**Cancel / Together** (`src/rat.c`): a new `flint_cancel_fraction` intercepts at
the Cancel entry — `g = gcd(num,den)` then `num/g`, `den/g` by exact division —
ahead of the under-reducing QA extension path; and `cancel_exact_div_wrapper`'s
classical `exact_poly_div` now falls back to `flint_extension_divexact` when it
fails on an algebraic generator. Because `Cancel` issues `PolynomialGCD`
internally and `Together` shares the path, both now reduce rigorously.

**New bridge entry points** (`src/poly/flint_bridge.c`): the shared core was
generalised to `absfield_op_core` doing either GCD or **exact division**
(`gr_poly_divrem`, exact iff the remainder vanishes); `flint_extension_gcd` /
`flint_polynomial_gcd` / `flint_extension_divexact` dispatch across Q(√d) →
Q(ζ_n) → tower. (A consumer must evaluate the bridge's raw output tree before
re-feeding it — Cancel does this for the GCD before the divide-back.)

**Effect** — previously unreduced, now correct:
`Cancel[(x²-2)/(x-√2)] = x+√2`, `Cancel[(x²-3)/(x+√3)] = x-√3`,
`Cancel[(x²+x+1)/(x-ζ₃)] = x+ζ₆`, the √2+√3 tower fraction, and
`Together[1/(x-√2)+1/(x+√2)]`. Plain-rational `Cancel`/`PolynomialGCD` outputs
are unchanged.

**Tests.** New `test_consumers` group in `tests/test_flint_bridge.c` (Cancel/
Together/PolynomialGCD over Q(√d)/Q(ζ_n)/tower + a `PossibleZeroQ` semantic
check + plain-rational regressions). Two pre-existing expectations that encoded
the old under-reduced / non-canonical form were updated to the canonical FLINT
result, guarded by `#ifdef USE_FLINT` so the `USE_FLINT=0` config still asserts
the classical form: `test_extension_options.c` (`Cancel[(x²-2)/(x-√2)]` →
`x+√2`) and `test_extension_auto_builtins.c` (a unit-factor tower GCD →
canonical monic `x-1`). Regression suites green: rat, poly, qa, qaupoly,
qafactor, radical_polyops, extension_options, extension_autodetect,
extension_auto_builtins, intrat, radical_simplify, rationalize. valgrind on a
Q(√2)/tower `Cancel` shows no bridge leak.

## Tower algebraic-extension factoring, Apart, and rational integration (2026-07-01)

Three linked fixes so `Factor`, `Apart`, and `Integrate` work over a **radical
tower** `Q(√d₁, √d₂, …)` when the coefficients carry a *composite* radical such
as `Sqrt[6] = Sqrt[2] Sqrt[3]` (which appears the moment you expand
`(x-√2)(x-√3)`):

**`Factor[…, Extension -> {√2, √3, …}]`** (`src/poly/qafactor.c`,
`qa_factor_with_extension_tower`) now decomposes composite radicals into the
tower's prime-Sqrt basis (`decompose_redundant_sqrts`) and substitutes via the
shared, base-value-matching `tower_substitute_alphas` — the previous bare
structural `expr_subst` against the `Power[c,1/2]`-form generators missed the
`Sqrt`-headed decomposed factors, so the lift failed and the polynomial came
back unfactored. Now `Factor[x² - (√2+√3)x + √6, Extension -> {√2,√3}]` →
`(x-√2)(x-√3)`.

**`Apart[…, Extension -> Automatic]`** (`src/parfrac.c`) routes an
auto-detected multi-generator tower (n ≥ 2) as the generator List, instead of
falling back to no-extension. `Apart[x/((x-√2)(x+√3)), Extension -> Automatic]`
now splits into `√2/((√2+√3)(x-√2)) + √3/((√2+√3)(x+√3))`.

**Rational integration over a tower no longer hangs** (`src/poly/poly.c`,
`poly_div_rem`). The Rothstein–Trager log-part fed `PolynomialRemainder` a
polynomial whose coefficients live in `Q(√2,√3)`; the symbolic-`q_coeff` branch
subtracted `R − q·x^d·B` as a *whole polynomial* via `Together/Cancel`, which is
blind to `√2·√3 = √6` and left the leading coefficient a nonzero-*looking*
artifact — so the degree never dropped and the divide loop spun forever
(`Integrate[1/((x-√2)(x-√3)), x]`). The branch now subtracts **coefficient-wise
in the division variable**, simplifying each coefficient on its own (where the
radical relation *does* cancel) and dropping the `x^degR` term outright (it is
`lcR − q·lcB = 0` by construction), guaranteeing a strict degree decrease. The
antiderivative verifies (`D[·] − integrand` is numerically zero; `PossibleZeroQ`
→ True), though it lands in a nested-radical `ArcTan` form Cancel cannot reduce
to 0.

**Tests.** `test_qafactor.c` (composite-radical tower factor over `{√2,√3}` and
`{√3,√5}`), `test_poly.c` (radical-coefficient `PolynomialRemainder` exercising
the `√2·√3=√6` cancellation), `test_intrat.c`
(`test_intrationallogpart_tower_terminates`, `PossibleZeroQ` verification that
also guards the hang), `test_extension_auto_builtins.c` (Apart tower auto vs
explicit list + `Head == Plus`). Regressions green: poly, rat, intrat,
qafactor, qa, extension_options/autodetect/auto_builtins, flint_bridge,
radical_simplify, radical_polyops. New paths valgrind-clean (definitely-lost at
macOS baseline). Pre-existing unrelated soft-fails unchanged (3 `simplify_tests`
symbolic `Sqrt[x²]`, 1 `solve_tests` nested-radical hidden zero).

## FLINT engine — parametric radical GCD/division Q(t)(√k) (M3, 2026-07-01)

GCD and exact division over the **parametric radical field** `Q(t_1,…,t_p)(√k)`
where the radicand `k` is a free *symbol* (parameter), not an integer — the
Goursat blocker ring `Q(a,b,k)(√k)[x]`.

**Key simplification.** `√k` is transcendental over `Q(t)` with `k = (√k)²`, so
`Q(a,b,k)(√k)` **is** the rational function field `Q(a,b,√k)` — no genuine
algebraic relation to reduce. The regime collapses to ordinary multivariate GCD
over `Q` via the substitution `√k → S, k → S²`, `fmpq_mpoly_gcd` (already
rigorous and fast), then readback `S → √k` (folding `S² → k` under evaluation).
So *no `fq_nmod` residue field, CRT, or Encarnación outer loop* is needed for
this regime — those stay for genuine constant-coefficient minimal polynomials
(later milestone).

**New** (`src/poly/flint_bridge.{c,h}`): `flint_parametric_sqrt_gcd` and
`flint_multivariate_divexact` (`fmpq_mpoly_divides`); `ps_detect` recognises the
single-symbolic-radicand regime (bails on a second radical, cube root, root of
unity, or two distinct radicands). Wired into `flint_extension_gcd` and
`flint_extension_divexact`, so `PolynomialGCD` / `Cancel` / `Together` reduce over
`Q(a,b,k)(√k)` **sub-second** (the multivariate-`PolynomialExtendedGCD` /
`exact_poly_div` chain that hung before). `Cancel[(x²−k)/(x−√k)] = √k + x`;
`Cancel[((x−√k)(a x²+b x+k))/((x−√k)(x³+a k))] = (a x²+b x+k)/(x³+a k)` (verified
via `PossibleZeroQ`).

Also fixed a `flint_cancel_fraction` cosmetic artifact: when the divided-out
denominator is a numeric constant (the unit `−1` from a GCD FLINT normalised
monic in its own variable order rather than in `x`), the result is now
distributed (`√k + x`, not `−(−√k − x)`); guarded against a use-after-free in the
numeric-check.

**Tests.** `test_parametric` in `tests/test_flint_bridge.c` (raw-bridge exact GCD,
consumer canonical forms, `PossibleZeroQ` semantic checks, and the four bail
cases). Regressions green: rat, poly, intrat, qafactor, qa, flint_bridge,
extension_options/autodetect/auto_builtins, radical_simplify, radical_polyops.
valgrind-clean (definitely-lost at macOS baseline). 3 pre-existing `simplify_tests`
`Sqrt[x²]` soft-fails unchanged.

## FLINT parametric radical wired into Cancel/Together Extension forms; Goursat M7 progress (2026-07-01)

**Cancel/Together with `Extension -> ...` now route through the FLINT parametric
path** (`src/rat.c`). Previously the FLINT `flint_cancel_fraction` hook only
fired for the bare 1-arg `Cancel[e]`; the 2-arg `Cancel[e, Extension -> ...]` /
`Together[e, Extension -> ...]` forms — used by the Goursat descent's
`canonic = Cancel[Together[e, Ext], Ext]` — fell to the classical QA path, which
hangs over `Q(a,b,k)(√k)`. Added the hook to both `builtin_cancel_compute` and
`builtin_together_compute` after option stripping. The Goursat V4 descent's
`canonic` layer (over the parametric radical ring) is now fast.

`flint_cancel_fraction` also now finishes the reduction itself when the
parametric field leaves `num, den` coprime (`gcd == 1`) — returning `num/den`
rather than dropping into the non-terminating classical path. **Scoped to the
parametric regime only**: for number-field / cyclotomic / tower fields the
coprime case still hands back to the classical path (which additionally
simplifies roots-of-unity identities the cube-root Goursat descent relies on) —
without this scoping, `Integrate[1/(t(t³−1)^(2/3)), t, Method->"GoursatAlgebraic"]`
regressed. Also distributes a numeric divided-out denominator so the surface form
matches the classical path (`√k + x`, not `−(−√k − x)`).

**Leak fix** (`src/poly/qafactor.c` `qa_cancel_with_poly_radical`): two
`evaluate(expr_new_function(...))` sites leaked their wrapper trees (`evaluate`
does not free its input). Switched to `eval_and_free` — valgrind back to the
macOS baseline (was +864 bytes on a parametric-radical `Together` whose radical
cancels).

**Regression tests from the real descent** (`tests/test_flint_bridge.c`
`test_goursat_descent_operands`): 14 Cancel/Together operands captured from the
Goursat `√k` integral's V4 descent — rational functions over `Q(a,b,k)(√k)` with
`k^(1/2)…k^(9/2)` coefficients and nested `Sqrt[…/…]` radicands — asserted to
terminate and preserve value (deterministic fixed-point numeric check; these hung
before the parametric path). All suites green; valgrind-clean.

**M7 status:** the descent's `canonic` (Cancel/Together) layer over the
parametric ring is fixed. The remaining blocker is the inner
`∫G(u)/√(quadratic)` → `QuadraticRadicals` substitution → `IntRationalLogPart`
over a degree-4 rational with `k^(9/2)` coefficients (a huge parametric
Rothstein–Trager resultant); the two named integrals do not yet close end-to-end.

## FLINT parametric resultant; Rothstein-Trager diagnosis (2026-07-01)

**Why Rothstein–Trager chokes on the Goursat inner integral even though the
answer is a single ArcTan:** RT computes the log part from the roots of a
*resultant* `R(t) = Res_x(A − t·D', D)`. The answer being a simple ArcTan means
`R(t)` has a conjugate root pair — but RT still has to *compute* `R(t)` first, and
its cost is set by `deg D` and the *size of D's coefficients*, not by the answer.
Here `deg D = 4` (the √quadratic was rationalised by a `QuadraticRadicals`/Euler
substitution, doubling the degree) and D's coefficients are large polynomials in
`a, b, k` with `k^(1/2)…k^(9/2)`. The classical symbolic subresultant PRS on a
7×7 Sylvester system with those parametric-radical entries blows up — measured
**>90 s** just for the one resultant.

**Fix:** `flint_parametric_sqrt_resultant` (`src/poly/flint_bridge.c`) computes
`Res_var(a, b)` over `Q(t..)(√k)` via the same `√k → S, k → S²` collapse used by
the parametric GCD, then FLINT's `fmpq_mpoly_resultant`; readback `S → √k`. Hooked
into `builtin_resultant` for the parametric regime. The `k^(9/2)`-coefficient
resultant that timed out now returns in **~0.5 ms**, verified to agree exactly
with the classical resultant evaluated at a concrete `k`.
`flint_multivariate_resultant` (plain `Q`) added as the underlying primitive.

**Tests:** `test_parametric_resultant` in `tests/test_flint_bridge.c`
(`Res[x−√k, x+√k, x] = 2√k`; shared-root → 0; numeric agreement with the
classical resultant on the `k^(9/2)` case). Regressions green; valgrind-clean.

**M7 status (honest):** the two named integrals still do not close. The inner
`∫G/√quadratic` is a *chain* of bottlenecks over `Q(a,b,k)(√k)`: `canonic`
(Cancel/Together — fixed) → `Apart`/`Factor` of the degree-4 denominator (number-
field factoring over a parametric field — the current blocker, hangs before RT) →
RT resultant (now fixed) → resultant factoring / root-finding. The real
architectural fix is to integrate `∫rational/√quadratic` *directly* (a low-degree
reduction to ArcTan/ArcTanh) instead of rationalising it to a degree-4 rational
and invoking full Rothstein–Trager — which would sidestep the whole chain.

## FLINT parametric radical factoring; rational-integrator layer sweep (2026-07-01)

**`Factor` / `FactorSquareFree` over a parametric radical field `Q(t..)(√k)`**
(`flint_parametric_sqrt_factor[_squarefree]`, `src/poly/flint_bridge.c`; hooked
into `builtin_factor` and `builtin_factorsquarefree`). Unlike the constant-
radicand number-field case (where FLINT's `gr` layer is `GR_UNABLE`), the
parametric case collapses to the rational function field `Q(t.., √k)`, so
factoring reduces to ordinary **multivariate factoring over Q**
(`fmpq_mpoly_factor`) via `√k → S, k → S²`, readback `S → √k`. Fires only when a
`Sqrt[symbol]` is present (matching WL's default of factoring over the field the
input's radicals generate); no regression on plain / single-generator / tower
Factor. `Factor[u² − (a+b)√k u + a b k] = (a√k − u)(b√k − u)`.

**Rational-integrator layer sweep** — replacing the polynomial-algebra routines
that hang over `Q(a,b,k)(√k)` with FLINT, one integrator layer at a time. Now
FLINT-backed for the parametric regime: `PolynomialGCD`, `Cancel`/`Together`
(incl. the 2-arg Extension forms), `Resultant`, `Factor`/`FactorSquareFree`. On
the Goursat inner integral the hang has moved forward accordingly: past
`canonic`, past `Apart`'s `Factor`, into the **partial-fraction linear solve**
(`RowReduce` over the `√k` field, `parfrac.c`) — Gaussian elimination whose
symbolic entries blow up. That layer is linear-algebra-over-a-function-field
(not a polynomial-GCD-family op), so it needs a different treatment (Heaviside
residue formula for distinct linear factors, or a fraction-free solve).

**Tests:** `test_parametric_factor` in `tests/test_flint_bridge.c`. Regressions
green (poly/rat/intrat/qafactor/facpoly/extension/radical/integrate_goursat);
valgrind-clean; pre-existing `simplify_tests` (3) and `solve_tests` (1, nested
constant radical) soft-fails unchanged.

## Apart Bézout partial fraction over radical fields; rational-function-field path confirmed (2026-07-01)

**`Apart` over a radical field no longer RowReduces.** The classical partial-
fraction solve builds an S×(S+1) matrix and `RowReduce`s it; over `Sqrt[k]`-laden
coefficients that symbolic Gaussian elimination blows up (measured: a 5×6 matrix
hangs — and, confirmed, it hangs the same over an ordinary variable `S` too, so
it is Gaussian-elimination fraction growth, not the radical). Added a **Bézout
modular-inverse fast path** (`src/parfrac.c`): for a squarefree denominator with
distinct irreducible factors `p_i` (any degree — covers the irreducible quadratics
that yield `ArcTan`),
`R/D = Σ A_i/p_i`, `A_i = PolynomialRemainder[R·u, p_i] / g` where
`{g,{u,v}} = PolynomialExtendedGCD[D/p_i, p_i]`. Each step is a low-degree op over
the field, fast via the FLINT parametric GCD/Cancel. Gated on a symbolic radical
so plain-rational `Apart` keeps its `RowReduce` output. Now
`∫1/((x−√k)(x+√k))dx = −ArcTanh[x/√k]/√k` and similar close end-to-end;
`Apart[1/((x²+k)(x−√k)),x]` decomposes.

**Integrator layer status.** FLINT-fast for the parametric `Q(a,b,k)(√k)` regime
now at: `PolynomialGCD`, `Cancel`/`Together`, `Resultant`, `Factor`/`FactorSquareFree`,
and `Apart` (Bézout). The Goursat inner integral advances through all of these and
now stalls in `PolynomialExtendedGCD` of two degree-2 factors carrying the full
`k^(9/2)` coefficients — classical Euclidean xgcd over the radical field.

**Confirmed enabling API for the next layer.** `gr_ctx_init_fmpz_mpoly_q`
(FLINT's ring of multivariate rational functions `Q(a,b,S)`) with `gr_poly` over
it gives univariate polynomials over the rational-function field, and
`gr_poly_xgcd` / `gr_poly_gcd` succeed on it (probed). So a `gr_poly`-over-
`fmpz_mpoly_q` bridge is the systematic fix: it makes xgcd / gcd / divrem (and
resultant) all fast over `Q(params)(√k)[x]` in one unified component (via `√k→S`),
replacing the per-op `fmpq_mpoly` collapses — the next milestone toward closing
the Goursat integrals.

**Tests:** `test_apart_parametric_sqrt_k` (`tests/test_extension_auto_builtins.c`).
Regressions green (intrat/rat/poly/extension/integrate_goursat); valgrind-clean.

## fmpz_mpoly_q field bridge: xgcd/divrem over Q(params)(sqrt k) (2026-07-01)

**The systematic univariate-over-a-field bridge.** Represents Q(t..)(sqrt k)[x]
as `gr_poly` over FLINT's rational-function ring `fmpz_mpoly_q` (= Q(t.., S),
sqrt k -> S), with full `Expr <-> gr_poly<fmpz_mpoly_q>` conversion (incl.
`fmpz_mpoly -> Expr` readback and `S -> sqrt k` restore). `parametric_field_op`
dispatches `gr_poly_xgcd` / `gr_poly_divrem` / `gr_poly_gcd`
(`flint_parametric_field_xgcd` / `_divrem`, `src/poly/flint_bridge.c`). Hooked
into `builtin_polynomialextendedgcd`, `builtin_polynomialquotient`,
`builtin_polynomialremainder`. Gate allows rational-function coefficients
(`Power[.,-1]`) — the xgcd cofactors carry denominators (a new `allow_neg_pow`
flag on the detector).

The degree-2 `k^(3/2)` xgcd that hung under the classical Euclidean algorithm now
runs in **~0.002 s** with the Bézout identity verified. This was the last blocker
in the parametric rational integrator: **every algebra layer is now FLINT-fast**
over Q(a,b,k)(sqrt k) — PolynomialGCD, Cancel/Together, Resultant,
Factor/FactorSquareFree, Apart (Bézout), PolynomialExtendedGCD,
PolynomialQuotient/Remainder.

**Effect.** The Goursat *inner* integral `∫G/√quadratic` (which hung) now closes
with an ArcTan; typical parametric integrals close cleanly and fast:
`∫1/((x-√k)(x²+1))dx`, `∫(x+√k)/((x-√k)(x²+k))dx`,
`∫1/((x-a√k)(x-b√k))dx = -2 ArcTanh[…]` — all sub-second. The full Goursat
*named* integral advances through the whole descent (canonic → Apart →
IntRationalLogPart → LogToReal/LogToAtan) but does not yet finish under budget:
its V4-descent intermediate (a degree-4 rational with k^(1/2)…k^(9/2), a, b
coefficients from the QuadraticRadicals rationalisation) is enormous, so the
*cumulative* cost across the descent's many operations — not any single hanging
or slow primitive — exceeds the time limit. Remaining work is expression-swell /
operation-count reduction, not FLINT algebra.

**Tests:** `test_parametric_field_ops` (`tests/test_flint_bridge.c`): Bézout
identity for xgcd (incl. the degree-2 `k^(3/2)` case), divrem round-trip, plain-
rational bail. Regressions green (poly/intrat/rat/extension/simplify/solve —
pre-existing soft-fails only); valgrind-clean.

## fmpz_mpoly_q field-normalize: Plus-of-fractions Cancel/Together (2026-07-01)

`flint_parametric_field_normalize` (`src/poly/flint_bridge.c`) canonicalises a
whole scalar rational function over `Q(t..)(√k)` by converting it to an
`fmpz_mpoly_q` element (stored in lowest terms) and reading back num/den. Called
first in `flint_cancel_fraction`, so `Cancel`/`Together` of a **Plus of
fractions** now reduce fast — the num/den-extraction path saw `den = 1` on a sum
and dropped to the slow QA extension path. This was the LogToReal `A²+B²`
bottleneck in the Goursat inner integral:
`Together[b⁴/(b²+2ab√k+a²k)² + a⁴k²/(…)², Extension→Automatic]` went
**3.47 s → 0.0004 s**; the inner Goursat integral **~200 s → ~17.5 s**.
Tests + valgrind-clean; `GOURSAT_INTEGRAL_PLAN.md` records the remaining ~50×
(cumulative post-processing cost / the degree-doubling QuadraticRadicals
rationalisation).

## Docs: FLINT context section on the documentation site (2026-07-01)

Added a dedicated **FLINT context** section to the documentation website
(<https://stblake.github.io/mathilda/documentation/>) documenting all 13
`` FLINT` `` routines — the direct-access wrappers over the FLINT-backed kernels:

- Polynomial algebra: `` FLINT`PolynomialGCD ``, `` FLINT`Resultant ``,
  `` FLINT`Factor ``, `` FLINT`FactorSquareFree ``.
- Exact linear algebra: `` FLINT`Det ``, `` FLINT`Inverse ``,
  `` FLINT`LinearSolve ``, `` FLINT`RowReduce ``, `` FLINT`MatrixRank ``.
- Rigorous numerics: `` FLINT`Zeta ``, `` FLINT`HurwitzZeta ``,
  `` FLINT`PolyGamma ``, `` FLINT`StieltjesGamma ``.

New spec file `docs/spec/builtins/flint.md` documents each routine with worked
examples. `site/generate.py` gained a dedicated FLINT pass
(`discover_flint_builtins` / `parse_flint_spec` / `emit_flint_section`): the
backtick-qualified names are skipped by the ordinary `System`` discovery and,
unlike other builtins, register their docstring against a symbol constant rather
than a string literal, so the routine name is recovered from the leading
`` FLINT`Name `` token of the docstring text. Examples are re-verified against the
live binary exactly like the rest of the site; pages land under
`documentation/flint/` with their own index and nav, and the routines are added
to the landing page, alphabetical index, and `builtins.json`. Strict
`mkdocs build` passes.

## Docs: how to compile Mathilda with FLINT

Documented the optional FLINT (≥ 3.0) backend across the build docs so users know
how to enable the accelerated, rigorous algebraic-extension and `acb`-numeric
kernels:

- `README.md` — added FLINT to the prerequisites list, the optional-backends
  flag table (new `USE_FLINT` row), the Debian/Ubuntu, Fedora/RHEL, and Homebrew
  dependency-install blocks, and a note on the ≥ 3.0 (ANTIC) version floor and
  the automatic `USE_FLINT=0` fallback on older distros.
- `docs/building.md` — added `libflint-dev`/`brew install flint` to the
  Linux/macOS dependency blocks, a `USE_FLINT=0` row in the build-flags
  reference, and a "FLINT not detected" troubleshooting entry.
- `site/docs/index.md` — FLINT added to the "Build & run" dependency tabs plus a
  "Building with FLINT" tip explaining the auto-detection and fallback.
- `site/generate.py` + `documentation/flint/index.md` — the generated FLINT
  context page now opens with a "Building with FLINT" section (install commands,
  `USE_FLINT` flag, `pkg-config --modversion flint` check, and the not-registered
  fallback behaviour); the generator and the current page are kept in sync.

## Cancel/Together buffer-leak fixes + cube-root Goursat leak analysis (2026-07-03)

Fixed three instances of a single memory-leak bug class in `src/rat.c`: a
locally `malloc`'d `Expr**` array is passed to `expr_new_function`, which
*copies* the array (memcpy) and adopts the element pointers but not the buffer —
so the caller must `free` the buffer. Three sibling branches that build a
`Times`/`Plus` from a multi-element buffer forgot the `free` (while their
`n == 1` counterparts did it):

- `rat_symbolic_content` (the `n_running >= 2` return) — the dominant source; it
  fires on every `Cancel`-over-extension whose symbolic content has ≥2 algebraic
  factors (e.g. powers of `κ = (lead R)^(1/3)` and `ω` in the cube-root Goursat
  descent's `canonic`).
- `rat_strip_symbolic_common` (the `num_nf >= 2` branch).
- `rat_div_distribute` (the `Plus` branch's `return`).

All three are exercised by `Integrate[(2-(k+1)x)/((1-(k+1)x)(x(1-x)(1-kx))^(1/3)),
x, Method -> "GoursatAlgebraic"]`; after the fix these frames no longer appear in
any valgrind leak stack for that integral. Affected suites (`rat`, `intrat`,
`integrate_goursat`, `extension_*`) all pass; the integral still solves.

Also corrected a stale diagnosis: the previously-suspected "cube-root Goursat
leak" reproduced via an NDJSON pipe input with *unescaped* quotes
(`Method -> "GoursatAlgebraic"` instead of `\"..\"`), which truncates the `expr`
string mid-expression — the leak measured there was the parser's handling of the
malformed/truncated `->` input, not the integrator. The properly-escaped
integral has no parse-symbol leak. A larger, ASLR-nondeterministic per-call leak
remains in the shared algebraic-extension arithmetic (poly GCD/LCM/quotient and
`Expand` over `Q(α)`, reached from `together_recursive_ext`/`cancel_with_extension`/
`get_coeff`); it does not reproduce when the same sub-expressions are round-tripped
through print+reparse in isolation (the top-level evaluator normalises them first),
so it needs a C-level harness that drives the internal `canonic` on the un-normalised
in-memory trees — deferred.

## Associations: the `<| ... |>` data structure (2026-07-05)

Mathilda gains a first-class **Association** data structure modelled on the
Wolfram Language, with `<|key -> value, ...|>` literal syntax. Associations are
ordinary expressions (`Association[Rule[k,v], ...]`) with unique,
insertion-ordered keys (first occurrence fixes position, last fixes value), so
the whole generic toolchain (`Length`, `Map`, `ReplaceAll`, `===`, `FullForm`)
works on them unchanged. Bulk operations are backed by a transient
open-addressing hash index (keyed by `expr_hash`/`expr_eq`), giving amortised
`O(n)` construction, de-duplication, grouping and lookup instead of the naive
`O(n^2)`.

New builtins (all `Protected`; `AssociateTo` is `HoldFirst`), documented in
[`builtins/data-structures.md`](../builtins/data-structures.md):

- **`Association`** / `<|...|>` — construct/normalise (splices lists of rules and
  nested associations; de-duplicates keys).
- **`AssociationQ`**, **`Keys`**, **`Values`**.
- **`Lookup`** — single key (with optional default → `Missing["KeyAbsent", key]`),
  or a list of keys resolved with one index build (`O(n+m)`).
- **`KeyExistsQ`**, **`KeyDrop`**, **`KeyTake`**, **`KeyValueMap`**.
- **`AssociationThread`**, **`Counts`**, **`GroupBy`**, **`Merge`**.
- **`AssociateTo`** — in-place add/update on a held symbol.

Also: `assoc[[key]]` / `assoc[[Key[k]]]` / `assoc[[i]]` indexing via `Part`
(`src/part.c`); `Normal[assoc]` → list of rules (`src/calculus/series.c`);
`<|...|>` StandardForm, TeXForm and KaTeX (notebook) printing
(`src/print.c`, `src/print_latex.c`). Implementation in `src/assoc.c`, parser
support in `src/parse.c`. Covered by 41 end-to-end tests in
`tests/test_association.c` (0 leaks under macOS `leaks`).

## Associations: functional-toolchain threading + key operations (2026-07-05)

Second increment to the Association data structure (see previous entry).

- **`Map` and `Select` now thread over association values**, preserving keys —
  `Map[f, <|k -> v|>]` → `<|k -> f[v]|>` and `Select[assoc, pred]` filters by
  value (matching Wolfram). Implemented via `assoc_map_values` /
  `assoc_select_values` (`src/assoc.c`), dispatched from `builtin_map` /
  `builtin_select` (`src/funcprog.c`) for the default (level-1) case.
- **New key operations** (all `Protected`, `src/assoc.c`): `KeySort`,
  `KeySortBy` (stable), `KeyMap`, `KeySelect`.
- **New aggregation builtins**: `CountsBy[list, f]`, `PositionIndex[list]`
  (`<|value -> {positions}|>`, hash-indexed `O(n)`), and
  `AssociationMap[f, {keys}]`.

Documented in [`builtins/data-structures.md`](../builtins/data-structures.md).
Covered by 10 additional end-to-end tests in `tests/test_association.c`
(53 total; 0 leaks under macOS `leaks`; no regressions in the funcprog / list /
core / eval / regression suites).

## Associations: ordering & aggregation over values (2026-07-05)

Third increment to the Association data structure.

- **`Sort[assoc]`** now orders entries by their values (keys follow), and
  **`Total`, `Min`, `Max`** reduce over an association's values — matching
  Wolfram. Implemented via `assoc_sort_by_value` and a reusable
  `assoc_apply_over_values` helper (`src/assoc.c`) dispatched from
  `builtin_sort` (`src/sort.c`), `builtin_total` (`src/list/total.c`) and
  `builtin_min`/`builtin_max` (`src/list/minmax.c`).
- **`Join[assoc1, assoc2, ...]`** already merges associations (later value
  wins) for free via key de-duplication; now documented and tested.

Documented in [`builtins/data-structures.md`](../builtins/data-structures.md).
+5 end-to-end tests (58 total in `tests/test_association.c`); 0 leaks; no
regressions in the sort / list / core / eval / regression suites.

## Associations: Part assignment + Mean over values (2026-07-05)

Fourth increment to the Association data structure.

- **Part assignment** now works on associations: `a[[key]] = val` updates an
  existing key or appends a new `key -> val` entry; `a[[Key[k]]] = val` targets
  a key explicitly; `a[[i]] = val` updates the i-th value positionally; and
  multi-index `a[[k1, k2]] = val` descends into nested associations/lists.
  Implemented by extending `expr_part_assign_rec` (`src/part.c`) with an
  association branch; the read-modify-write idiom `a[[k]] = a[[k]] + 1` works.
- **`Mean[assoc]`** averages the association's values (via the shared
  `assoc_apply_over_values`, `src/stats.c`).

Documented in [`builtins/data-structures.md`](../builtins/data-structures.md).
+8 end-to-end tests (66 total in `tests/test_association.c`); 0 leaks; no
regressions in the list_set / stats / core / eval / regression / list suites.

## Associations: Cases / Count / DeleteCases over values (2026-07-05)

Fifth increment to the Association data structure.

- **`Cases[assoc, patt]`** and **`Count[assoc, patt]`** now match against the
  association's values (delegating to `Cases`/`Count` over `Values[assoc]` via
  the shared `assoc_apply_over_values`, `src/patterns.c`).
- **`DeleteCases[assoc, patt]`** removes the entries whose value matches the
  pattern, returning an association — new `assoc_delete_cases` (`src/assoc.c`,
  matching each value with `MatchQ`).

Documented in [`builtins/data-structures.md`](../builtins/data-structures.md).
+4 end-to-end tests (70 total in `tests/test_association.c`); 0 leaks; no
regressions in the patterns / core / eval / regression suites.

## Associations: AllTrue/AnyTrue/NoneTrue + MemberQ over values (2026-07-05)

Sixth increment to the Association data structure.

- **New `AllTrue`, `AnyTrue`, `NoneTrue`** builtins (`src/funcprog.c`, all
  `Protected`) — general predicate tests over a list's elements, short-circuiting
  and left unevaluated when a test result is neither True nor False. Over an
  association they test the values (via `assoc_apply_over_values`).
- **`MemberQ[assoc, form]`** now tests the association's values (`src/patterns.c`).

Documented in [`builtins/functional-programming.md`](../builtins/functional-programming.md)
and the value-threading note in [`builtins/data-structures.md`](../builtins/data-structures.md).
+6 end-to-end tests (82 total in `tests/test_association.c`); 0 leaks; no
regressions in funcprog / patterns / core / eval / regression / list suites.

## SortBy builtin; sorts associations by value-key (2026-07-05)

Seventh increment.

- **New `SortBy`** builtin (`src/sort.c`, `Protected`): `SortBy[list, f]` sorts a
  list by the canonical order of `f[element]` (key evaluated once per element
  via a paired-key qsort); `SortBy[assoc, f]` sorts an association by `f` of each
  value (keys follow); `SortBy[f]` is the operator form. Benefits plain lists as
  well as associations.

Documented in [`builtins/functional-programming.md`](../builtins/functional-programming.md)
and [`builtins/data-structures.md`](../builtins/data-structures.md). +6 e2e tests
(88 total in `tests/test_association.c`); 0 leaks; no regressions in
sort / list / core / eval / regression suites.

## MaximalBy / MinimalBy builtins (2026-07-05)

Eighth increment.

- **New `MaximalBy` / `MinimalBy`** builtins (`src/sort.c`, `Protected`):
  `MaximalBy[list, f]` gives the element(s) maximising `f` (all ties, in order);
  over an association it returns the entries whose value maximises `f` (as an
  association). `MaximalBy[f]` is the operator form; `MinimalBy` is symmetric.
  Symbols `SYM_MaximalBy`/`SYM_MinimalBy` added.

Documented in [`builtins/functional-programming.md`](../builtins/functional-programming.md)
and [`builtins/data-structures.md`](../builtins/data-structures.md). +5 e2e tests
(93 total in `tests/test_association.c`); 0 leaks; no regressions in
sort / core / eval / regression suites.

## TakeLargest / TakeSmallest (+By) builtins (2026-07-05)

Ninth increment.

- **New `TakeLargest`, `TakeSmallest`, `TakeLargestBy`, `TakeSmallestBy`**
  builtins (`src/sort.c`, `Protected`): the `n` ranked-extreme elements of a
  list (descending / ascending), or, over an association, the `n` entries with
  the largest / smallest values (or `f` of value), returned as an association.
  `n` beyond the length returns all elements, still ranked. Shared `take_extreme`
  helper reuses the SortBy paired-key machinery.

Documented in [`builtins/functional-programming.md`](../builtins/functional-programming.md)
and [`builtins/data-structures.md`](../builtins/data-structures.md). +7 e2e tests
(100 total in `tests/test_association.c`); 0 leaks; no regressions in
sort / core / eval / regression suites.

## GroupBy reducer form + GatherBy (2026-07-05)

Tenth increment.

- **`GroupBy[list, f, g]`** — the three-argument split-apply-combine form now
  applies a reducer `g` to each group, giving `<|f[x] -> g[{group}], ...|>`
  (e.g. `GroupBy[Range[10], EvenQ, Total]` -> `<|False -> 25, True -> 30|>`).
  `src/assoc.c`, extending `builtin_groupby`.
- **New `GatherBy[list, f]`** — gathers elements with equal `f[element]` into a
  list of sublists (first-appearance order), hash-indexed `O(n)`.

Documented in [`builtins/data-structures.md`](../builtins/data-structures.md).
+5 e2e tests (105 total in `tests/test_association.c`); 0 leaks; no regressions
in core / eval / regression suites.

## ReverseSort/ReverseSortBy + a pipeline showcase (2026-07-05)

Eleventh increment.

- **New `ReverseSort` / `ReverseSortBy`** builtins (`src/sort.c`, `Protected`):
  descending Sort / SortBy, thin wrappers that reverse the ascending result;
  over an association they sort entries by value (or `f` of value), descending.
- **New showcase** `examples/association_showcase.m` + `association-showcase.md`:
  a 100k-record split-apply-combine pipeline (`Counts`, `GroupBy` + reducer,
  `ReverseSort`, `TakeLargest`, `Mean` per group) with measured timings,
  demonstrating the toolchain end-to-end at scale.

Documented in [`builtins/functional-programming.md`](../builtins/functional-programming.md).
+4 e2e tests (109 total in `tests/test_association.c`); 0 leaks; no regressions
in sort / core / regression suites.

## SelectFirst / FirstCase builtins (2026-07-05)

Twelfth increment.

- **New `SelectFirst`** (`src/funcprog.c`) — first element satisfying a
  predicate, short-circuiting; `Missing["NotFound"]` or a supplied default when
  none match. Over an association tests the values.
- **New `FirstCase`** (`src/patterns.c`) — first element matching a pattern,
  reusing `Cases` (so it inherits the pattern and association-value semantics);
  `Missing["NotFound"]` or a default when none match.

Documented in [`builtins/functional-programming.md`](../builtins/functional-programming.md).
+8 e2e tests (117 total in `tests/test_association.c`); 0 leaks; no regressions
in patterns / funcprog / core / eval / regression suites.

## DeleteMissing builtin (2026-07-05)

Thirteenth increment.

- **New `DeleteMissing[expr]`** (`src/patterns.c`) — removes all `Missing[...]`
  elements (equivalent to `DeleteCases[expr, _Missing]`, which it delegates to,
  so it inherits list and association-value handling). The natural cleanup after
  a multi-key `Lookup`; over an association it drops entries whose value is
  `Missing[...]`.

Documented in [`builtins/data-structures.md`](../builtins/data-structures.md).
+4 e2e tests (121 total in `tests/test_association.c`); 0 leaks; no regressions
in patterns / core / eval / regression suites.

## Association integration tests + design notes (2026-07-05)

Fourteenth increment — hardening (no new builtins).

- Verified the whole toolchain composes correctly and that empty-collection
  reductions over an association match the underlying list behaviour
  (`Total[<||>]` behaves as `Total[{}]`, `Min[<||>]` as `Min[{}]`, etc.).
- Added multi-builtin **integration tests** (realistic pipelines: word-frequency
  top-N, group-reduce-rank, Lookup->DeleteMissing->Total, Merge of Counts) and
  empty-collection edge tests (129 total in `tests/test_association.c`).
- Documented the **value-threading design principle** in
  [`builtins/data-structures.md`](../builtins/data-structures.md).

## KeyValuePattern; Append/Prepend on associations (2026-07-05)

Fifteenth increment.

- **New `KeyValuePattern`** (`src/match.c`) — a pattern head that matches an
  association (or list of rules) containing given keys with matching values;
  value patterns bind, so associations can be destructured in rules and used to
  filter records (`Cases[records, KeyValuePattern[{"t" -> _}]]`). Implemented as
  a self-contained branch in the matcher (a new pattern head, so existing
  matching is unaffected); registered Protected with a docstring.
- Documented that **`Append` / `Prepend`** already extend associations (update
  or add an entry, preserving order) — now covered by tests.

Documented in [`builtins/data-structures.md`](../builtins/data-structures.md).
+11 e2e tests (140 total in `tests/test_association.c`); 0 leaks; no regressions
in match / match_extensive / patterns / replace / core / eval / regression suites.

## Association as accessor: assoc[key] (2026-07-05)

Sixteenth increment.

- **`assoc[key]`** — applying an association as a function now looks the key up,
  giving the value or `Missing["KeyAbsent", key]`; `assoc[Key[k]]` is the explicit
  form. This is the idiomatic Wolfram accessor and makes `Map[#[key] &, records]`
  work over a list of associations. Implemented as a compound-head application
  branch in the evaluator (`src/eval.c`), alongside pure-function application.

Documented in [`builtins/data-structures.md`](../builtins/data-structures.md).
+5 e2e tests (145 total in `tests/test_association.c`); 0 leaks; no regressions
in eval / purefunc / funcprog / match / core / regression suites.

Note: `KeyValuePattern` works at top level (MatchQ / Cases / Replace / Select);
using it as a *function-argument* pattern in a DownValue LHS is not yet matched
(a pre-existing matcher trait it shares with `Except` in that position).

## Nested/multi-key accessor: assoc[k1, k2] (2026-07-05)

Seventeenth increment.

- Extended the association accessor (`src/eval.c`) to **multi-key nested lookup**:
  `assoc[k1, k2, ...]` looks up k1 and applies the result to the remaining keys,
  so `<|"a" -> <|"b" -> 5|>|>["a", "b"]` gives 5 and `tab["row", "col"]` reads a
  cell of an association-of-associations. A missing intermediate key propagates
  `Missing["KeyAbsent", ...]`.

Documented in [`builtins/data-structures.md`](../builtins/data-structures.md).
+4 e2e tests (149 total in `tests/test_association.c`); 0 leaks; no regressions
in eval / purefunc / core / regression suites.

## Fix: KeyValuePattern (and Except) in DownValue LHSs (2026-07-05)

Eighteenth increment — a matcher/dispatch fix.

- `f[KeyValuePattern[{...}]] := ...` now matches: associations can be
  destructured directly in a function definition (e.g.
  `area[KeyValuePattern[{"w" -> w_, "h" -> h_}]] := w h`). The same fix also
  repairs `Except` in a DownValue LHS.
- Root cause: the §3.5 DownValue dispatch filter (`src/symtab.c`,
  `pattern_arg_head_canon`) treated a first-argument pattern head of
  `KeyValuePattern`/`Except` as a literal head, so it skipped the rule when the
  input's first-arg head differed (e.g. `Association`). These heads are now
  treated as wildcards (return NULL), so the filter never skips them — strictly
  more conservative, so it cannot cause missed matches elsewhere. (Matching
  itself always worked via MatchQ/Cases; only the DownValue fast-path filter was
  wrong.)

+4 e2e tests (153 total in `tests/test_association.c`); 0 leaks; no regressions
in symtab / match / match_extensive / patterns / replace / eval / core /
regression suites.

## GroupBy key -> value transform form (2026-07-05)

Nineteenth increment.

- **`GroupBy[list, keyfn -> valfn]`** groups by `keyfn[x]` but collects `valfn[x]`
  in each group; **`GroupBy[list, keyfn -> valfn, g]`** then reduces each group by
  `g` — completing the Wolfram GroupBy signature and expressing the whole
  group / extract-field / reduce pipeline in a single call (e.g.
  `GroupBy[txns, First -> Last, Total]`). `src/assoc.c`, extending `builtin_groupby`.

Documented in [`builtins/data-structures.md`](../builtins/data-structures.md).
+3 e2e tests (156 total in `tests/test_association.c`); 0 leaks; no regressions
in core / eval / regression suites.

## SortBy multi-criteria (tie-breaking) keys (2026-07-05)

Twentieth increment.

- **`SortBy[list, {f1, f2, ...}]`** sorts by `f1`, breaking ties with `f2`, etc.
  Implemented by building each element's sort key as the tuple `{f1[e], ...}`;
  `expr_compare` already orders equal-length lists lexicographically, so this is
  exact multi-criteria ordering (`src/sort.c`). Also applies to associations
  (by value). Single-criterion and operator forms are unchanged.

Documented in [`builtins/functional-programming.md`](../builtins/functional-programming.md).
+2 e2e tests (158 total in `tests/test_association.c`); 0 leaks; no regressions
in sort / core / regression suites.

## Fold/FoldList over values; new Scan (2026-07-05)

Twenty-first increment.

- **`Fold` / `FoldList`** now fold over an association's values in key order
  (`Fold[Plus, 0, <|"a" -> 1, "b" -> 2|>]` -> 3), by rebuilding the call over
  `Values[assoc]` (`src/funcprog.c`, `fold_impl`).
- **New `Scan[f, expr]`** (`src/funcprog.c`, Protected) — applies f to each
  element for side effects, returning Null; over an association it scans the
  values.

Documented in [`builtins/functional-programming.md`](../builtins/functional-programming.md).
+5 e2e tests (163 total in `tests/test_association.c`); 0 leaks; no regressions
in fold / foldlist / core / eval / regression suites.

## First/Last give the value on associations (2026-07-05)

Twenty-second increment.

- **`First` / `Last`** on an association now return the first / last *value*
  (matching Wolfram), rather than the whole `key -> value` rule (`src/part.c`).
- Confirmed and covered `Rest` / `Most` / `Take` / `Drop`, which already slice
  entries and return an association (order preserved).

Documented in [`builtins/data-structures.md`](../builtins/data-structures.md).
+6 e2e tests (169 total in `tests/test_association.c`); 0 leaks; no regressions
in core / eval / regression / list suites.

## KeyMemberQ / KeyFreeQ (2026-07-05)

Twenty-third increment.

- **New `KeyMemberQ`** (== `KeyExistsQ`) and **`KeyFreeQ`** (its complement,
  True when the key is absent), `src/assoc.c`, both Protected. Round out the
  key-presence predicates.
- Confirmed and tested `Reverse[assoc]` (reverses key order).

Documented in [`builtins/data-structures.md`](../builtins/data-structures.md).
+3 e2e tests (174 total in `tests/test_association.c`); 0 leaks; no regressions
in core / regression suites.

## Association reference overview + capstone tests (2026-07-05)

Twenty-fourth increment — documentation and test hardening (no new builtins).

- Added a **"toolchain at a glance"** table to
  [`builtins/data-structures.md`](../builtins/data-structures.md) grouping the
  full ~65-function association surface by category (Construct / Access / Write /
  Keys & values / Presence / Transform / Aggregate / Order-rank / Iterate-reduce /
  Structural / Patterns) — a scannable map of the now-extensive reference.
  Verified every listed operation is a registered builtin (or matcher pattern head).
- Added capstone end-to-end tests chaining construct -> group+reduce -> order ->
  access in a single expression (176 total in `tests/test_association.c`).

## Position on associations returns {Key[k]} (2026-07-05)

Twenty-fifth increment.

- **`Position[assoc, patt]`** now reports the positions of matching *values* as
  `{Key[k]}` (Wolfram semantics), rather than raw `{ruleIndex, valueSlot}`
  indices. The `{Key[k]}` positions round-trip through `Part`/`Extract`.
  Implemented as a contained branch in `builtin_position` (`src/patterns.c`)
  using the matcher directly; list/general Position is unchanged.

Documented in [`builtins/data-structures.md`](../builtins/data-structures.md).
+3 e2e tests (179 total in `tests/test_association.c`); 0 leaks; no regressions
in patterns / core / regression suites.

## Loadable Associations example library (2026-07-05)

Twenty-sixth increment — examples (no source change).

- Added **`examples/associations.lb`**, a loadable four-notebook tour of the
  association toolchain (Basics, Aggregation, Transform & rank, Patterns &
  pipelines; 51 cells) that opens in the Mathilda app. All expressions verified
  against the binary. Linked from `examples/association-showcase.md`.

## Code-review fixes (2026-07-05)

Twenty-seventh increment — correctness/polish from a code review of the branch.

- **Part assignment on associations** with `All` / `Span` / a key-or-index list
  now assigns into the targeted entry *values* (`a[[All]] = v`, `a[[i;;j]] = v`,
  `a[[{k1,...}]] = v`) instead of appending a bogus `All -> v` / `Span[...] -> v`
  / `{...} -> v` key (`src/part.c`).
- **`Select[assoc, pred, n]`** (three-argument form) now filters the association's
  values and keeps the first `n`, instead of testing `Rule` nodes (`src/funcprog.c`,
  `assoc_select_values` gained a count).
- **`Sort[assoc]`** is now a stable sort — equal values keep their input order
  (tie-break by original position, not by key; `src/assoc.c`).
- **`Lookup`** accepts a bare list of rules (like `Keys`/`Values`); `assoc_scan`
  now skips non-rule entries defensively (`src/assoc.c`).
- Convention: replaced a runtime `intern_symbol("MatchQ")` with a cached
  `SYM_MatchQ` (`src/sym_names.{c,h}`), per CLAUDE.md ("internal symbols defined
  in sym_names.c").

+6 regression tests (185 total in `tests/test_association.c`); 0 leaks; no
regressions in association / funcprog / patterns / list_set / sort / match /
core / eval / regression suites.

## Position descends into association values (2026-07-05)

Twenty-eighth increment.

- **`Position[assoc, patt]`** now descends into values, returning
  `{Key[k], subpos...}` positions (e.g. `Position[<|"a" -> {1, 2}|>, 1]` gives
  `{{Key["a"], 1}}`), and these round-trip through `Part`/`Extract`. Reimplemented
  by delegating to `Position` over `Values[assoc]` and remapping the leading
  value-index to `Key[key]` (`src/patterns.c`) — reusing the existing
  level-descent machinery instead of a shallow whole-value match.

+4 e2e tests (189 total in `tests/test_association.c`); 0 leaks; no regressions
in patterns / core / regression suites.

## Iterating an association in Table/Do/Sum/Product (2026-07-05)

Twenty-ninth increment.

- Iterator specs of the form `{var, assoc}` now enumerate the association's
  **values** (e.g. `Table[v^2, {v, <|"a" -> 2, "b" -> 3|>}]` -> `{4, 9}`),
  so `Table`, `Do`, `Sum` and `Product` all work over associations. One change
  in the shared iterator-spec parser (`src/iter.c`) covers every
  iterator-driven builtin.

+3 e2e tests (192 total in `tests/test_association.c`); 0 leaks; no regressions
in iter / core / eval / regression suites.

## assoc[[{k1,...}]] gives the list of values (2026-07-05)

Thirtieth increment.

- `assoc[[{k1, k2, ...}]]` (Part with a list index) now returns
  `{assoc[[k1]], assoc[[k2]], ...}` — a list of the looked-up values (Missing for
  absent keys), mirroring list `Part` and `Lookup[assoc, {keys}]`; further indices
  apply per element (`assoc[[{k1,k2}, 2]]`). The `expr_part` association branch was
  refactored to a single-index helper (`assoc_part_single`) mapped over the list
  index (`src/part.c`).

+3 e2e tests (195 total in `tests/test_association.c`); 0 leaks; no regressions
in core / eval / regression / list_set suites.

## Implicit multiplication with association literals (2026-07-05)

Thirty-first increment.

- `2 <|"a" -> 1|>` and similar now parse as `Times[2, <|...|>]` — an association
  literal is admitted as a right operand of implicit multiplication (the parser's
  implicit-Times check now recognises a leading `<|`, `src/parse.c`). Comparisons
  (`a < b`, `2 < 3`) are unaffected since only the exact `<|` pair triggers it.

+1 e2e test (196 total in `tests/test_association.c`); 0 leaks; no regressions
in parse / comparisons / core / eval / regression suites.

## Full-suite verification; key predicates accept rule lists (2026-07-05)

Thirty-second increment.

- **Verification:** built and ran the project test suites; every association-
  relevant and core suite passes (parser, evaluator, matcher, symtab, part, sort,
  iter, funcprog, patterns, comparisons, stats, list, list_set, regression, ...).
  The only failures on this machine are `moebiusmu`/`primenu` large-number tests,
  which need ECM (disabled here) and fail identically on `origin/main` — the
  association branch touches no number-theory code.
- **`KeyExistsQ` / `KeyMemberQ` / `KeyFreeQ`** now also accept a bare list of
  rules (consistent with `Lookup`/`Keys`/`Values`); `assoc_scan` already skips
  non-rule entries (`src/assoc.c`).

+1 e2e test (199 total in `tests/test_association.c`); 0 leaks.

## KeyValuePattern: backtracking + composes with Condition (2026-07-05)

Thirty-third increment — matcher completeness for KeyValuePattern.

- **Backtracking:** KeyValuePattern requirements are now matched with
  backtracking (`kvp_match_reqs`, `src/match.c`), so a consistent assignment is
  found whenever one exists even when requirements share a bound variable
  (`MatchQ[<|x -> y, y -> 1|>, KeyValuePattern[{k_ -> _, _ -> k_}]]` is now True).
- **Condition:** `KeyValuePattern[...] /; cond` now matches — `KeyValuePattern`
  (and `Except`) were added to the Condition handler's pattern-wrapper list so
  `Condition` recurses into them instead of matching their head literally.
  Enables `Cases[records, KeyValuePattern[{"p" -> v_}] /; v > 5 :> v]`.

+3 e2e tests (204 total in `tests/test_association.c`); 0 leaks; no regressions
in match / match_extensive / patterns / replace / core / regression suites.

## MapAt into associations (2026-07-05)

Thirty-fourth increment.

- **`MapAt[f, assoc, pos]`** now applies `f` to the value at a key
  (`"k"` / `Key[k]`), a positional index, or a nested position
  (`{Key[k1], Key[k2]}`) — composing with the `{Key[k]}` positions that
  `Position` returns (`MapAt[f, a, First[Position[a, patt]]]`). Implemented by
  making the shared `mapat_at_path` walker association-aware (`src/funcprog.c`);
  lists and general expressions are unchanged.

+5 e2e tests (209 total in `tests/test_association.c`); 0 leaks; no regressions
in mapat / funcprog / core / regression suites.

## Delete by Key position on associations (2026-07-05)

Thirty-fifth increment.

- **`Delete[assoc, {Key[k]}]`** removes an entry by key position; a nested
  position `{Key[k1], Key[k2]}` descends into inner associations, and
  `{{Key[k1]}, {Key[k2]}}` removes several (`src/part.c`, `delete_path` gained an
  association-key branch). Completes the position-based toolset
  (`Extract`/`Position`/`MapAt`/`Delete`) for `Key[...]` positions.
- Fixed a latent position-spec ambiguity in `expr_delete`: a `List` position is
  "multiple paths" only when every element is itself a `List`, so a `Key[k]`
  step is treated as part of a single path (`{Key["a"], Key["x"]}` is one nested
  path, not two). Integer paths are unaffected.

+4 e2e tests (213 total in `tests/test_association.c`); 0 leaks; no regressions
in expr (Part/Delete/ReplacePart) / core / eval / regression / list suites.

## ReplacePart by Key position on associations (2026-07-05)

Thirty-sixth increment.

- **`ReplacePart[assoc, {Key[k]} -> v]`** (or bare `Key[k]`, or positional `i`)
  replaces the value at that key; multiple positions are supported. Replace-only,
  matching Wolfram — absent keys are left unchanged (`src/replace.c`, a
  self-contained association branch in `builtin_replace_part`). This completes
  the position-based toolset for `Key[...]` positions:
  `Extract` / `Position` / `MapAt` / `Delete` / `ReplacePart`.

+5 e2e tests (218 total in `tests/test_association.c`); 0 leaks; no regressions
in expr (Part/Delete/ReplacePart) / replace / core / regression suites.

## Showcase the position ops; fix a MapAt doc example (2026-07-05)

Thirty-seventh increment — examples/docs (no source change).

- Added a fifth notebook ("Positions & edits") to `examples/associations.lb`
  showcasing `Position` -> `Extract`/`MapAt`/`Delete`/`ReplacePart` on a small
  inventory association, and a matching frontend demo cell.
- Fixed a `MapAt` doc example that used `Minus` (not a Mathilda builtin, so it
  stayed `Minus[9]` unevaluated); replaced with the pure function `-# &`, which
  the documented `-9` output now actually matches.

## Doc-integrity check; Merge accepts an association of associations (2026-07-05)

Thirty-eighth increment.

- **Verified** every documented In[]:=/Out[]= example in the association spec
  pages against the binary: `data-structures.md` is 100% correct, and all
  association examples in `functional-programming.md` pass. (The remaining
  mismatches there are pre-existing, unrelated examples — Distribute term order,
  InterpolatingFunction `...` placeholders, `%` history, `6.` vs `6.0`.)
- **`Merge`** now accepts an association *of* associations (not just a list),
  merging its values — `Merge[<|"g1" -> a1, "g2" -> a2|>, f]` (`src/assoc.c`,
  delegates to the list form via `Merge[Values[col], f]`).

+1 e2e test (219 total in `tests/test_association.c`); 0 leaks; no regressions
in core / regression suites.

## Counts/CountsBy over association values (2026-07-05)

Thirty-ninth increment.

- **`Counts[assoc]`** tallies the association's values, and
  **`CountsBy[assoc, f]`** tallies them by `f` — consistent with the
  value-threading principle (`src/assoc.c`, delegating via
  `assoc_apply_over_values`). Lists are unchanged.

+2 e2e tests (221 total in `tests/test_association.c`); 0 leaks; no regressions
in core / regression suites.

## DeleteDuplicates over association values (2026-07-05)

Fortieth increment.

- **`DeleteDuplicates[assoc]`** keeps the first entry for each distinct value
  and returns an association (Wolfram semantics). Implemented as a hash-indexed
  `O(n)` scan (`assoc_delete_duplicate_values` in `src/assoc.c`, dispatched from
  `builtin_deleteduplicates`); the seen values are indexed in a transient hash
  table rather than compared pairwise. A custom equivalence test still falls
  through to the generic list path. Lists are unchanged.

+1 e2e test group (`test_deleteduplicates_over_values`); 0 leaks; no regressions
in core / regression suites. Docs (`data-structures.md`) and the loadable
`examples/associations.lb` tour updated.

## Statistics & Tally over association values (2026-07-05)

Forty-first increment.

- **`Median`, `Variance`, `StandardDeviation`** now reduce over an
  association's values (joining `Mean`/`Total`/`Min`/`Max`), and **`Tally`** /
  **`Commonest`** tally the values (siblings of `Counts` / `CountsBy`). All are
  the established value-threading delegation via `assoc_apply_over_values`
  (`src/stats.c`, `src/list/setops.c`); lists are unchanged.

+2 e2e test groups in `tests/test_association.c`; 0 leaks; no regressions in
core / regression suites. Docs (`data-structures.md`) and the loadable
`examples/associations.lb` tour updated.

## KeyUnion (2026-07-05)

Forty-second increment.

- **`KeyUnion[{assoc1, assoc2, ...}]`** — new builtin that pads every
  association to the union of all their keys (first-appearance order), filling
  a key absent from an association with `Missing["KeyAbsent", key]`. Returns the
  list of equalised associations, ready for row-wise/tabular processing. The
  union and each rebuild are hash-indexed (`builtin_keyunion` in `src/assoc.c`):
  one `O(sum of sizes)` pass to collect distinct keys, then each association
  rebuilt against a hash index of its own keys — linear rather than
  `O(keys * entries)`. Registered `Protected`, with docstring; `SYM_KeyUnion`
  added to `sym_names.{c,h}`.

+1 e2e test group (`test_keyunion`) in `tests/test_association.c`; 0 leaks; no
regressions in core / regression suites. Docs (`data-structures.md`) and the
loadable `examples/associations.lb` tour updated.

## Accumulate / Differences over association values (2026-07-05)

Forty-third increment.

- **`Accumulate[assoc]`** now gives the running totals of the values with every
  key retained, and **`Differences[assoc]`** the successive value differences
  keyed by the trailing key of each pair (leading key drops, `n` → `n-1`).
  Previously both mis-fired, treating the `Rule` nodes as arithmetic operands.
- New shared helper **`assoc_rekey_over_values`** (`src/assoc.c`): rewrites
  `head[assoc, rest...]` as `head[Values[assoc], rest...]`, evaluates, then
  re-pairs the resulting values with the *trailing* keys — so a dropped leading
  value drops the leading key, keeping alignment general. This is the
  shape-preserving counterpart to `assoc_apply_over_values` (which collapses).

+2 e2e test groups in `tests/test_association.c`; 0 leaks; no regressions in
core / regression suites (list Accumulate/Differences, incl. the Kahan
`CompensatedSummation` path, unchanged). Docs (`data-structures.md`) and the
loadable `examples/associations.lb` tour updated.

## FoldList over associations keeps keys (2026-07-05)

Forty-fourth increment.

- **`FoldList[f, assoc]`** now keeps the association shape, pairing each key
  with its running result (`n -> n`) — the general form of `Accumulate[assoc]`.
  It previously returned a bare list of the running values. `Fold[f, assoc]`
  still collapses to a scalar.
- Refactored the windowed-rekey path: extracted **`assoc_rekey_from_list`**
  (re-pairs a values List with an association's trailing keys) out of
  `assoc_rekey_over_values`, so it is reusable when the collection is not the
  first argument. `fold_impl` (`src/funcprog.c`) uses it for the 2-arg
  `FoldList`; `Accumulate` / `Differences` keep using `assoc_rekey_over_values`,
  now layered on the shared helper.

+1 e2e test group (`test_foldlist_over_association_values`); 0 leaks; no
regressions in core / regression suites (list FoldList/Fold, incl. the seeded
3-arg form, unchanged). Docs (`data-structures.md`) and the loadable
`examples/associations.lb` tour updated.

## TakeWhile / LengthWhile (2026-07-05)

Forty-fifth increment.

- New builtins **`TakeWhile[list, crit]`** (the longest leading run of elements
  `e` with `crit[e] === True`, same head as the input) and
  **`LengthWhile[list, crit]`** (the length of that run). Over an association
  the criterion tests the values: `TakeWhile` keeps the matching leading entries
  as an association (keys preserved), `LengthWhile` counts them. Both share a
  `leading_run_length` helper in `src/funcprog.c` and are registered `Protected`
  with docstrings in `src/core.c`.

+1 e2e test group (`test_takewhile_lengthwhile`, list + association + empty +
all-pass + first-fail); 0 leaks; no regressions in core / regression suites.
Docs (functional-programming + data-structures) and the loadable
`examples/associations.lb` tour updated.

## MinMax + Min/Max single-element fix (2026-07-05)

Forty-sixth increment.

- New builtin **`MinMax[list]`** giving `{Min[list], Max[list]}` (the value
  range, e.g. for plot bounds); over an association it uses the values. It
  delegates to `Min` / `Max` (`builtin_minmax` in `src/list/minmax.c`),
  inheriting all their numeric handling. Registered `Protected` with a docstring.
- Root-cause fix in **`Min` / `Max`**: a single element now reduces to itself
  (`Min[5]` -> `5`, `Min[{5}]` -> `5`, `Max[x]` -> `x`); previously a lone
  argument was left unevaluated. Surfaced by `MinMax[{singleton}]`. Multi-arg,
  symbolic-mixed, empty (`Infinity` / `-Infinity`), and list-flattening behaviour
  are unchanged.

+1 e2e test group (`test_minmax`, list + association + single-element Min/Max);
0 leaks; full suite green apart from the two pre-existing FLINT-disabled
number-theory suites (unrelated — no Min/Max dependency). Docs
(structural-manipulation + data-structures) and the loadable
`examples/associations.lb` tour updated.

## Catenate (2026-07-05)

Forty-seventh increment.

- New builtin **`Catenate[{e1, e2, ...}]`** — takes a single list and flattens
  one level: the `ei` must share a head, and their elements are concatenated
  under it (`{{1,2},{3,4}}` -> `{1,2,3,4}`; `{f[1,2],f[3]}` -> `f[1,2,3]`). A
  list of associations merges into one association with later keys winning (via
  `assoc_from_rules`), so `Catenate` composes with `GroupBy` / `Merge` pipelines
  that produce a list of associations. `builtin_catenate` in `src/list/join.c`,
  registered `Protected` with a docstring; mixed heads are left unevaluated.

+1 e2e test group (`test_catenate`, lists + custom head + association merge +
empty + GroupBy pipeline); 0 leaks; no regressions in core / regression / list
suites. Docs (`structural-manipulation.md`) and the loadable
`examples/associations.lb` tour updated.

## MapIndexed (2026-07-05)

Forty-eighth increment.

- New builtin **`MapIndexed[f, list]`** giving `{f[e1, {1}], f[e2, {2}], ...}`
  (the second argument is the position `{i}`). Over an association it applies
  `f[value, {Key[k]}]` keeping keys — the `{Key[k]}` position is the same shape
  `Position` reports for associations, so the two compose. The `f[...]`
  applications are left for the evaluator to reduce (matching `Map`).
  `builtin_mapindexed` in `src/funcprog.c`, registered `Protected` with a
  docstring.

+1 e2e test group (`test_mapindexed`, list + association + #1/#2 slots + empty);
0 leaks; no regressions in core / regression suites. Docs
(functional-programming) and the loadable `examples/associations.lb` tour
updated.

## Lookup over a list of associations (2026-07-05)

Forty-ninth increment.

- **`Lookup[{a1, a2, ...}, key]`** now threads over a list of associations,
  extracting the key from each — the natural way to pull one field out of a
  column of records. A key-list and a default thread through unchanged (each
  element is delegated to `Lookup`). This is distinct from the rule-list form
  (whose elements are `Rule`s, not associations), which is unchanged; an empty
  list still behaves as before. `builtin_lookup` in `src/assoc.c`.

+1 e2e test group (`test_lookup_over_list_of_associations`: thread, per-element
default, key-list per record, rule-list unchanged); 0 leaks; no regressions in
core / regression suites. Docs (`data-structures.md`) and the loadable
`examples/associations.lb` tour updated.

## Ratios over association values (2026-07-05)

Fiftieth increment.

- **`Ratios[assoc]`** now gives the successive value ratios keyed by the trailing
  key of each pair (leading key drops, `n` -> `n-1`), completing the windowed-
  transform family: `Accumulate`, `Differences`, `Ratios`, and `FoldList` all
  keep the association shape via the shared `assoc_rekey_over_values` mechanism.
  Previously `Ratios[assoc]` mis-fired, treating the `Rule` nodes as division
  operands. `src/list/ratios.c`; lists unchanged.

+1 e2e test group (`test_ratios_over_association_values`); 0 leaks; no
regressions in core / regression / list suites. Docs (`data-structures.md`) and
the loadable `examples/associations.lb` tour updated.

## Frontend: keep editor input ASCII (2026-07-05)

Fifty-first increment (frontend fix).

- The macOS WebView (WKWebView, used by the Tauri shell) was applying "smart
  dashes"/"smart quotes" to the CodeMirror editor, silently turning a typed
  `->` into `–>` (en-dash) and `"` into curly quotes — both of which the parser
  correctly rejects, so `<|"k" -> v|>` produced a spurious "Parse error".
- Fixed at two layers: (1) `EditorView.contentAttributes` now sets
  `autocorrect`/`autocapitalize`/`spellcheck` off so the WebView stops
  substituting; (2) a `normalizeInput` safety-net in `frontend/src/lib/ipc.ts`
  maps curly quotes → straight, en/em/minus dashes → `-`, and `→` → `->` before
  each expression reaches the kernel (also covers pasted text). The C engine is
  unchanged — en-dash is genuinely not Mathilda syntax; the mangling was purely
  a WebView input artifact.

## Parser: Unicode arrow as Rule (2026-07-05)

Fifty-second increment.

- The parser now accepts the Unicode rightwards arrow **`→`** (U+2192, Wolfram
  `\[Rule]`) as a synonym for `->`, at the same precedence (120, right-assoc).
  Wolfram-Language rules and associations paste and parse directly:
  `<|"gold" → 5|>`, `1 → 10`, `x /. 2 → 99`. `get_operator` in `src/parse.c`
  recognises the three UTF-8 bytes `E2 86 92`; ASCII `->` is unchanged. This
  complements the frontend input-normalization fix as an engine-level belt-and-
  suspenders (also helps CLI/pipe users pasting WL code).

+1 e2e test group (`test_unicode_arrow_rule`: bare rule, association, accessor,
ReplaceAll); 0 leaks; no regressions in core / regression suites. Operators doc
updated.

## Apply over association values (2026-07-05)

Fifty-third increment.

- **`Apply[f, assoc]`** (`f @@ assoc`) now uses the association's *values* as
  f's arguments — `f @@ <|k1 -> v1, ...|>` is `f[v1, ...]` (matching Wolfram, and
  consistent with `Total == Plus @@ assoc` and `Apply[List, assoc] ==
  Values[assoc]`). Previously it replaced the head but kept the `Rule` nodes as
  arguments, e.g. `Plus @@ <|"a"->1,"b"->2|>` gave `("a"->1)+("b"->2)`. Only the
  default level is special-cased (`src/funcprog.c`); an explicit level spec
  (`@@@`) and lists are unchanged.

+1 e2e test group (`test_apply_over_association_values`: Plus/Times/Max, symbolic
head, Apply[List], empty); 0 leaks; no regressions in core / regression suites.
Docs (`data-structures.md`) and the loadable `examples/associations.lb` tour
updated.

## Frontend: refresh the built-in Associations demo (2026-07-05)

Fifty-fourth increment (frontend/examples).

- Extended the built-in "Associations" demo notebook (`nb9` in
  `frontend/src/lib/canvas.ts`) with two new sections covering the builtins
  added over the last ~15 increments: **Windowed & statistical**
  (`MinMax`, `Median`, `Tally`, `Accumulate`, `Ratios`, `Plus @@ assoc`) and
  **Records & alignment** (`Lookup` over a list of records, `KeyUnion`,
  `Catenate`, `TakeWhile`, `MapIndexed`, and the Unicode-arrow `→` literal).
- All 49 code cells in the demo were evaluated end-to-end through the engine
  and confirmed correct (0 errors). No engine or spec changes.

## Key[k] operator form (2026-07-06)

Fifty-fifth increment.

- **`Key[k][assoc]`** now extracts the value at key `k` (or
  `Missing["KeyAbsent", k]`) — the curried complement of `assoc[Key[k]]`,
  handled in the function-application path of the evaluator (`src/eval.c`). This
  is the record-field extractor that makes list-of-record pipelines work:
  `GroupBy[records, Key["field"]]`, `SortBy[records, Key["field"]]`,
  `Map[Key["field"], records]`. Previously `Key["g"][<|...|>]` stayed unevaluated,
  so `GroupBy[records, Key["g"]]` produced garbage keys.

+1 e2e test group (`test_key_operator_form`: extract, missing, integer key,
GroupBy/SortBy/Map over records, accessor unchanged); 0 leaks; no regressions in
core / regression suites. Docs (`data-structures.md`), the built-in demo
(`canvas.ts`), and the loadable `examples/associations.lb` tour updated.

## KeyDrop / KeyTake over a list of records (2026-07-06)

Fifty-sixth increment.

- **`KeyDrop[{a1, a2, ...}, keys]`** and **`KeyTake[{a1, ...}, keys]`** now thread
  over a (non-empty) list of associations, dropping/keeping the keys in each
  record — the column-of-records form, matching Wolfram and consistent with
  `Lookup`'s threading. Each element is delegated to the same builtin, so a
  single key or a key-list both thread (`src/assoc.c`, shared `key_drop_take`).
  Single-association forms are unchanged.

+1 e2e test group (`test_keydrop_keytake_over_records`: drop/take thread, key-list
per record, single-assoc unchanged); 0 leaks; no regressions in core / regression
suites. Docs (`data-structures.md`) and the loadable `examples/associations.lb`
tour updated.

## DeleteDuplicatesBy (2026-07-06)

Fifty-seventh increment.

- New builtin **`DeleteDuplicatesBy[expr, f]`** — keeps the first element for
  each distinct `f[element]`, preserving order (the by-key companion to
  `DeleteDuplicates`). Over an association, `f` is applied to the values and the
  surviving entries keep their keys. Works on any list head.
  `builtin_deleteduplicatesby` in `src/list/setops.c`, registered `Protected`
  with a docstring.

+1 e2e test group (`test_deleteduplicatesby`: list, StringLength key, association,
empty, singleton); 0 leaks; no regressions in core / regression / list suites.
Docs (`data-structures.md`) and the loadable `examples/associations.lb` tour
updated.

## First/Last with a default (2026-07-06)

Fifty-eighth increment.

- **`First[expr, default]`** and **`Last[expr, default]`** now return `default`
  when `expr` has no elements (an empty association, empty list, or an atom),
  matching Wolfram. The 1-arg forms are unchanged (still left unevaluated on an
  empty/atomic argument). Works for lists as well as associations
  (`src/part.c`).

+1 e2e test group (`test_first_last_default`: association value, empty
association/list default, atomic default, list element); 0 leaks; no regressions
in core / regression suites. Docs (`data-structures.md`) and the loadable
`examples/associations.lb` tour updated.

## GroupBy over an association (2026-07-06)

Fifty-ninth increment.

- **`GroupBy[assoc, f]`** now groups an association's *entries* by `f[value]`
  into sub-associations (keys preserved), and **`GroupBy[assoc, f, g]`** reduces
  each sub-association — so `GroupBy[assoc, f, Total]` composes with the
  value-threading `Total`. Implemented by parametrizing the existing
  `builtin_groupby` (`src/assoc.c`) rather than duplicating the hash-grouping
  loop: the association path groups by `f[value]`, collects the whole entry, and
  wraps each group as `Association[...]` instead of `List[...]`. The
  `keyfn -> valfn` transform form stays list-only. Lists and the
  `GroupBy[records, Key["field"]]` form are unchanged.

+1 e2e test group (`test_groupby_over_association`: parity grouping, reducer,
group-by-value, empty); 0 leaks; no regressions in core / regression suites.
Docs (`data-structures.md`) and the loadable `examples/associations.lb` tour
updated.

## GatherBy over an association (2026-07-06)

Sixtieth increment.

- **`GatherBy[assoc, f]`** now gathers an association's entries by `f[value]`
  into sub-associations (keys preserved), returned as a list in first-appearance
  order — the sibling of `GroupBy[assoc, f]` without the outer group keys.
  Parametrized the existing `builtin_gatherby` (`src/assoc.c`) with the same
  association flag used for `GroupBy`: group by `f[value]`, collect the whole
  entry, wrap each group as `Association[...]`. Lists are unchanged.

+1 e2e test group (`test_gatherby_over_association`: parity, group-by-value,
empty, singleton); 0 leaks; no regressions in core / regression suites. Docs
(`data-structures.md`) and the loadable `examples/associations.lb` tour updated.

## LaTeX: directional quotes for strings (2026-07-06)

Sixty-first increment (display fix).

- The LaTeX form of a string now uses a left/right curly-quote pair
  (`\text{“...”}`, U+201C / U+201D) instead of straight `"` on both sides. In the
  math font a straight `"` renders as a *closing* quote, so `"apples"` displayed
  as `”apples”` with the opening quote leaning the wrong way; it now shows
  `“apples”`. `src/print_latex.c`.
- Also removed three pre-existing dead-code warnings in `print_latex.c` (unused
  `is_one`, `split_fraction`, and a `cur` variable), surfaced when the file
  recompiled — the file now builds clean under `-Wall -Wextra`.

## LaTeX: recurse generic functions so nested strings get curly quotes (2026-07-06)

Sixty-second increment (display fix, follow-up).

- The LaTeX renderer's generic-function fallback previously dumped the plain
  re-parseable form (`expr_to_string`), so a function of a string — e.g.
  `Key["a"]`, `Missing["KeyAbsent", "z"]` — showed straight quotes (`Key[”a”]`).
  It now renders `Name[arg, ...]` with each argument recursed through the LaTeX
  printer, so nested strings pick up the directional `\text{“...”}` quotes and
  nested expressions (e.g. `x + y`) render as math. `expr_to_string` (InputForm)
  is unchanged — it must keep straight quotes to stay re-parseable.
  `src/print_latex.c`.

## Association performance regression gate (2026-07-06)

Sixty-third increment.

- New **`tests/bench_assoc.c`** — a performance regression gate for the
  Association operations. It times each hash-backed op (`Association @@ rules`,
  `Counts`, `CountsBy`, `GroupBy`, `Merge`, `KeyUnion`, bulk `Lookup`, `Map`,
  `KeySort`) at size *n* and *2n* and asserts the doubling ratio `t(2n)/t(n)`
  stays under 3.3 — ~2.0 for `O(n)`, ~4.0 for an accidental `O(n^2)`. The ratio
  is machine-independent, so it is a robust gate (nonzero exit on regression),
  and it also prints absolute ns/element for tracking constant-factor drift.
- Wired into `tests/CMakeLists.txt`; built by `make` and run as `./bench_assoc`.
  All ops currently ratio ≈ 2.0 (clean linear scaling). Build note added to
  `SPEC.md` §9.

## Association perf gate: absolute "much slower" tripwire (2026-07-06)

Sixty-fourth increment.

- Extended `tests/bench_assoc.c` with a second, machine-independent gate: an
  absolute-cost tripwire that errors if an op got *much* slower. Each op's
  per-element time is normalized against a runtime calibration (`Total[list]` of
  the same size) — a dimensionless ratio that is stable across machines — and
  compared to a checked-in `baseline_norm`. Fails (nonzero exit) if any op
  exceeds **2.5×** its baseline; comfortably above the ~few-percent run-to-run
  noise, tight enough to catch a real regression. Baselines recorded on an
  Apple M-series build; verified the tripwire fires (a simulated 9.7× slowdown
  flags "MUCH SLOWER" and exits 1) and passes on the real timings (~1.0×).
- `SPEC.md` §9 documents both gates (scaling ratio + absolute cost).
