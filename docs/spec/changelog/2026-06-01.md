# Changelog: week of 2026-06-01 (Mon) – 2026-06-07 (Sun)

Feature additions and fixes recorded during this week.

## Integration of undefined (unknown) functions — Roach §1.7 (2026-06-06)

New module `src/calculus/integrate_unknown.c` (+ `.h`) and builtin
`Integrate`Undefined[f, x]`, wired as the first stage of the `Integrate`
dispatch cascade (gated to run only when the integrand contains an
undefined-function derivative such as `f'[x]`) and selectable strictly via
`Method -> "Undefined"`. Implements the algorithm of Kelly Roach, *Indefinite
and Definite Integration* (1992), §1.7 ("Undefined Functions").

Each undefined function value `u[g]` and its derivative tower
`u'[g], u''[g], …` is treated as a differential-field generator. The integrand
is reduced by an integration-by-parts step on the highest-order generator `A`
present: writing `f = C·A + D` (linear in `A`) and `B` for `A`'s antiderivative
atom, `∫f dx = W − ∫(dW/dx − C·A − D) dx` where `W = ∫(C/g') dB` and
`g' = D[arg, x]` is the chain factor. The inner `∫… dB` step is delegated to the
existing rational integrator over a substituted generator symbol, which means a
single mechanism subsumes Roach's polynomial, fraction, **and** log parts:

```
Integrate[f'[x], x]                                  -> f[x]
Integrate[x f'[x] + f[x], x]                         -> x f[x]
Integrate[f'[x] g[x] + f[x] g'[x], x]                -> f[x] g[x]
Integrate[f''[x] g[x] + 2 f'[x] g'[x] + f[x] g''[x], x] -> f[x] g'[x] + f'[x] g[x]
Integrate[f'[x]/f[x], x]                             -> Log[f[x]]
Integrate[f'[x]/f[x]^2, x]                           -> -1/f[x]
Integrate[(f'[x] g'[x] - f[x] g''[x])/(f[x]^2 + g'[x]^2), x] -> -ArcTan[g'[x]/f[x]]
Integrate[2 x f'[x^2], x]                            -> f[x^2]   (* chain rule *)
```

Robustness: the by-parts residual is handed back to the integrator as a *whole*
(sums are not split term-by-term, since e.g. `f'[x]g'[x] + f[x]g''[x]` is an
exact derivative whose pieces individually have no closed form), and a
canonical-form cycle guard makes genuinely non-elementary inputs like
`Integrate[f'[x] g'[x], x]` terminate unevaluated rather than looping.
Canonicalisation throughout uses `Cancel[Together[Expand[…]]]` so that
mathematically-zero residuals collapse to literal `0`. Memory-clean under
valgrind (no leak stacks in the module).

A logarithm of an unknown-function expression, `Log[eta]`, is additionally
recognised as a transcendental generator (Roach §1.6/§1.7) and reduced by parts,
`Integrate[C L + D, x] = G L − Integrate[G L' − D, x]` with `G = Integrate[C, x]`
and `L' = eta'/eta`. A self-referential resolution closes perfect-power cases
where the residual is a constant multiple of the original integrand
(`Integrate[Iorig] = bdy − k Integrate[Iorig] ⇒ bdy/(1+k)`):

```
Integrate[(f[x] - x f[x] + f[x] Log[x f[x]] + x f'[x])/f[x], x]
                                            -> -x^2/2 + x Log[x f[x]]
Integrate[Log[f[x]] f'[x]/f[x], x]          -> Log[f[x]]^2/2
```

New unit + stress suite `tests/test_integrate_unknown.c` (`integrate_unknown_tests`):
exact-form assertions, a randomized `D`-round-trip harness, negative cases, a
cycle-termination check, and `Log[eta]`-generator cases. Known limitations:
transcendental generators other than `Log` (`ArcTan[eta]`, `Exp[eta]`, …) and
nested unknown arguments `f[g[x]]` are deferred.

## Integrate of InterpolatingFunction objects (2026-06-06)

`Integrate[InterpolatingFunction[...][x], x]` now returns the antiderivative as
a fresh applied `InterpolatingFunction[...][x]`, closing the asymmetry where
`D` differentiated such objects but `Integrate` left them unevaluated (matching
Mathematica).

New module `src/calculus/integrate_interp.c` (+ `.h`), called from
`builtin_integrate` *before* the inexact-rationalisation scan (the object embeds
Real sample data that must not be force-rationalised). Algorithm: read the grid
nodes from the stored table, sample `y_i = ifun[x_i]`, accumulate antiderivative
node values `F_i` (with `F_0 = 0`) by 5-point Gauss-Legendre quadrature of the
piecewise-polynomial interpolant, then build a Hermite `InterpolatingFunction`
through `{{x_i}, F_i, y_i}`. Supplying the exact derivative `F'(x_i) = y_i`
makes `D[Integrate[ifun[x], x], x]` round-trip to `ifun[x]` exactly. The
construction is uniform: it also handles the derivative-annotated objects `D`
produces (integrating the sampled derivative recovers the lower order). Only the
1-D, direct case (the applied argument is the integration variable) is reduced;
`Integrate[ifun[g[x]], x]` is left to the normal cascade. Exact integer grids
keep an integer domain by reusing the original coordinate Exprs.

New `test_integrate_interp` in `tests/test_interp.c` (result shape, `2x → x^2`
antiderivative numerics, `D∘Integrate` round-trip, and the non-reducible
chain-rule / foreign-variable cases). `leaks`-clean. Reference docs:
`docs/spec/builtins/calculus.md` (Integrate → InterpolatingFunction integrands).

## Parser: leading minus binds looser than `^` on numeric literals (2026-06-06)

Fixed a precedence bug where a leading minus on a *number* was folded into the
literal before `^` was applied: `-3^(2/3)` parsed as `Power[-3, Rational[2,3]]`
instead of `Times[-1, Power[3, Rational[2, 3]]]`. Mathematica binds unary minus
looser than `Power`, so `-3^2` means `-(3^2)`. The symbolic case (`-x^2`) was
already correct; only numeric literals took the wrong path.

`src/parse.c` now routes a leading `-<number>` through the prefix-operator
branch (yielding `Times[-1, …]`) when the literal is immediately followed by `^`
(allowing intervening whitespace), via two helpers `number_followed_by_power`
and `minus_is_prefix`. A plain `-3` (no following `^`) still parses as the
integer literal `-3`, and `-3*5`, `-3 + 5`, `2 - 3^2` are unchanged. Verified
against `parse_tests` and `parser_precision_tests`.

## Floor/Ceiling/Round/IntegerPart/FractionalPart on exact real numerics (2026-06-06)

`Round`, `Floor`, `Ceiling`, `IntegerPart`, and `FractionalPart` now resolve
**exact real numeric arguments** that previously fell through unevaluated —
named constants and their products/powers such as `10000000 3^(2/3)` or
`25000000000000000000 Pi`:

```mathematica
In[1]:= Round[10000000*3^(2/3)]
Out[1]= 20800838          (* was: Round[10000000 3^(2/3)] *)

In[2]:= Round[25000000000000000000 Pi]
Out[2]= 78539816339744830962
```

**Implementation** (`src/piecewise.c`, MPFR-only). When a leaf branch
(`Integer`/`BigInt`/`Real`/`MPFR`/`Rational`/`Complex`) and the symbolic
simplifications (sign extraction, idempotency) all decline, `do_piecewise_1`
calls the new `do_piecewise_numeric_exact`. It `numericalize()`s the argument
to MPFR at 256 bits, then doubles (to a 65536-bit / ~19,700-digit cap),
applying the rounding op at each precision and accepting the result only once
**two successive precisions agree on the integer**. This interval-style
certification means the answer is never wrong: a value too close to a rounding
boundary to certify, or one that does not collapse to a pure real number
(still symbolic, or `Complex[...]`), is left unevaluated rather than guessed.
`FractionalPart` is kept exact as `x - IntegerPart[x]` (e.g.
`FractionalPart[10000000 3^(2/3)]` → `-20800838 + 10000000 3^(2/3)`), matching
Mathematica. Verified leak-free (`leaks --atExit`). New cases added to
`tests/test_piecewise.c`.

No new builtins or attributes; behaviour of the existing five heads extended.

## Build hygiene: silence GCC -Wclobbered / -Wmisleading-indentation (2026-06-06)

Cleaned up three warnings emitted by GCC 15 under `-std=c99 -Wall -Wextra`
(Apple Clang did not surface them):

- **`core.c` — `builtin_time_constrained` `-Wclobbered`.** The `sigsetjmp`
  was isolated into a new `TC_NOINLINE` helper `tc_run_guarded(body)`, whose
  only jump-spanning automatic (`result`) is now `volatile`. Removing the
  setjmp from `builtin_time_constrained` stops GCC's conservative pass from
  flagging `fail_arg` and the inlined `eval_and_free` temporaries (`eval.h:73`)
  as possibly clobbered. `TC_NOINLINE` is `__attribute__((noinline))` guarded
  on `__GNUC__`. Behaviour is byte-for-byte unchanged (success, `Infinity`,
  and timeout paths all verified).
- **`interp_mpfr.c` — `-Wmisleading-indentation` (x2).** Two `for`-loop
  cleanup tails (`mpfr_clear` loop followed by `free(p); return`) on one line
  were split across lines so the trailing statements are no longer indented as
  if guarded by the loop.

No functional change; no builtins added or modified.

## Fit and DesignMatrix: linear least-squares regression (2026-06-06)

New module `src/fit.c` adds the `Fit` and `DesignMatrix` builtins (registered in
`core_init`; symbols `Fit`, `DesignMatrix`, `NormFunction`, `FitRegularization`
in `sym_names.c`; docstrings in `info.c`; both `Protected`).

- `Fit[data, {f1, ..., fn}, vars]` fits `a1 f1 + ... + an fn` to data;
  `Fit[{m, v}]` returns the coefficient vector minimising `Norm[m.a - v]`.
- Data shapes: flat value list (coordinates `1..n`), univariate pairs, and
  multivariate rows `{{x, y, ..., v}, ...}`. `vars` may be a symbol or a list.
- Solvers are reuse-first: L2 and Tikhonov/ridge route through `LeastSquares`
  (so they support exact, machine and MPFR arithmetic); ridge is reduced to an
  augmented `[m; Sqrt[lambda] I]` system. `FitRegularization -> {"LASSO", lambda}`
  uses cyclic coordinate descent with soft-thresholding; `NormFunction ->
  Function[Norm[#, 1]]` uses iteratively reweighted least squares; other norms
  (and norm+regularisation combinations) fall back to `FindMinimum`, warm-started
  from the L2 solution.
- `WorkingPrecision -> Automatic | n | Infinity`; `Automatic` preserves the
  precision of already-approximate `N[..., p]` input. Verified against the
  Wolfram reference results (line, quadratic, plane, sine basis, exact rational
  `135/199 ...`, 24-digit MPFR, Hilbert design matrix, ridge, LASSO sparsity,
  1-norm LAD). Aliases `"L2"`/`"RidgeRegression"` and `"L1"`.
- 22 unit tests in `tests/test_fit.c`; clean under valgrind (no Fit-attributable
  leaks). Full reference in `docs/spec/builtins/linear-algebra.md`.

## Factor: NULL-head crash on non-field GCD division (2026-06-05)

Fixed a segfault in `factor_degree_one` (`src/poly/facpoly_simple.inc`) reached
via `Simplify → Factor`. For a degree-1-in-some-variable polynomial it pulls out
`G = gcd(L, C)` of the linear and constant coefficients, then forms `L/G` and
`C/G` with `exact_poly_div`. When the coefficients live in a non-field ring —
e.g. radical coefficients like `Sqrt[3]`, so `Q[Sqrt[3]]` is a polynomial ring,
not a field — `exact_poly_div` returns `NULL` (its documented "no exact
division" signal) even though `G` is a formal GCD. The unchecked `NULL` flowed
into `Times[L_G, x]` and the evaluator dereferenced a `NULL` arg in
`flatten_sequences`.

- **Repro (pre-existing; independent of the dispatch fix above, confirmed on
  baseline `symtab.c`).** `Simplify[D[ArcSin[Sqrt[3] Sin[x]/2]/Sqrt[3], x] -
  1/Sqrt[4 + Tan[x]^2]]` → SIGSEGV. Surfaced by the CRC corpus once the
  Optional-dispatch fix let `Integrate[1/Sqrt[4 + Tan[x]^2], x]` evaluate and
  its verifier ran `Simplify` over a `Sqrt[3]`-bearing expression.
- **Fix.** Guard both `exact_poly_div` results; if either is `NULL`, free the
  partial state and `continue` to the next variable (this var does not factor
  out cleanly). The factorisation degrades to "no degree-1 factor here" instead
  of crashing.
- Pinned by `tests/test_integrate_dispatch.c` (the integrand above now returns
  cleanly) and the `factor_baseline/phase0/recombine/terms`, `mvfactor`,
  `mvfactor3`, `qafactor`, `squarefreeq`, and `facpoly` suites (all green).

## CRC table: branch-correct 1/Sqrt[1 ± Cos/Sin[x]] (Formulas 398–401, 2026-06-05)

Replaced the four reciprocal-square-root antiderivatives in
`src/internal/CRCMathTablesIntegrals.m` with branch-correct forms. The classical
`Sqrt[2] Log[Tan[x/4 (+ shift)]]` primitives are real on only every *other*
inter-pole interval of the integrand (elsewhere the `Tan` argument turns
negative and the `Log` goes complex), and even where real the sign is wrong —
the integrand carries an absolute value, e.g.
`1/Sqrt[1 - Cos[x]] = 1/(Sqrt[2] |Sin[x/2]|)`. These integrands have poles, so
no continuous antiderivative spans one; correctness is per pole-bounded
interval.

- **Surfaced by the corpus** as the lone DIFF-NONZERO regression once the
  Optional-dispatch fix made `1/Sqrt[1 - Sin[x]]` reach Formula 400: it returned
  `Sqrt[2] Log[Tan[x/4 - Pi/8]]`, correct at the old sample point x≈2.6 but
  complex/wrong at x≈0.7. (The three siblings shared the latent bug but happened
  to sample inside their good intervals.)
- **Fix.** Keep the integrand's radical literally; the factor
  `Sqrt[1 - Cos[x]]/(Sqrt[2] Sin[x/2]) = sign(Sin[x/2])` restores the sign the
  `ArcTanh` primitive of the *signed* reciprocal drops:
  - `1/Sqrt[1 - Cos[x]]` → `-ArcTanh[Cos[x/2]] Csc[x/2] Sqrt[1 - Cos[x]]`
  - `1/Sqrt[1 + Cos[x]]` → `ArcTanh[Sin[x/2]] Sec[x/2] Sqrt[1 + Cos[x]]`
  - `1/Sqrt[1 - Sin[x]]` → `-ArcTanh[Cos[x/2 - Pi/4]] Csc[x/2 - Pi/4] Sqrt[1 - Sin[x]]`
  - `1/Sqrt[1 + Sin[x]]` → `ArcTanh[Sin[x/2 - Pi/4]] Sec[x/2 - Pi/4] Sqrt[1 + Sin[x]]`
  This mirrors the Phase C/D radical-literal treatment already applied to the
  non-reciprocal Formulas 394–397.
- Pinned by `test_crctable_reciprocal_sqrt_branch` in
  `tests/test_integrate_dispatch.c` (numeric `D[result, x] − integrand` at points
  on the previously-broken interval for all four).
- **Verification-cost note.** In the `crc_corpus` harness these four cases now
  report TIMEOUT rather than DIFF-ZERO. Integration and the derivative are
  instant; the cost is entirely in the corpus's symbolic verifier
  (`is_zero_after_simplify` → `Together`/`Cancel`/`Simplify`), which cannot
  reduce `D[ArcTanh-form] − integrand` to 0 within the 5 s budget (the residual
  *is* algebraically zero — the `ArcTanh` terms cancel — but Simplify does not
  see it through the trig radical). The verifier's numeric fallback would pass,
  but the symbolic pass exhausts the budget first. The integrals are correct
  (dense numeric scan, max error ~1e-12). This is strictly better than before:
  `1/Sqrt[1 - Sin]` was DIFF-NONZERO (wrong), and the other three were "closed"
  only because the corpus sample points happened to miss their wrong branches.

## Pattern dispatch: Optional-headed first-arg slots (2026-06-05)

Fixed a §3.5 DownValue dispatch-filter bug that silently skipped any rule whose
first argument is a function-call pattern with an `Optional` argument
(`src/symtab.c`, `pattern_arg_head_canon`). Such a slot can collapse to its base
via the Optional default — e.g. `Power[b_, n_.]` matches a bare `b` as `b^1`,
`Times[a_., x_]` matches `x` — so a matching input need **not** be headed by the
pattern's head. The filter was caching the head anyway (e.g. `"Power"`) and
rejecting any input whose first-arg head differed (e.g. a bare symbol, `"Symbol"`)
**before** the matcher ran, even though `MatchQ`/`ReplaceAll` (which bypass the
filter) matched fine.

- **User-visible symptom (CRC integral table).** `Integrate[a x, x,
  Method -> "CRCTable"]` returned unevaluated because the monomial rule
  `IntegrateTable[x_^n_., x_] /; FreeQ[n, x] && n =!= -1 := x^(n+1)/(n+1)`
  never reached the matcher for the `IntegrateTable[x, x]` reduction
  (`x` ≡ `x^1`). `a x^2` and `a x^(-1)` worked because their exponents are
  explicit Powers. Now `Integrate[a x, x, Method -> "CRCTable"]` → `1/2 a x^2`.
- **Fix.** `pattern_arg_head_canon` returns `NULL` (wildcard, scan-always) for a
  plain function-call pattern when any of its arguments is an `Optional` (detected
  via a new `pattern_arg_is_optional` helper that also sees through a
  `Pattern[name, …]` wrapper). The dispatch filter is a pure optimization, so
  widening to wildcard only ever costs an extra `match()` call — never
  correctness. Other slot shapes are unaffected, so head specialization is
  retained everywhere it is sound.
- Pinned by `tests/test_condition_downvalue.c::test_crc_table_formula_fires`
  (which already asserted the `a x` case) plus the `crc_corpus`, `match`,
  `match_extensive`, `patterns`, `symtab`, and `integrate_dispatch` suites.

## Interpolation: O(1) re-evaluation + cached grid (2026-06-05)

Two-part performance fix for `InterpolatingFunction` application
(`src/interp.c`). Per evaluated point the system did two O(n)-in-the-node-count
passes over the n-point table that have nothing to do with the interpolation
maths: (1) the evaluator re-evaluated the object's entire `table` argument on
every application, and (2) `interp_apply` rebuilt the parsed tensor grid from
scratch. Together these dominated per-point cost and made it scale with `n`.
At n=1600 (1000 off-node points) the fix takes per-point eval from ≈1.93 ms to
≈0.38 ms (**≈5×**), with the speedup growing with `n`.

- **`HoldAll` on `InterpolatingFunction`** (the larger win, ≈3× alone). The
  object's `domain`/`table` arguments are constant, already-evaluated numeric
  data, but the symbol had no Hold attribute, so each `ifun[x]` made the
  evaluator re-evaluate the whole table — an O(n) deep re-traversal every call.
  Holding them keeps the object a true normal form and makes evaluating it O(1).
  The application's own argument `x` is still evaluated normally (governed by the
  object head, not by `InterpolatingFunction`).
- **Single-entry grid cache** of the most-recently-built `IFun`, keyed on the
  **structural identity** (`expr_eq`) of `(domain, table, periodic)`.
  `interp_eval_double` borrows the grid from `grid_cache_get` instead of
  building/freeing one per call (which included an O(n²) `grid_insert` sort); the
  cache owns deep copies of the keys (so `entryAt` stays valid) and frees the
  previous entry on every rebuild. Correct by construction: redefining the data
  yields a structurally different `table`, which misses and rebuilds; the single
  entry serves the build-once / evaluate-many loop (`Table[f[x], {x, …}]`), and
  interleaving distinct objects rebuilds each time (no stale values).
- Note: the bracketing-interval search (`bracket_interval`) was **already** a
  binary search; the earlier review's "O(n) window scan" diagnosis was
  inaccurate — the real costs were the table re-evaluation and the grid rebuild.
- Machine-precision grid cache only; the MPFR kernels (`interp_mpfr.c`) are
  unchanged. No behavioural change: every value, the `dmval` extrapolation
  warning, and the full `tests/test_interp.c` suite are byte-identical
  before/after (also verified against the `match`/`replace`/`patterns`/`eval`
  suites for the attribute change). Leak-clean under `leaks --atExit` (0 leaks).
  Strict C99 `-Wall -Wextra` clean.

## InterpolatingFunction (2026-06-04)

New builtin object `InterpolatingFunction` (`src/interp.c` / `src/interp.h`,
registered in `interp_init`, called from `core.c`). Attribute `Protected`;
symbol `SYM_InterpolatingFunction` in `sym_names.{c,h}`; docstring in
`info.c`; reference page in `docs/spec/builtins/functional-programming.md`.

- `InterpolatingFunction[domain, table]` represents an approximate function over
  tabulated data; the object is a normal form that persists unevaluated, like
  `Function[...]`. `domain = {{x1min,x1max}, ...}` carries one interval per
  dimension; `table = {{coord, val}, ...}` (scalar `coord` for 1-D, an
  `{x1,...,xm}` list for m-D).
- `InterpolatingFunction[...][x1,...,xm]` evaluates the approximate function by
  **tensor-product** piecewise-polynomial interpolation of default order 3
  (cubic) in each dimension, with a sliding window centred on the bracketing
  interval and a Newton divided-difference polynomial. Order drops to
  `min(3, n_k - 1)` per dimension for small grids. The windowing reproduces
  Mathematica's default piecewise interpolant (verified against `wolframscript`
  for non-polynomial 1-D and 2-D data, values agreeing to full double
  precision; e.g. hill table `ifun[2.5] → 3.6875`).
- **Multi-dimensional**: data must fill the full Cartesian product of the
  per-dimension grids; points may be supplied in any order (sorted internally).
- **Derivatives**: `Derivative[d1,...,dm][InterpolatingFunction[...]]` (so
  `ifun'`, `D[ifun[x],x]`, `D[f[x,y],{x,2}]`) returns another
  `InterpolatingFunction` carrying the accumulated orders — like Mathematica —
  which evaluates the mixed partial of the windowed interpolant; orders are
  additive and an order beyond the local degree gives `0`. Implemented via
  `interp_make_derivative` (dispatched from `eval.c`'s Derivative branch) plus a
  generic-chain-rule fall-through for applied `InterpolatingFunction` objects in
  `src/calculus/deriv.c`. Verified vs `wolframscript`: `g'[2.5]=1.04166…`,
  `g''=-1.5`, `g'''=-1`, `g''''=0`, 2-D `D[1,0]=3`, `D[0,1]=18.75`, `D[1,1]=0`.
- Application is dispatched in `eval.c` alongside `Function`/`Composition`
  (function-headed expression branch), via `interp_apply`.
- Exact node-tuple hits return the stored value exactly (`ifun[2] → 3`,
  2-D `f[2,3] → 31`); other queries return a machine real. Out-of-domain
  arguments are extrapolated with an `InterpolatingFunction::dmval` warning.
  Symbolic, wrong-arity (≠ `m`), or malformed objects (fewer than two grid
  points, incomplete tensor grid, non-numeric data) are left unevaluated.
- Standard output shows only the domain, abbreviating the rest as `<>`
  (`InterpolatingFunction[{{0, 5}}, <>]`); `FullForm` shows the full structure.
- 48 unit tests in `tests/test_interp.c` (linear/quadratic/cubic, exact cubic
  reproduction, reference windows, exact nodes, extrapolation, 1-D derivatives,
  derivative-as-object, `D` chain rule, 2-D interpolation + mixed partials,
  unsorted input, unevaluated and malformed cases, printing). Leak-clean (the
  only `leaks` hit is the pre-existing `system_constants_init` global). Strict
  C99 `-Wall -Wextra` clean.

## Interpolation: periodic interpolation + vector/array-valued data (2026-06-05)

Closes the last two `Interpolation` gaps (`src/interp.c`, `src/interp_mpfr.c`).
Both features work for every method, in n-D, and at machine **and** MPFR
precision; standard mathematically-correct interpolants (exact at nodes).

- **Vector/array-valued samples**: `Interpolation[{{x, {v1,v2}}, …}]` (and arrays
  of any rank, with explicit coordinates). Implemented as an outer component loop
  over the value-array shape, each component a scalar sub-problem reusing the
  existing kernels; the result is reassembled into an array of the same shape.
  Composes with Spline/Hermite, with **supplied derivatives** (whose tensors
  carry the value-array axes first, then the derivative axes), and with n-D.
  E.g. the documented `fv[1.5] → {4.03175, 4.07143}`.
- **`PeriodicInterpolation -> True`** (and a per-dimension `{True, False}` list):
  period = the data span; out-of-range arguments wrap with no `dmval` warning.
  The seam is handled by **wrap-around window gathering** (signed window start,
  `±period` abscissa offsets) for the default and Hermite methods, periodic
  cyclic finite differences for estimated/mixed derivatives (`fd_apply`), and a
  new **cyclic cubic spline** (`spline_eval_periodic`, Sherman–Morrison solve of
  the cyclic tridiagonal system) for `Method -> "Spline"`. Verified: node
  reproduction, exact wrap (`f[x+kP] = f[x]`, including negative `k`), C1 seam
  continuity for the cyclic spline, and 2-D / per-axis periodicity.
- Object representation gains a 6th positional argument
  `InterpolatingFunction[domain, table, ders, orders, method, periodic]` (a
  length-m `True`/`False` list); `interp_make_derivative` preserves it. Vector
  data needs no object change (detected from the table's value shape).
- MPFR mirror (`interp_mpfr.c`) adds the component loop, periodic wrap gather,
  cyclic-spline and periodic-FD kernels, with full-precision period arithmetic
  (`mpfr_fmod` reduction). 22 new tests in `tests/test_interp.c` (133 total),
  run on both precisions; leak-clean under `leaks` (only the pre-existing
  `system_constants_init` global). Both interp files compile under strict C99
  with and without `USE_MPFR`.

## Interpolation: Spline/Hermite methods, derivative data, arbitrary precision (2026-06-04)

Major extension of `Interpolation` toward Mathematica fidelity (`src/interp.c`
rewritten; new `src/interp_mpfr.c` for the MPFR kernels). Standard,
mathematically-correct interpolants — pass through every data point and honour
every supplied derivative; interior values need not match Wolfram's proprietary
B-spline scheme.

- **`Method -> "Spline"`**: natural cubic spline (C², second derivative 0 at the
  ends; linear for two points), tensor-product over the full grid via a
  tridiagonal Thomas solve.
- **`Method -> "Hermite"`**: piecewise cubic Hermite with node slopes estimated by
  3-point finite differences (reproduces quadratics exactly).
- **Derivative-supplied data** `{{{x,...}, f, df, ddf, ...}, ...}` (1-D and n-D):
  interpolated by tensor-product Hermite of per-dimension order `k = max(K,1)`
  (`K` = highest supplied derivative order). A full mixed-derivative tensor is
  built from the supplied gradient/Hessian/… tensors; mixed partials absent from
  the data (e.g. `f_xy` from gradient-only data) are estimated by central finite
  differences across the grid. Evaluated by a direct tensor-Hermite-basis sum
  over the bracketing cell. Reproduces a cubic from `{f,f'}` and a quintic from
  `{f,f',f''}` exactly; separable polynomials in m-D reproduced exactly.
  Derivatives of the interpolant (`D`, `Derivative`, `f'`) compose for every form.
- **Arbitrary precision**: when the data, domain or evaluation point carry MPFR
  precision the MPFR kernels run at the data's working precision and an
  `EXPR_MPFR` is returned (otherwise machine `double`/`EXPR_REAL`). Precision is
  detected with `common_scan_inexact` / `numeric_expr_is_mpfr`; values coerced via
  `get_approx_mpfr`. Verified to agree with the machine path (e.g. spline interior
  value to 30+ digits, `Precision` = 30.103).
- The method/order annotations are carried on the `InterpolatingFunction` object
  (`[domain, table, ders, orders, method]`) and preserved through differentiation.
- The object representation is unchanged for value-only data; derivative-supplied
  data is stored as longer table entries `{coord, val, df, ...}`.
- 35 new unit tests in `tests/test_interp.c` (111 total): Spline (node
  reproduction, n=2 linear, linear-data, derivative), Hermite method (quadratic
  reproduction, derivative), 1-D supplied `{f,f'}`/`{f,f',f''}` (cubic/quintic
  exact, derivative), n-D gradient (separable cubic + partials) and Hessian
  (biquintic) data, and arbitrary-precision (MPFR result type + value) across all
  methods/forms. Leak-clean under `leaks` (only the pre-existing
  `system_constants_init` global). Both `interp.c` and `interp_mpfr.c` compile
  under strict C99 with and without `USE_MPFR`.

## Interpolation (2026-06-04)

New builtin `Interpolation` (`src/interp.c`, registered in `interp_init`).
Attribute `Protected`; symbols `SYM_Interpolation`, `SYM_InterpolationOrder`,
`SYM_PeriodicInterpolation` in `sym_names.{c,h}`; docstring in `info.c`;
reference page in `docs/spec/builtins/functional-programming.md`. Produces the
[`InterpolatingFunction`](#interpolatingfunction-2026-06-04) object documented
above.

- `Interpolation[data]` accepts three data forms: `{f1, f2, ...}` (values at
  `x = 1, 2, ...`), `{{x1, f1}, ...}` (values at explicit abscissae), and
  `{{{x1, y1, ...}, f1}, ...}` (an m-D tensor grid). It normalises each to the
  `{coord, val}` table form and infers `domain = {{min, max}, ...}` per
  dimension, preserving the data's number type (integer abscissae → integer
  domain). E.g. `Interpolation[{1,2,3,5,8,5}][2.5] → 2.4375` (matches
  Mathematica).
- `Interpolation[data, x]` builds the object and immediately evaluates it at the
  point `x` (a number, or an `{x, y, ...}` coordinate list in m-D).
- **`InterpolationOrder -> n`**: piecewise-polynomial degree (default 3). Carried
  on the object as an optional 4th argument
  (`InterpolatingFunction[domain, table, ders, orders]`) that `interp_apply`
  honours and `interp_make_derivative` preserves through differentiation.
  `0` → piecewise-constant (left node), `1` → piecewise-linear; e.g.
  `Interpolation[{1,5,7,2,3,1}, InterpolationOrder->1][2.5] → 6.`.
- `Method` and `PeriodicInterpolation` accept only their default settings
  (`Automatic`, `False`). Derivative-reproducing (Hermite) data
  (`{{x, f, df, ...}, ...}`), the `"Spline"`/`"Hermite"` methods, periodic
  interpolation, vector-valued samples, and any unknown option leave the call
  unevaluated.
- 28 new unit tests in `tests/test_interp.c` (76 total in the file): the three
  data forms, immediate evaluation, `InterpolationOrder` 0/1/3, exact-node
  reproduction, differentiation of an `Interpolation`-built object (`f'`, `D`,
  order preserved), and unsupported/malformed inputs left unevaluated.
  Leak-clean (only the pre-existing `system_constants_init` global hit). Strict
  C99 `-Wall -Wextra` clean.

## MinimalPolynomial (2026-06-04)

New builtin `MinimalPolynomial` (`src/poly/minpoly.c`, registered in the new
`minpoly_init`, called from `core.c`). Attributes `Listable`, `Protected`;
symbol in `sym_names.c`; docstring in `info.c`; reference page in
`docs/spec/builtins/arithmetic-and-algebra.md`.

- `MinimalPolynomial[s, x]` gives the lowest-degree polynomial in `x` with
  integer coefficients, positive leading coefficient and content `1`, having the
  algebraic number `s` as a root (e.g. `MinimalPolynomial[Sqrt[2] + Sqrt[3], x]`
  → `1 - 10 x^2 + x^4`).
- `MinimalPolynomial[s]` returns the minimal polynomial as a pure function
  (`1 - 10 #1^2 + #1^4 &`).
- `MinimalPolynomial[s, x, Extension -> a]` gives the characteristic polynomial
  of `s` in `Q(a)` over `Q(a)` — `m_s(x)^([Q(a):Q]/[Q(s):Q])` when `s ∈ Q(a)`,
  else left unevaluated (`MinimalPolynomial::ext`).
- `s` may be built from integers/rationals, radicals (`Sqrt`, `Power[_, p/q]`),
  the imaginary unit, roots of unity (`Power[E, I Pi r]`), `Root[f &, k]`
  objects, and `Plus`/`Times`/`Power`. The first argument threads over lists.
- **Algorithm (exact resultant elimination):** each algebraic atom is replaced
  by a fresh auxiliary variable with a polynomial defining relation — negative
  powers introduce a reciprocal variable (`D w − 1`) so every relation stays
  polynomial and no `Together`/`Numerator` of the system is needed. The
  auxiliaries are eliminated from `x − s` by repeated `internal_resultant`
  (highest index first; introduction order guarantees each is present when
  eliminated). The univariate result is denominator-cleared
  (`Together`→`Numerator`), made primitive over `Z`, and factored
  (`internal_factor`); the unique irreducible factor vanishing at `s` is chosen
  by a high-precision (`80`-digit MPFR) numeric test, then returned primitive
  with positive leading coefficient (`zupoly_primitive_part`).
- The `Extension` form reduces to the tower law on degrees from the same core;
  membership `s ∈ Q(a)` is verified via the primitive-element degree
  `[Q(a, s):Q] == [Q(a):Q]` (max minimal-polynomial degree of `a + r s` over
  `r ∈ {1, 2}`).
- Non-algebraic input (`Pi`, `Log[2]`, …) is left unevaluated.
- Tests: `tests/test_minimalpolynomial.c` (radicals, nested radicals, complex /
  Gaussian numbers and roots of unity, `Root[]` objects, rationals/integers,
  listable threading, pure-function form, the `Extension` form including the
  non-membership case, and unevaluated non-algebraic input). Valgrind-clean —
  byte-for-byte identical leak totals to a baseline test that never calls it, so
  no leaks attributable to the new code.

## FindIntegerNullVector (2026-06-04)

New builtin `FindIntegerNullVector` — PSLQ-style integer-relation detection
(`src/linalg/latticereduce.c`, registered in `linalg_init`). Attribute
`Protected`; symbol in `sym_names.c`; docstring in `info.c`; reference page in
`docs/spec/builtins/linear-algebra.md`.

- `FindIntegerNullVector[{x1, …, xn}]` finds integers `{a1, …, an}`, not all
  zero, with `a1 x1 + … + an xn == 0`. `FindIntegerNullVector[{…}, d]` limits
  the search to relations of norm `≤ d`.
- Numbers may be **real or complex**, **exact or inexact**; complex inputs
  yield **Gaussian-integer** relations. Recovers minimal polynomials of
  algebraic numbers, Machin-type π formulas, and `Log` relations.
- Reuses the exact Gaussian-rational LLL kernel of `LatticeReduce`: the
  numbers are numericalised to a working precision `b` and embedded as the
  rows `(e_i | round(2^b x_i))` of an integer-relation lattice; the relation
  is the shortest reduced row. `lll_reduce` was extended to optionally report
  `minᵢ |b*ᵢ|²` for the certified bound (LatticeReduce behaviour unchanged).
- **Validation:** exact residuals are checked with `PossibleZeroQ`, guarded by
  a confidence test (`n·log2(‖a‖²) < 1.35·b`) that rejects large-coefficient
  artefacts and forces a precision increase; inexact relations hold to input
  precision.
- **Precision:** new user-settable system variable **`$MaxExtraPrecision`**
  (default `50`). Exact `Automatic` searches escalate from `$MachinePrecision`
  up to `$MachinePrecision + $MaxExtraPrecision` digits; inexact input uses
  its own precision; `WorkingPrecision → p` fixes the precision.
- **Options:** `WorkingPrecision → Automatic | digits`,
  `ZeroTest → Automatic | f`.
- **Certified bound:** the rigorous LLL Gram–Schmidt bound
  `λ₁² ≥ minᵢ |b*ᵢ|²` gives a lower bound `B = √(M²/(1 + n/4))` on the norm of
  any integer relation, driving `::norel` / `::lgrelb` / `::rnfb` / `::rnfu`.
  This is the LLL bound (conservative versus Wolfram's tighter PSLQ bound), so
  at the razor edge of the minimal-relation norm Mathilda may return the
  relation with `::lgrelb` where Wolfram proves nonexistence.
- Diagnostics: `::norel`, `::lgrelb`, `::rnfb`, `::rnfu`, `::ztest1`.
- Relations are canonical only up to sign and (complex) a Gaussian-unit
  multiple.

## LatticeReduce (2026-06-04)

New builtin `LatticeReduce` (`src/linalg/latticereduce.c`, registered in
`linalg_init`). Attribute `Protected`; symbol registered in `sym_names.c`;
docstring in `info.c`; reference page in
`docs/spec/builtins/linear-algebra.md`.

- `LatticeReduce[m]` returns an LLL-reduced basis (an `n × d` matrix) for the
  lattice spanned by the rows of the `n × d` matrix `m`. The result spans the
  same lattice (same `Z`- / `Z[i]`-module), so `Abs[Det]` and every linear
  relation in the right null space are preserved exactly.
- Entries may be **integers, rationals, Gaussian integers, or Gaussian
  rationals** (`Complex[a, b]` with exact `a`, `b`).
- Exact Lenstra–Lenstra–Lovász reduction (`δ = 3/4`) performed entirely in
  GMP rational arithmetic — **no floating point** — so it is correct for both
  machine-size and arbitrary-precision (bignum) inputs. Reproduces the
  documented Wolfram output on the reference examples, including the 10²⁰
  `Pi/4 + ArcTan[1/239] - 4 ArcTan[1/5] == 0` integer-relation lattice.
- Each Gaussian-rational scalar is a pair of GMP `mpq_t` (`GRat`). The
  Gram–Schmidt orthogonalisation uses the Hermitian inner product
  `⟨x, y⟩ = Σ x_k conj(y_k)` (one code path for real and Gaussian lattices);
  size reduction rounds the `μ` coefficients to the nearest Gaussian integer.
  The Gram–Schmidt data (`μ`, `|b*|²`) is computed once and maintained
  incrementally — in place on size reduction and via the conjugate-aware
  Cohen swap formulas on a Lovász swap — so no full recomputation is needed.
- The rows must be linearly independent. Diagnostics: `LatticeReduce::argx`
  (argument count), `LatticeReduce::matrix` (not a non-empty rectangular
  matrix), `LatticeReduce::latm` (a non-rational entry), `LatticeReduce::dep`
  (linearly dependent rows).
- Tests: `tests/test_latticereduce.c` (exact reduced bases, `Abs[Det]` and
  null-space-relation invariants, bignum relation finding, Gaussian and
  rational entries, and all diagnostics). Valgrind-clean — no leaks
  attributable to the new code.

## ExtendedGCD (2026-06-04)

New builtin `ExtendedGCD` (`src/arithmetic.c`, next to `builtin_gcd`),
the multi-argument extended / Bézout GCD. Attributes `Listable`, `Protected`
(deliberately *not* `Flat`/`Orderless`/`OneIdentity` — unlike `GCD` — because
the cofactor list is positional and order-sensitive). Symbol registered in
`sym_names.c`; docstring in `info.c`; registered (builtin + attributes) in
`core.c`.

- `ExtendedGCD[n1, n2, ...]` returns `{g, {r1, r2, ...}}` where
  `g == GCD[n1, ...]` and `g == r1 n1 + r2 n2 + ...`. The leading element is
  exactly the GCD.
- Integer-only, working with **both machine integers and GMP bigints**: every
  argument folds through GMP's `mpz_gcdext` pairwise
  (`gcd(running_g, n_i) = s·running_g + t·n_i`), scaling the accumulated
  cofactors by `s` and appending `t` at each step. `running_g` starts at `0`
  and stays non-negative, so `g` is non-negative and the sign convention
  matches Mathematica. Cofactors demote back to `EXPR_INTEGER` when they fit.
- Threads element-wise over lists (e.g. `ExtendedGCD[3, {5, 15}]` →
  `{{1, {2, -1}}, {3, {1, 0}}}`).
- Edge cases: `ExtendedGCD[]` → `{0, {}}`; `ExtendedGCD[n]` → `{|n|, {±1}}`;
  zeros and negatives handled (e.g. `ExtendedGCD[-6]` → `{6, {-1}}`).
- Diagnostics: an inexact (`Real`) argument emits `ExtendedGCD::exact`
  ("Argument … is not an exact number"); an all-exact-but-non-integer
  (`Rational`) call emits `ExtendedGCD::egcd` ("Arguments … should be
  integers"); symbolic arguments leave the call unevaluated silently. All
  diagnostics respect the arithmetic-warning mute used by internal callers.
- Extensive unit tests in `tests/test_extended_gcd.c` (documented examples,
  GCD/Bézout identity property checks, single-arg / empty / zero / negative
  edge cases, bigint and mixed machine/bigint arguments, listable threading,
  all three diagnostic paths, attribute / docstring / interned-symbol
  introspection, and a repeated-evaluation leak stress loop). No leaks from
  the new code (macOS valgrind reports only Accelerate/dyld system noise).

## GroebnerBasis: CoefficientDomain — Integers (strong Gröbner basis over Z) (2026-06-04)

`CoefficientDomain -> Integers` now computes a strong Gröbner basis over the
ring Z via the new `gb_strong_buchberger` (`src/poly/groebner.{c,h}`,
dispatched from `src/poly/groebnerbasis.c`). Any parameter symbol is an
ordinary variable of Z[vars]. Buchberger's pair criteria do not hold over Z,
so completion processes all pairs and, per pair, forms both the S-polynomial
(coefficient lcm, `gb_spoly_Z`) and a Bézout G-polynomial (coefficient gcd via
`mpz_gcdext`, `gb_gpoly_Z`); each is strong-reduced (`gb_strong_reduce` —
cancels a term only when the divisor's leading integer coefficient divides it,
`gb_lt_divides` for minimalisation). Denominators are cleared but content is
preserved (`gb_poly_clear_denoms`) since content is ideal data over Z, and the
constant-collapse shortcuts to `{1}` are skipped for this domain (a constant
`c` denotes `c·Z[x]`). The result is minimalised, tail-reduced,
leading-coefficient-positive, and LM-ascending. Reproduces Mathematica's
documented five-element strong basis exactly; classic GCD combinations
(`<2x, 3x> = <x>`, `<2x+2, 3x+3> = <x+1>`) verified. Tests added
(`test_coeff_domain_integers_{strong_basis,gcd_combination,documented}`),
replacing the former fallback test.

## GroebnerBasis: CoefficientDomain — RationalFunctions (2026-06-04)

`CoefficientDomain -> RationalFunctions` now computes the Gröbner basis over
the field Q(params), where the parameters (the non-main symbols) are units
(`src/poly/groebner.{c,h}`, `src/poly/groebnerbasis.c`). The ring basis over
Q[params][main vars] (parameters placed last under lex, so a ring leading term
projects onto the field leading term — the localisation theorem) is autoreduced
by the new `gb_rational_function_reduce`: generators whose main leading monomial
is a multiple of another's are dropped, and survivors are tail-reduced. All
arithmetic stays integral via pseudo-reduction
(`f := lc_main(g)·f − x^q·lc_main(f)·g`, helpers `gb_poly_mul`, `gb_lc_main`,
`gb_lead_group`, `gb_field_reduce`), with each intermediate made primitive over
Z. Coefficients are kept as primitive integer polynomials in the parameters,
matching Mathematica's presentation (e.g.
`GroebnerBasis[{a x^2+5x-1, 2x+3xy+y^2}, {x,y}, CoefficientDomain ->
RationalFunctions]` returns the two-element basis, dropping `2x+3xy+y^2`).
Honoured in the plain form only (no elimination block) and a no-op when there
are no parameters. Tests added in `tests/test_groebner.c`
(`test_coeff_domain_rational_functions{,_no_params,_eliminates}`).

## GroebnerBasis: CoefficientDomain — Polynomials[x] and InexactNumbers[p] (2026-06-04)

`GroebnerBasis` now honours `CoefficientDomain` beyond the default
`Rationals`/`Automatic` (`src/poly/groebnerbasis.c`, `src/poly/groebner.{c,h}`):

- **`Polynomials[x, ...]`** — the named symbols become coefficient-ring
  variables, i.e. parameters. Implemented by folding them into the existing
  explicit-parameter set, so the basis matches the default auto-parameter
  behaviour (`GroebnerBasis[polys, vars, CoefficientDomain -> Polynomials[a]]`
  ≡ `GroebnerBasis[polys, vars]`).
- **`InexactNumbers[p]`** — the exact rational basis, made monic, with
  coefficients rendered as `p`-digit MPFR reals (bits = ⌈p·log₂10⌉) via the new
  `gb_to_expr_inexact`. Inexact (`Real`) input coefficients are now
  force-rationalised before the Buchberger run (`internal_force_rationalize` in
  `normalise_polynomial`), so real-coefficient systems evaluate in every domain
  instead of being left unevaluated. This is exact-compute-then-round, not a
  genuine floating-point Buchberger; it reproduces Mathematica's documented
  output for exact/cleanly-rationalisable inputs.

Option parsing was generalised: `CoefficientDomain` now recognises `Integers`,
`RationalFunctions`, `Polynomials[...]`, and `InexactNumbers`/`InexactNumbers[p]`
(a `GBCoeffDomain` enum on `GBOptions`). `Integers` and `RationalFunctions` are
recognised but still computed over the rationals pending their dedicated
algorithms. Tests added in `tests/test_groebner.c`
(`test_coeff_domain_polynomials_equals_default`,
`test_coeff_domain_inexact_numbers`, `test_real_input_rationalised`).

Known limitation (pre-existing, in `src/print.c`): negative `EXPR_MPFR`
coefficients in a sum print as `+ -12.0 y` rather than `- 12.0 y` — the
printer's sign-folding handles `EXPR_REAL` but not arbitrary-precision reals.
The numeric values are correct.

## Fix: trig-rationalisation memory leak (2026-06-01)

Fixed a memory leak in `simp_trig_rational` (`src/simp/trigrat.c`) exercised by
`Simplify` on polynomial inputs (e.g.
`Simplify[(b^2 c - b c^2 - a (b^2-c^2) + a^2(b-c)) - (b-a)(c-a)(c-b)]`). Five
tree-walk helpers (`tr_walk_subst`, `tr_subst_back`, `tr_rewrite_pass`,
`tr_subst_sym_zero`) `calloc`'d a temporary `args_new` pointer array and passed
it to `expr_new_function`, which *copies* the array into its own buffer rather
than taking ownership — so each call leaked the temporary array (16/32 bytes per
recursion node). Each site now frees `args_new` after constructing the node. No
behavioural change; `leaks` now reports 0 bytes leaked (was 432).

## ToeplitzMatrix (2026-06-01)

New builtin `ToeplitzMatrix` (`src/linalg/toeplitzmat.c`), a matrix that is
constant along its diagonals. Attribute `Protected`. Symbol registered in
`sym_names.c`; docstring in `info.c`; registered in `linalg_init`
(`src/linalg/linalg.c`).

- `ToeplitzMatrix[n]` gives the `n x n` Toeplitz matrix whose first row and
  column are the successive integers `1..n`; the `(i, j)` entry is
  `|i - j| + 1`, so the matrix is symmetric.
- `ToeplitzMatrix[{c1, ..., cn}]` gives the `n x n` symmetric Toeplitz matrix
  whose first column (and first row) is the given list — plain symmetric, with
  no conjugation.
- `ToeplitzMatrix[{c1, ..., cm}, {r1, ..., rn}]` gives the `m x n` Toeplitz
  matrix with the first list down the first column and the second across the
  first row. The `(i, j)` entry is `c_{i-j+1}` when `i >= j`, and `r_{j-i+1}`
  otherwise.
- Entries are copied verbatim, so symbolic, exact, complex, machine and
  arbitrary-precision (e.g. ``1`20``) entries all flow through unchanged. For
  `m = n` the matrix is symmetric.
- The shared corner `c_1` should equal `r_1`; if not, the column element is
  used (the formula reads `c_1` on the diagonal, never `r_1`) and a
  `ToeplitzMatrix::crs` warning is emitted (the matrix is still built).
  `ToeplitzMatrix::argb` reports a zero-argument call; a first argument that is
  neither a positive integer nor a list, and over-arity calls, are returned
  unevaluated.
- Extensive unit tests in `tests/test_toeplitzmatrix.c` (integer / symbolic /
  complex square forms, wide and tall rectangular forms, the corner-mismatch
  warning path, symmetry and diagonal-constancy identities, `Dimensions`,
  machine / MPFR precision propagation, all unevaluated / diagnostic paths,
  attributes / docstring / interned symbol, and a repeated-evaluation leak
  stress loop). `leaks`-clean.

## VandermondeMatrix (2026-06-01)

New builtin `VandermondeMatrix` (`src/linalg/vandermondemat.c`), a matrix whose
rows are the successive powers of a sequence of nodes; the `(i, j)` entry is
`xi^(j-1)`. Attribute `Protected`. Symbol registered in `sym_names.c`;
docstring in `info.c`; registered in `linalg_init` (`src/linalg/linalg.c`).

- `VandermondeMatrix[{x1, ..., xn}]` gives the `n x n` Vandermonde matrix on the
  nodes `xi`; `VandermondeMatrix[{x1, ..., xn}, k]` gives the `n x k` matrix.
- The first column is all ones, the second is the nodes, the third their
  squares, and so on. Entries are built as `Power[xi, j-1]` and simplified by
  the evaluator, so symbolic nodes stay as `Power` expressions while numeric
  powers fold to their value. The first column is the literal integer `1`, so a
  zero node reads as `1` there (not `Indeterminate`), matching the
  interpolation semantics.
- The nodes need not be numerical or distinct. Precision flows from the nodes
  themselves (e.g. ``2`20``) or from wrapping the result in `N`. For distinct
  nodes, `LinearSolve[V, b]` recovers the coefficients of the interpolating
  polynomial through `{xi, bi}`, and `Det[V]` is the product of node
  differences `prod_{i<j} (xj - xi)`.
- `VandermondeMatrix::argt` reports a zero-argument call. An empty node list, a
  non-list first argument, a non-positive integer `k`, the single-argument
  structured-array conversion form `VandermondeMatrix[vmat]` (unsupported —
  Mathilda has no structured-array representation), and over-arity calls are
  returned unevaluated.
- Extensive unit tests in `tests/test_vandermondematrix.c` (symbolic / numeric
  square forms, single node, rectangular `n x k` forms, the `0^0 = 1` /
  all-ones-first-column convention, `Dimensions`, numeric and symbolic
  determinant identities, `LinearSolve` interpolation, machine / MPFR precision
  propagation, all unevaluated / diagnostic paths, attributes / docstring /
  interned symbol, and a repeated-evaluation leak stress loop). `leaks`-clean.

## HankelMatrix (2026-06-01)

New builtin `HankelMatrix` (`src/linalg/hankelmat.c`), a matrix that is
constant along its antidiagonals. Attribute `Protected`. Symbol registered in
`sym_names.c`; docstring in `info.c`; registered in `linalg_init`
(`src/linalg/linalg.c`).

- `HankelMatrix[n]` gives the `n x n` Hankel matrix whose first row and column
  are the successive integers `1..n`, with zeros below the main antidiagonal.
- `HankelMatrix[{c1, ..., cm}]` gives the `m x m` Hankel matrix whose first
  column is the given list (zeros below the antidiagonal).
- `HankelMatrix[{c1, ..., cm}, {r1, ..., rn}]` gives the `m x n` Hankel matrix
  with the first list down the first column and the second across the last
  row. The `(i, j)` entry is `c_{i+j-1}` when `i + j - 1 <= m`, and
  `r_{i+j-m}` otherwise.
- Entries are copied verbatim, so symbolic, exact, complex, machine and
  arbitrary-precision (e.g. ``1`20``) entries all flow through unchanged; the
  antidiagonal fill is the exact integer `0`. For `m = n` the matrix is
  symmetric.
- The shared corner `c_m` should equal `r_1`; if not, the column element is
  used and a `HankelMatrix::crs` warning is emitted (the matrix is still
  built). `HankelMatrix::argb` reports a zero-argument call; a first argument
  that is neither a positive integer nor a list, and over-arity calls, are
  returned unevaluated.
- Extensive unit tests in `tests/test_hankelmatrix.c` (integer / symbolic /
  complex square forms, wide and tall rectangular forms, the corner-mismatch
  warning path, symmetry and antidiagonal-constancy identities, `Dimensions`,
  machine / MPFR precision propagation, exact-zero fill, all unevaluated /
  diagnostic paths, attributes / docstring / interned symbol, and a
  repeated-evaluation leak stress loop). `leaks`-clean.

## HilbertMatrix (2026-06-01)

New builtin `HilbertMatrix` (`src/linalg/hilbertmat.c`), the `m x n` Hilbert
matrix whose `(i, j)` entry is `1/(i + j - 1)`. Attribute `Protected`.
Symbol registered in `sym_names.c`; docstring in `info.c`; registered in
`linalg_init` (`src/linalg/linalg.c`).

- `HilbertMatrix[n]` gives the `n x n` Hilbert matrix; `HilbertMatrix[{m, n}]`
  gives the `m x n` matrix. Entries are exact `Rational`s by default.
- The `WorkingPrecision` option selects the entry representation:
  `Infinity` (default) → exact `Rational`s; `MachinePrecision` → machine
  `Real`s; a digit count `d` → `d`-digit MPFR `Real`s (degrading to machine
  precision when `d` is at or below machine precision, or when built without
  MPFR).
- Diagnostics leave the call unevaluated: `::argx` (0 arguments), `::dims`
  (dimension spec that is not a positive integer or a pair of positive
  integers), and `::nonopt` (trailing non-option arguments).
- Extensive unit tests in `tests/test_hilbertmatrix.c` (exact square /
  rectangular matrices, symmetry, `Transpose` dimension swap, known
  determinants and the exact integer inverse, machine / MPFR precision
  paths, all diagnostic paths, attributes / docstring / interned symbol,
  and a repeated-evaluation leak stress loop). Valgrind/`leaks`-clean.

## Ratios (2026-06-01)

New builtin `Ratios` (`src/list.c`), the multiplicative analog of
`Differences` and inverse of `FoldList[Times, ...]`. Attribute `Protected`.
Symbol registered in `sym_names.c`; docstring in `info.c`.

- `Ratios[list]` gives the successive ratios `{l[[2]]/l[[1]], l[[3]]/l[[2]],
  ...}`, of length `s - 1`. Ratios divides successive elements by preceding
  ones, so `Ratios[{x1, x2}]` gives `{x2/x1}`.
- `Ratios[list, n]` applies the ratio operator `n` times, giving length
  `s - n`. `n` must be a non-negative integer (`n = 0` returns `list`
  unchanged); e.g. `Ratios[{a, b, c, d, e}, 2]` is
  `{(a c)/b^2, (b d)/c^2, (c e)/d^2}`.
- `Ratios[list, {n1, n2, ...}]` gives the successive `nk`-th ratios at level
  `k` of a nested list, and is equivalent to
  `Ratios[Ratios[list, n1], {0, n2, ...}]`. Each `nk` must be a non-negative
  integer.
- Division threads element-wise over sublists via the `Listable` `Power`/`Times`
  that `Divide` rewrites to, so for a matrix `m`, `Ratios[m]`
  (= `Ratios[m, 1]` = `Ratios[m, {1, 0}]`) takes ratios of successive rows
  within each column, while `Ratios[m, {0, 1}]` takes ratios of columns within
  each row.
- The head of the input is preserved; a list of length `<= n` reductions yields
  the empty list. Works on machine integers, GMP arbitrary-precision integers
  (exact `Rational` results), machine-precision doubles, and symbolic
  expressions. First ratios are constant for a geometric sequence, and
  `FoldList[Times, x, Ratios[list]]` reconstructs the original list.

Implemented by mirroring the `Differences` helpers: `ratio_divide`,
`ratio_once`, `ratio_n`, and `ratio_levels` build `Divide[hi, lo]` and let the
evaluator reduce it. 28 unit tests in `tests/test_ratios.c`; valgrind/`leaks`
clean.

## GroebnerBasis: weight-matrix MonomialOrder + GroebnerWalk method (2026-06-04)

`GroebnerBasis` now accepts a custom term order given by an integer **weight
matrix** and a new `Method -> "GroebnerWalk"` engine. Both extend the existing
Buchberger subsystem (`src/poly/groebner.{c,h}`, `groebnerbasis.c`) and share
the same weight-matrix machinery.

**Weight-matrix `MonomialOrder`.** `MonomialOrder -> {{...}, ...}` (a
rectangular integer matrix with one column per main variable) ranks monomials
by the lexicographic comparison of the weight vectors `M·a` vs `M·b`. The
standard orders are special cases (the identity matrix is `Lexicographic`,
`{{1,1,1},{0,0,-1},{0,-1,0}}` is `DegreeReverseLexicographic`).

- New `GB_ORDER_MATRIX` order plus a borrowed, caller-owned `GBWeightMatrix`
  carried on each `GBPoly`. Comparison (`cmp_matrix_desc`) accumulates row
  weights in `int64` with `__builtin_*_overflow` guards, falling back to exact
  `mpz` only on overflow, so the hot compare path stays fast.
- `gb_wmat_validate` is a hard gate: it requires full column rank (via exact
  rational Gaussian elimination — otherwise distinct monomials could tie and
  `gb_poly_normalize` would silently merge them) **and** a positive first
  non-zero entry in every column (well-foundedness, so Buchberger terminates).
  Invalid matrices, or matrices combined with elimination/parameter variables,
  emit `GroebnerBasis::nimpl` and fall back to `Lexicographic`.

**`Method -> "GroebnerWalk"`** (new file `src/poly/groebnerwalk.c`,
`gb_groebner_walk`). Implements the Collart–Kalkbrener–Mall walk: compute a
cheap `DegreeReverseLexicographic` basis, then convert it to the target order
(Lexicographic or a weight matrix) by walking the weight path
`w(t) = (1-t)·(1,…,1) + t·σ` through the Gröbner fan. At each cone wall it
takes the initial-form ideal's basis under the target order and lifts it back
via a new quotient-tracking division `gb_divmod`; `gb_initial_form` extracts
the `w`-initial forms. The path parameter is exact (`mpq`), so simultaneous
wall crossings are handled without perturbation. By reduced-GB uniqueness the
output is identical to a direct Buchberger run, so any safety-guard trip
(overflow, a degenerate lift, or a step-count blow-up) falls back to
`gb_buchberger` with no loss of correctness. The walk is honoured only without
elimination/parameter variables; otherwise Buchberger runs directly. The
shared finalize tail (interreduce → fixed point → primitive-over-Z → sort) was
factored out of `gb_buchberger` into `gb_finalize_basis`.

Docstring (`info.c`) and the `GroebnerBasis` reference
(`docs/spec/builtins/structural-manipulation.md`) updated; `Method ->
"GroebnerWalk"` and explicit weight matrices removed from the "deferred" list.
New differential tests in `tests/test_groebner.c` (identity matrix ≡
Lexicographic, grevlex matrix ≡ DegreeReverseLexicographic, walk ≡ Buchberger
for lex and matrix targets on the unit-sphere/cyclic system, single-variable
and `⟨1⟩` short-circuits, and the rank-deficient-matrix fallback). `leaks`-clean.
