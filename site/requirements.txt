# Build dependencies for the Mathilda documentation site (MkDocs Material).
# CI installs these; the generated Markdown is committed, so no C toolchain is
# needed to build the site.
mkdocs-material>=9.5
mkdocs-awesome-pages-plugin>=2.9
pymdown-extensions>=10.0
