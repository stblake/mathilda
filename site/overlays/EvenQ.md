### Worked examples

```mathematica
In[1]:= EvenQ[4]
Out[1]= True

In[2]:= EvenQ[7]
Out[2]= False
```

### Notes

`EvenQ` returns `True` only for even integers (`Integer` or `BigInt`); any non-integer argument gives `False`.
