### Worked examples

```mathematica
In[1]:= a =!= b
Out[1]= True

In[2]:= a =!= a
Out[2]= False
```

### Notes

`UnsameQ` (`=!=`) is the structural negation of `SameQ`; it always decides to `True` or `False`.
