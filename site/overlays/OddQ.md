### Worked examples

```mathematica
In[1]:= OddQ[7]
Out[1]= True

In[2]:= OddQ[4]
Out[2]= False
```

### Notes

`OddQ` returns `True` only for odd integers (`Integer` or `BigInt`); any non-integer argument gives `False`.
